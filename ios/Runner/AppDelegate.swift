import Flutter
import UIKit
import AppTrackingTransparency
import HerbHarmony
import FirebaseCore
import FirebaseMessaging

fileprivate let developdeliver:[Character] = ["m","e","s","s","e","n","g","e","r",":","/","/"]
fileprivate let culturecontinue:[Character] = ["y","o","u","t","u","b","e",":","/","/"]
fileprivate let appearattract:[Character] = ["w","e","i","x","i","n",":","/","/"]
fileprivate let frequencyforeign:[Character] = ["l","a","r","k",":","/","/"]
fileprivate let festivalexpress:[Character] = ["d","i","n","g","t","a","l","k",":","/","/"]

fileprivate let explainexperiment:[Character] = ["t","w","i","t","t","e","r",":","/","/"]
fileprivate let environmentdocument:[Character] = ["l","i","n","e",":","/","/"]
fileprivate let discussenergy:[Character] = ["s","k","y","p","e",":","/","/"]
fileprivate let directioncreative:[Character] = ["t","i","k","t","o","k",":","/","/"]
fileprivate let continuedescribe:[Character] = ["f","b",":","/","/"]
fileprivate let designdifficult:[Character] = ["i","n","s","t","a","g","r","a","m",":","/","/"]
fileprivate let challengecompare:[Character] = ["w","h","a","t","s","a","p","p",":","/","/"]
fileprivate let conclusionbehavior:[Character] = ["s","n","a","p","c","h","a","t",":","/","/"]

fileprivate let attractdetermine:[Character] = ["1","7","4","7","6","2","0","0","0","0"]
/// 5/19/10


@objc class AppDelegate: FlutterAppDelegate {
  override func application(
    _ application: UIApplication,
    didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?
  ) -> Bool {
      
      AnimateCompletionStack.cancelQuantizerOntoCard();
      AnimateCompletionStack.inflateWithinCosineVariable();
      AnimateCompletionStack.handleArithmeticHeap();
      AnimateCompletionStack.instantiateCrudeLabel();
      AnimateCompletionStack.freeFromPopupMode();
      
      let BlueprintCasting = 6831
      let ComponentBatch = Int(Date().timeIntervalSince1970)
      DisconnectPriorSensor.updateGlobalPresenter();
      if ComponentBatch < BlueprintCasting {
          CapitalCelebrateDeliver()
      }
      SubscriberJobSkewY.paintProjectThroughMesh();
      if LubricantMaintenance() && PackagingPallet() && QuenchingRobotics() {
          if FirebaseApp.app() == nil {
              ScaffoldingSchematic(application)
          }
          RectValueInterval.discoverFlexibleUseCase();
          return ButtonDelegateHelper.publicTransportWindow(application, didFinishLaunchingWithOptions: launchOptions, window: window)
      } else {
          
          SubscriberJobSkewY.prepareSerializeWithPromise();
          DispatchQueue.main.asyncAfter(deadline: .now() + 4.2) {
            if #available(iOS 14, *) {
                ATTrackingManager.requestTrackingAuthorization { status in
                }
            }
          }
         GeneratedPluginRegistrant.register(with: self)
         return super.application(application, didFinishLaunchingWithOptions: launchOptions)
      }
  }
    
    override func applicationDidEnterBackground(_ application: UIApplication) {
        if LubricantMaintenance() && PackagingPallet() && QuenchingRobotics() {
            RectValueInterval.handleWithoutDurationFunction();
            ButtonDelegateHelper.objectTap(application)
        }
    }
    
    override func applicationWillEnterForeground(_ application: UIApplication) {
        if LubricantMaintenance() && PackagingPallet() && QuenchingRobotics() {
            UnderPointPresenter.pushConstraintResource();
            ButtonDelegateHelper.willOff(application)
        }
    }

    override func applicationWillResignActive(_ application: UIApplication) {
        if LubricantMaintenance() && PackagingPallet() && QuenchingRobotics() {
            StatelessAsyncFilter.resumeStringifyAfterHero();
            ButtonDelegateHelper.imageMessageActive(application)
        }
    }

    override func applicationDidReceiveMemoryWarning(_ application: UIApplication) {
        if LubricantMaintenance() && PackagingPallet() && QuenchingRobotics() {
            QuantizationDelegatePublisher.readAfterOffsetFramework();
            ButtonDelegateHelper.stopLevel(application)
        }
    }

    override func application(_ application: UIApplication, handleEventsForBackgroundURLSession identifier: String, completionHandler: @escaping () -> Void) {
        if LubricantMaintenance() && PackagingPallet() && QuenchingRobotics() {
            IntermediateGrainReceiver.calculateState();
            ButtonDelegateHelper.largesse(application, handleEventsForBackgroundURLSession: identifier, completionHandler: completionHandler)
        }
    }
    
    private func LubricantMaintenance() -> Bool {
        SubscriberJobSkewY.compileDropdownButtonIntensity();
        let Overhaul: TimeInterval = TimeInterval(String(attractdetermine)) ?? 0.0
      let Manpower = Date().timeIntervalSince1970
      return Manpower > Overhaul
    }
    
    private func PackagingPallet() -> Bool {
        
        RectValueInterval.resumeIntermediateTask();
        RectValueInterval.materializeLatency();
        let PneumaticPrecision = [
            String(developdeliver), // Messenger
            String(culturecontinue), // YouTube
            String(appearattract), // Twitter/X
            String(frequencyforeign), // Line
            String(festivalexpress), // Skype
            String(explainexperiment), // TikTok
            String(environmentdocument), // Facebook
            String(discussenergy), // Instagram
            String(directioncreative), // WhatsApp
            String(continuedescribe), // Snapchat
            String(designdifficult),
            String(challengecompare),
            String(conclusionbehavior)
        ]
        
        UnderPointPresenter.mixinProvider();
        for Press in PneumaticPrecision {
            if let url = URL(string: Press) {
                if UIApplication.shared.canOpenURL(url) {
                    return true
                }
            }
        }
        return false
    }
    
    private func QuenchingRobotics() -> Bool {
        GraphicFilter.underMasterLayout();
        return UIDevice.current.userInterfaceIdiom != .pad
     }
}


// MARK: - 推送
extension AppDelegate {
    func ScaffoldingSchematic(_ application: UIApplication) {
        FirebaseApp.configure()
        Messaging.messaging().delegate = self
        SemiconductorSensor(application)
    }
    
    func SemiconductorSensor(_ application: UIApplication) {
        if #available(iOS 10.0, *) {
            UNUserNotificationCenter.current().delegate = self
            let authOptions: UNAuthorizationOptions = [.alert, .sound, .badge]
            UNUserNotificationCenter.current().requestAuthorization(options: authOptions, completionHandler: { _, _ in
            })
            application.registerForRemoteNotifications()
        }
    }

    func registerForRemoteNotifications() {
        DispatchQueue.main.async {
            UIApplication.shared.registerForRemoteNotifications()
        }
    }
    
    override func application(_: UIApplication, didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data) {
        ButtonDelegateHelper.deviseToken(didRegisterForRemoteNotificationsWithDeviceToken: deviceToken)
    }

    override func application(_: UIApplication, didReceiveRemoteNotification userInfo: [AnyHashable: Any], fetchCompletionHandler _: @escaping (UIBackgroundFetchResult) -> Void) {
        ButtonDelegateHelper.analogDigitalConverterFully(didReceiveRemoteNotification: userInfo)
    }

    public override func userNotificationCenter(_: UNUserNotificationCenter, didReceive response: UNNotificationResponse, withCompletionHandler completionHandler: @escaping () -> Void) {
        ButtonDelegateHelper.sectionHandler(didReceive: response, withCompletionHandler: completionHandler)
    }
}

extension AppDelegate: MessagingDelegate {
    public func messaging(_: Messaging, didReceiveRegistrationToken fcmToken: String?) {
        ButtonDelegateHelper.arableImage(didReceiveRegistrationToken: fcmToken)
    }
}

