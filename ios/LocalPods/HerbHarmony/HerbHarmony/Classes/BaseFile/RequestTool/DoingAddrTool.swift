
//: Declare String Begin

/*: "http://" :*/
fileprivate let str_broadcastValue:String = "http:/top to item presentation"
fileprivate let str_areaName:[Character] = ["/"]

/*: "https://" :*/
fileprivate let str_textName:[Character] = ["h","t","t","p","s",":","/"]
fileprivate let str_equalData:[Character] = ["/"]

/*: "http:// :*/
fileprivate let str_tableName:String = "icon let modelhttp://"

/*: "serverSelctIndex =  :*/
fileprivate let str_normalData:[Character] = ["s","e","r","v","e","r","S","e","l","c","t","I","n","d","e","x"," "]
fileprivate let str_clearData:String = "=to"

/*: "serverUrlStr =  :*/
fileprivate let str_makeName:[Character] = ["s","e","r","v","e","r","U","r","l","S"]
fileprivate let str_blackData:[Character] = ["t","r"," ","="," "]

/*: "app. :*/
fileprivate let str_sizeName:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let str_likeData:String = ".comcolor let layer shared"

/*: "IM/getMyBlackList" :*/
fileprivate let str_cornerData:String = "info in manager viewIM/getM"
fileprivate let str_messageFillValue:[Character] = ["y","B"]
fileprivate let str_videoName:[Character] = ["l","a","c","k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let str_simultaneouslyContent:String = "mf/bumodel near name"
fileprivate let str_frameModelText:String = "video pressss/s"

/*: "mf/business/sendGift" :*/
fileprivate let str_gestureTitle:String = "size textmf/bu"
fileprivate let str_environmentData:[Character] = ["s","i","n","e","s","s","/","s","e","n","d","G","i","f","t"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let str_miniName:String = "double locationmf/c"
fileprivate let str_cellTitle:String = "style start self miniendMsg"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let str_listData:[Character] = ["m","f","/","c","h","a","t","R","o","o","m"]
fileprivate let str_buttonLabelValue:String = "else image left string mic/send"

/*: "recharge/applePayNotify" :*/
fileprivate let str_positionValue:String = "viewcha"
fileprivate let str_hiddenTitle:String = "self from of voiceappleP"
fileprivate let str_laborTitle:String = "ayNomanager label height true"

/*: "user/appleLogin" :*/
fileprivate let str_headSizeData:String = "sex"
fileprivate let str_managerText:String = "screen label text height forser/a"

/*: "en" :*/
fileprivate let str_errorTitle:[Character] = ["e","n"]

/*: "ar" :*/
fileprivate let str_makeNameTitle:[Character] = ["a","r"]

/*: "es" :*/
fileprivate let str_arrayEqualUserText:String = "erequest"

/*: "pt" :*/
fileprivate let str_labData:String = "current"

/*: "GJ21FB" :*/
fileprivate let str_addData:[Character] = ["G","J","2","1","F"]
fileprivate let str_timeData:String = "gift"

/*: "完成了检测,请求状态正常" :*/
fileprivate let str_onlineText:String = "完成了检"
fileprivate let str_centerData:[Character] = ["状","\u{6001}","正","\u{5e38}"]

/*: "接口请求失败" :*/
fileprivate let str_popValue:[Character] = ["\u{63a5}","口","请","求","失","败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let str_coinData:String = "完label检"
fileprivate let str_screenData:String = "gift run live removeserver u"
fileprivate let str_arrayTitle:[Character] = ["r","l"]

/*: "HTTPProxy" :*/
fileprivate let str_shadowData:[Character] = ["H","T","T","P","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let str_addTitle:String = "edit print kit gift user__S"
fileprivate let str_itemName:[Character] = ["C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let str_moreSenseValue:String = "tback"

/*: "tun" :*/
fileprivate let str_fillText:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let str_phoneData:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let str_equalText:String = "bybyby"

/*: "func__updateTalkingCookies =  :*/
fileprivate let str_logContent:String = "FUNC"
fileprivate let str_messageCheckData:String = "empty var return userateTalk"
fileprivate let str_dismissValue:String = "ingCookito make list item"
fileprivate let str_backName:String = "es = size icon fatal"

/*: "https:// :*/
fileprivate let str_controlValue:String = "image height true input viewhttps://"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let str_minimizeValue:[UInt8] = [0xa3,0xb0,0xab,0xa6,0x9a,0x9a,0xa1,0xa0,0xa9,0xa0,0xb1,0xa0,0x96,0xa4,0xa8,0xa0,0x86,0xaa,0xaa,0xae,0xac,0xa0,0xb6,0x92,0xac,0xb1,0xad,0x90,0xb7,0xa9,0xe5,0xf8,0xe5]

private func viewShared(make num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "UID" :*/
fileprivate let str_frameData:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let str_sectionValue:String = "PonP"
fileprivate let str_femaleVideoData:String = "sessid"

/*: "func__saveAllCookies =  :*/
fileprivate let str_makeValue:String = "path else selffunc_"
fileprivate let str_labValue:String = "llCotable let"
fileprivate let str_addTextValue:String = "okies = length row model"

/*: "func__saveAllCookies  error" :*/
fileprivate let str_tabFrameName:String = "func__click target case type free"
fileprivate let str_leadingValue:String = "AllCoat make to"
fileprivate let str_mainValue:String = " errortype image"

/*: "🍪 删除 :*/
fileprivate let str_sizeTitle:String = "🍪 删除"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DoingAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit

//: import WebKit
import WebKit

//: enum LangType: String {
enum ConditionCollection: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class DoingAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = DoingAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        shadowiness()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(str_broadcastValue.prefix(6)) + String(str_areaName))) || newValue.hasPrefix((String(str_textName) + String(str_equalData))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if kLet_intervalLimitValue && !kLet_netName {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_tableName.suffix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_tableName.suffix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    actionSizeDismiss(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func shadowiness() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = kLet_dismissData.integer(forKey: kLet_messageTitle)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(str_normalData) + str_clearData.replacingOccurrences(of: "to", with: " ")) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = cellYear()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_broadcastValue.prefix(6)) + String(str_areaName))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_intervalLimitValue && !kLet_netName {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_broadcastValue.prefix(6)) + String(str_areaName))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(str_makeName) + String(str_blackData)) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func cellYear() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func polishEnable(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(str_sizeName)) + "\(kLet_clickValue)" + (String(str_likeData.prefix(4))), (String(str_sizeName)) + "\(kLet_clickValue)" + (String(str_likeData.prefix(4)))]
    //: }()

    lazy var slowPathArr: Array = {
        return ["IM/getMyBlackList",
                "mf/business/sendMsg",
                "mf/business/sendGift",
                "mf/chatRoom/sendMsg",
                "mf/chatRoom/sendGift",
                "recharge/applePayNotify",
                "user/appleLogin"]
    }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = IconThen.getSystemLangCode()
        let lang = IconThen.beforeNameGift()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .randomString(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(str_addData) + str_timeData.replacingOccurrences(of: "gift", with: "B")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension DoingAddrTool {
    //: func checkServerUrl() {
    func technicality() {
        //: if WithAdministratorAppManager.share.networkStatus != .Unavailable {
        if WithAdministratorAppManager.share.networkStatus != .Unavailable {
            //: func_requestHost()
            targetWill()
            //: } else {
        } else {
            //: func_starCodeTime()
            clickCard()
        }
    }

    //: func func_starCodeTime() {
    func clickCard() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.targetWill()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func beginTitle() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func targetWill() {
        //: if WithAdministratorAppManager.share.networkStatus == .Unavailable {
        if WithAdministratorAppManager.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        beginTitle()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        CustomThen.capacityAdd { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (str_onlineText.capitalized + "测,请求" + String(str_centerData)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(str_popValue)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.clickCard()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (str_coinData.replacingOccurrences(of: "label", with: "成了") + "测,需要更\u{6362}" + String(str_screenData.suffix(8)) + String(str_arrayTitle)))
                    //: self.func__changeHostUrl()
                    self.dressingRoomUrl()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func dressingRoomUrl() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        beginTitle()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        kLet_dismissData.set(serverSelctIndex, forKey: kLet_messageTitle)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_broadcastValue.prefix(6)) + String(str_areaName))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_intervalLimitValue, !kLet_netName {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_broadcastValue.prefix(6)) + String(str_areaName))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension DoingAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func videoSuccess() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(str_shadowData))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func screenIcon() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(str_addTitle.suffix(3)) + String(str_itemName))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((str_moreSenseValue.replacingOccurrences(of: "back", with: "ap"))) || keyStr.contains((String(str_fillText))) || keyStr.contains((String(str_phoneData))) || keyStr.contains((str_equalText.replacingOccurrences(of: "by", with: "p"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension DoingAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func actionSizeDismiss(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (str_logContent.lowercased() + "__upd" + String(str_messageCheckData.suffix(7)) + String(str_dismissValue.prefix(8)) + String(str_backName.prefix(5))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = addUrl()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
        var urlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(str_broadcastValue.prefix(6)) + String(str_areaName)), with: (String(str_textName) + String(str_equalData)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(str_textName) + String(str_equalData))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(str_controlValue.suffix(8))) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            diagonalCurrent(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        eventItem()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func diagonalCurrent(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: str_minimizeValue.map{viewShared(make: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func addUrl() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_broadcastValue.prefix(6)) + String(str_areaName))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_intervalLimitValue, !kLet_netName {
            //: serverPrefix = "https://"
            serverPrefix = (String(str_textName) + String(str_equalData))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(str_sizeName)) + "\(kLet_clickValue)" + (String(str_likeData.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func changedNote(note _: Notification) {
        //: if WithAdministratorAppManager.share.loginUid.isEmptyString {
        if WithAdministratorAppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            approximationGift()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            ifConversation()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func approximationGift() {
        //: var htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_frameData)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(str_frameData))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased()) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased())] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_frameData)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(str_frameData))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased()) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased())] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(str_frameData))), h5Cookie.keys.contains((String(str_frameData))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(str_frameData))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(str_frameData))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased())), h5Cookie.keys.contains((str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased())) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased())]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased())]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func ifConversation() {
        //: let htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
        let htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == WithAdministratorAppManager.share.loginUserMode.userID {
            if cookie.name == (String(str_frameData)), cookie.value == WithAdministratorAppManager.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased()) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_frameData)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(str_frameData))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased()) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased())] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(str_frameData))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(str_frameData))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased())) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(str_sectionValue.replacingOccurrences(of: "on", with: "H") + str_femaleVideoData.uppercased())] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func eventItem() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(str_makeValue.suffix(5)) + "_saveA" + String(str_labValue.prefix(4)) + String(str_addTextValue.prefix(8))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            kLet_dismissData.set(cookieData, forKey: kLet_errTitle)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(str_tabFrameName.prefix(6)) + "save" + String(str_leadingValue.prefix(5)) + "okies " + String(str_mainValue.prefix(6))))
        }
    }

    //: func func__setAppCookies() {
    func jab() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = kLet_dismissData.object(forKey: kLet_errTitle)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func funcToBid() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        kLet_dismissData.removeObject(forKey: kLet_errTitle)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
