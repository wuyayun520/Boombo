
//: Declare String Begin

/*: / :*/
fileprivate let str_viewName:[Character] = ["/"]

/*:  ( :*/
fileprivate let str_centerData:String = " icon"

/*: ; build: :*/
fileprivate let str_viewTitle:[Character] = [";"," ","b","u","i","l","d",":"]

/*: ; iOS  :*/
fileprivate let str_nowData:String = "request block; iOS "

/*: ;  :*/
fileprivate let str_collectionData:String = "; "

/*: )" :*/
fileprivate let str_timeLayerLabData:String = ")"

/*: "audio" :*/
fileprivate let str_topContent:[Character] = ["a","u","d","i","o"]

/*: + :*/
fileprivate let str_viewText:String = "gesture"

/*: .wav" :*/
fileprivate let str_sizeName:String = "cookie let make lock.wav"

/*: "audio/wav" :*/
fileprivate let str_managerText:String = "if container successaudio/wav"

/*: .jpg" :*/
fileprivate let str_topValue:String = ".jpgfalse data count var"

/*: "image/jpeg" :*/
fileprivate let str_labData:[Character] = ["i","m","a","g","e","/","j","p","e"]
fileprivate let str_screenSendName:String = "model"

/*: "://app." :*/
fileprivate let str_diskValue:String = "://app.link text back title guard"

/*: "\"data\":null" :*/
fileprivate let str_statusUpData:String = "\"dat"
fileprivate let str_toData:String = "sizesize"

/*: "\"data\":{}" :*/
fileprivate let str_errName:[Character] = ["\""]
fileprivate let str_showName:String = "data\":{}"

/*: ":null" :*/
fileprivate let str_midValue:String = "female succeed:null"

/*: ":\"\"" :*/
fileprivate let str_convertUserContent:[Character] = [":","\"","\\"]

/*: "Request failed, Try again later" :*/
fileprivate let str_tempLiveName:[UInt8] = [0x72,0x65,0x74,0x61,0x6c,0x20,0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x54,0x20,0x2c,0x64,0x65,0x6c,0x69,0x61,0x66,0x20,0x74,0x73,0x65,0x75,0x71,0x65,0x52]

/*: "json error" :*/
fileprivate let str_resultText:String = "modern"
fileprivate let str_viewValue:String = "on errorhidden guard voice control"

/*: "platform=iphone&version= :*/
fileprivate let str_plusValue:String = "platfmodelr"
fileprivate let str_videoValue:String = "next equal in equalm=ip"
fileprivate let str_roomLayerData:String = "block for view&ver"

/*: &packageId= :*/
fileprivate let str_targetName:String = "from return count else&pac"
fileprivate let str_effectName:String = "Id=gift in to height model"

/*: &bundleId= :*/
fileprivate let str_clickName:[Character] = ["&","b","u","n","d","l","e","I","d","="]

/*: &lang= :*/
fileprivate let str_voiceName:String = "&lang=session app color block"

/*: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| " :*/
fileprivate let str_makeName:String = "?!sense#"
fileprivate let str_menuTitle:String = "+,:;=\'\"`"
fileprivate let str_leadingMeText:[Character] = ["<",">","(",")","[","]","{","}","/","\\","|"," "]

/*: "/route/slowindex" :*/
fileprivate let str_aspectValue:String = "/rouview model"
fileprivate let str_containerTitle:[Character] = ["i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let str_voiceUnprecedentedTitle:[Character] = ["/","r","o","u","t","e","/","i","n","d","e","x"]

/*: "?path= :*/
fileprivate let str_appValue:String = "?path=menu price self right"

/*: "&GJNonceStr= :*/
fileprivate let str_userData:String = "info pic if&GJNo"
fileprivate let str_colorName:String = "tap"

/*: "& :*/
fileprivate let str_messageName:[Character] = ["&"]

/*: = :*/
fileprivate let str_valueName:[Character] = ["="]

/*: "&params= :*/
fileprivate let str_pickValue:String = "lab play&param"
fileprivate let str_leadingTitle:String = "error app lists="

/*: "/ :*/
fileprivate let str_liveSizeName:String = "result"

/*: "? :*/
fileprivate let str_youName:String = "?"

/*: "token" :*/
fileprivate let str_titleData:String = "tolabelen"

/*: "请求成功 :*/
fileprivate let str_toValue:[Character] = ["\u{8bf7}","求","成","功"]

/*: "AAAAAAAA" :*/
fileprivate let str_fileValue:[Character] = ["A","A","A","A","A","A","A","A"]

/*: "AA" :*/
fileprivate let str_atMinValue:String = "aa"

/*: "BB" :*/
fileprivate let str_mText:[Character] = ["B","B"]

/*: "下载完成 -  :*/
fileprivate let str_fromTitle:String = "下载完\u{6210} - "

/*: "/Documents/" :*/
fileprivate let str_atBarValue:String = "var point live for/D"
fileprivate let str_toTitle:String = "hidden tool if list labocuments/"

/*: ".mp4" :*/
fileprivate let str_arcValue:String = ".mp4self view height"

/*: "移动地址: :*/
fileprivate let str_layerText:String = "移\u{52a8}地址:"

/*: "后台任务下载回来" :*/
fileprivate let str_viewData:String = "\u{540e}台任务下"
fileprivate let str_equalValue:[Character] = ["载","回","\u{6765}"]

/*: "下载成功 : :*/
fileprivate let str_commentValue:[Character] = ["下","载","成","\u{529f}"," ",":"]

/*: "Net Error" :*/
fileprivate let str_mapTitle:String = "return to succeedNet Err"
fileprivate let str_signatureName:String = "omodel"

/*: "下载失败 : :*/
fileprivate let str_arrayTitle:String = "下载\u{5931}败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EmptyRequestTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import UIKit
import UIKit

//: import Alamofire
import Alamofire

//: import CoreMedia
import CoreMedia

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let kLet_nameFaceValue = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let kLet_statusContent = "\(kLet_routeData)/\(kLet_streamValue) (\(kLet_recordName)" + (String(str_viewTitle)) + "\(kLet_popValue)" + (String(str_nowData.suffix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: MediaTalkingErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let kLet_deviceName = EmptyRequestTool()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class EmptyRequestTool: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func clickLocation(model: NumberryMReactiveCompatible, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.appCharm() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.sexCompletion(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.digitiserQuantityeraction(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func sexCompletion(model: NumberryMReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.server(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.versionEqual(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    nameCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, MediaTalkingErrorResponse(errorCode: MasterAdditiveArithmetic.NetError.rawValue, errorMsg: kLet_detailMessageValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        atShow(showMsg: kLet_detailMessageValue)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(str_topContent)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.levelWith())+\(kLet_nameTopText)" + (String(str_sizeName.suffix(4))), mimeType: (String(str_managerText.suffix(9))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(str_topValue.prefix(4))), mimeType: (String(str_labData) + str_screenSendName.replacingOccurrences(of: "model", with: "g")))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    nameCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, MediaTalkingErrorResponse(errorCode: MasterAdditiveArithmetic.NetError.rawValue, errorMsg: kLet_detailMessageValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        atShow(showMsg: kLet_detailMessageValue)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func digitiserQuantityeraction(model: NumberryMReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.server(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.versionEqual(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: kLet_netName ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_nameFaceValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    nameCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, MediaTalkingErrorResponse(errorCode: MasterAdditiveArithmetic.NetError.rawValue, errorMsg: kLet_detailMessageValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        atShow(showMsg: kLet_detailMessageValue)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_nameFaceValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    nameCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, MediaTalkingErrorResponse(errorCode: MasterAdditiveArithmetic.NetError.rawValue, errorMsg: kLet_detailMessageValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        atShow(showMsg: kLet_detailMessageValue)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func nameCompletion(model: NumberryMReactiveCompatible, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            DoingAddrTool.share.actionSizeDismiss(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if kLet_netName, model.requestServer.contains((String(str_diskValue.prefix(7)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.aes256Decrypt(key: DoingAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: (str_statusUpData + "a\":nu" + str_toData.replacingOccurrences(of: "size", with: "l")), with: (String(str_errName) + str_showName))
        //: if !Encryption_Request {
        if !kLet_netName {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(str_midValue.suffix(5))), with: ":\"\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<PopBaseResponse>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == MasterAdditiveArithmetic.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, MediaTalkingErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    atShow(showMsg: responseModel.msg ?? String(bytes: str_tempLiveName.reversed(), encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case MasterAdditiveArithmetic.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_useTitle, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, MediaTalkingErrorResponse(errorCode: MasterAdditiveArithmetic.NetError.rawValue, errorMsg: (str_resultText.replacingOccurrences(of: "modern", with: "js") + String(str_viewValue.prefix(8))).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func server(model: NumberryMReactiveCompatible) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (str_plusValue.replacingOccurrences(of: "model", with: "o") + String(str_videoValue.suffix(4)) + "hone" + String(str_roomLayerData.suffix(4)) + "sion=") + "\(kLet_timeTitle)" + (String(str_targetName.suffix(4)) + "kage" + String(str_effectName.prefix(3))) + "\(kLet_nameTopText)" + (String(str_clickName)) + "\(kLet_recordName)" + (String(str_voiceName.prefix(6))) + "\(DoingAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if kLet_netName, serverUrl.contains((String(str_diskValue.prefix(7)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if DoingAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(str_aspectValue.prefix(4)) + "te/slow" + String(str_containerTitle)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(str_voiceUnprecedentedTitle))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(str_appValue.prefix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(str_userData.suffix(5)) + "nceStr" + str_colorName.replacingOccurrences(of: "tap", with: "=")) + "\(DoingAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.aes256Encrypt(key: DoingAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(str_pickValue.suffix(6)) + String(str_leadingTitle.suffix(2))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func versionEqual(model: NumberryMReactiveCompatible) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(kLet_statusContent)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (str_titleData.replacingOccurrences(of: "label", with: "k")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func opModel(model: NumberryMReactiveCompatible) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.server(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(str_toValue)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(str_fileValue))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (str_fromTitle) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(str_atBarValue.suffix(2)) + String(str_toTitle.suffix(9))) + NSDate.levelWith() + (String(str_arcValue.prefix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (str_layerText) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (str_viewData + String(str_equalValue)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = ButtonDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func course(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: kLet_clickValue)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(str_commentValue)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, MediaTalkingErrorResponse(errorCode: MasterAdditiveArithmetic.Normal.rawValue, errorMsg: (String(str_mapTitle.suffix(7)) + str_signatureName.replacingOccurrences(of: "model", with: "r")).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, MediaTalkingErrorResponse(errorCode: MasterAdditiveArithmetic.NetError.rawValue, errorMsg: (String(str_mapTitle.suffix(7)) + str_signatureName.replacingOccurrences(of: "model", with: "r")).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_arrayTitle) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func modelRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: kLet_clickValue)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(str_commentValue)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, MediaTalkingErrorResponse(errorCode: MasterAdditiveArithmetic.Normal.rawValue, errorMsg: (String(str_mapTitle.suffix(7)) + str_signatureName.replacingOccurrences(of: "model", with: "r")).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, MediaTalkingErrorResponse(errorCode: MasterAdditiveArithmetic.NetError.rawValue, errorMsg: (String(str_mapTitle.suffix(7)) + str_signatureName.replacingOccurrences(of: "model", with: "r")).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_arrayTitle) + "\(errMsg)")
            }
        }
    }
}
