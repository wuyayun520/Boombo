
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_makeValue:[UInt8] = [0x98,0x9f,0x98,0x85,0xd9,0x92,0x9e,0x95,0x94,0x83,0xcb,0xd8,0xd1,0x99,0x90,0x82,0xd1,0x9f,0x9e,0x85,0xd1,0x93,0x94,0x94,0x9f,0xd1,0x98,0x9c,0x81,0x9d,0x94,0x9c,0x94,0x9f,0x85,0x94,0x95]

private func errorStream(main num: UInt8) -> UInt8 {
    return num ^ 241
}

/*: "Text greeting" :*/
fileprivate let str_downData:String = "coin else add picText "

/*: "Add" :*/
fileprivate let str_picName:String = "self nameAdd"

/*: "type" :*/
fileprivate let str_mediumName:String = "sizeype"

/*: "addStatus" :*/
fileprivate let str_roomName:String = "addStatuscustom to"

/*: "list" :*/
fileprivate let str_responseData:[Character] = ["l","i","s","t"]

/*: "Are you sure to delete this message？" :*/
fileprivate let str_titleCellData:[UInt8] = [0xca,0xf9,0xee,0xab,0xf2,0xe4,0xfe,0xab,0xf8,0xfe,0xf9,0xee,0xab,0xff,0xe4,0xab,0xef,0xee,0xe7,0xee,0xff,0xee,0xab,0xff,0xe3,0xe2,0xf8,0xab,0xe6,0xee,0xf8,0xf8,0xea,0xec,0xee,0x64,0x37,0x14]

private func equalJump(space num: UInt8) -> UInt8 {
    return num ^ 139
}

/*: "Cancel" :*/
fileprivate let str_modelTitle:String = "Cancelfor shared true"

/*: "Delete" :*/
fileprivate let str_buttonDataValue:String = "share"
fileprivate let str_mineValue:[Character] = ["e","l","e","t","e"]

/*: "id" :*/
fileprivate let str_hiddenClickText:String = "iroute"

/*: "The upper limit has been reached" :*/
fileprivate let str_framePathTryData:[UInt8] = [0x64,0x65,0x68,0x63,0x61,0x65,0x72,0x20,0x6e,0x65,0x65,0x62,0x20,0x73,0x61,0x68,0x20,0x74,0x69,0x6d,0x69,0x6c,0x20,0x72,0x65,0x70,0x70,0x75,0x20,0x65,0x68,0x54]

/*: "text" :*/
fileprivate let str_lengthTitle:String = "teshowt"

/*: "Successfully added, please wait for approval" :*/
fileprivate let str_statusValue:[UInt8] = [0x3b,0x1d,0xb,0xb,0xd,0x1b,0x1b,0xe,0x1d,0x4,0x4,0x11,0x48,0x9,0xc,0xc,0xd,0xc,0x44,0x48,0x18,0x4,0xd,0x9,0x1b,0xd,0x48,0x1f,0x9,0x1,0x1c,0x48,0xe,0x7,0x1a,0x48,0x9,0x18,0x18,0x1a,0x7,0x1e,0x9,0x4]

/*: "Any-Hex/Java" :*/
fileprivate let str_shareCellTitle:[Character] = ["A","n","y","-","H","e","x","/","J"]
fileprivate let str_titleData:String = "asizea"

/*: "NameThen" :*/
fileprivate let str_successName:String = "share manager upTalkin"
fileprivate let str_colorData:String = "type fullTextCel"
fileprivate let str_modelValue:String = "L"

/*: "#FF506D" :*/
fileprivate let str_normalToName:String = "#FF506Dtext model value mode regular"

/*: "btn_message_delete_nor" :*/
fileprivate let str_actionShowData:String = "btn_messsucceed in"
fileprivate let str_shareText:String = "info yellow a layer sharedage_"
fileprivate let str_labelTitle:String = "action top corner_nor"

/*: "Click the button below to add\ncontent" :*/
fileprivate let str_maxValue:[UInt8] = [0x9,0x26,0x23,0x29,0x21,0x6a,0x3e,0x22,0x2f,0x6a,0x28,0x3f,0x3e,0x3e,0x25,0x24,0x6a,0x28,0x2f,0x26,0x25,0x3d,0x6a,0x3e,0x25,0x6a,0x2b,0x2e,0x2e,0x40,0x29,0x25,0x24,0x3e,0x2f,0x24,0x3e]

private func whiteGift(icon num: UInt8) -> UInt8 {
    return num ^ 74
}

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let str_dataKitTitle:[UInt8] = [0x14,0x2b,0x26,0x2d,0x63,0x22,0x27,0x27,0x2a,0x2d,0x24,0x63,0x2e,0x36,0x2f,0x37,0x2a,0x33,0x2f,0x26,0x63,0x37,0x26,0x3b,0x37,0x63,0x2e,0x26,0x30,0x30,0x22,0x24,0x26,0x6f,0x63,0x37,0x2b,0x26,0x63,0x30,0x3a,0x30,0x37,0x63,0x26,0x2e,0x63,0x34,0x2a,0x2f,0x2f,0x63,0x30,0x26,0x2d,0x27,0x63,0x37,0x2b,0x26,0x2e,0x63,0x21,0x3a,0x63,0x37,0x36,0x31,0x2d,0x30,0x78,0x30,0x2f,0x2a,0x27,0x26,0x63,0x37,0x2c,0x63,0x37,0x2b,0x26,0x63,0x2e,0x26,0x30,0x30,0x22,0x24,0x26,0x63,0x37,0x2c,0x63,0x27,0x26,0x2f,0x26,0x37,0x26]

private func userText(with num: UInt8) -> UInt8 {
    return num ^ 67
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  HandleButtonViewDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class HandleButtonViewDelegate: NameViewController {
    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: CustomReactiveCompatible?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_makeValue.map{errorStream(main: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(str_downData.suffix(5)) + "greeting").localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.air()
        //: designView()
        actionCurrent()
        //: getAudioListData()
        inheritThroughAssemblageData()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [MediaGreetModel] = {
        //: var array = [TalkingGreetModel]()
        var array = [MediaGreetModel]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_senseContent, height: kLet_environmentClickValue - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(str_picName.suffix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.nameColors(colors: UIColor.modelColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(buttonAction), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension HandleButtonViewDelegate {
    //: func getAudioListData() {
    func inheritThroughAssemblageData() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(str_mediumName.replacingOccurrences(of: "size", with: "t"))] = 2
        //: ProgressHUD.show()
        StandardProgressHUD.indexRoom()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        QuantityeractionThen.tagCompletion(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            StandardProgressHUD.giftTo()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.flag()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(str_roomName.prefix(9)))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(str_responseData))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [MediaGreetModel] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<MediaGreetModel>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func shared(index: Int) {
        //: let config = ShowAlertConfig()
        let config = ManagerAlertConfig()
        //: config.textFont = UIFont.liveAttention(type: .Medium, fontSize: 18)
        config.textFont = UIFont.liveAttention(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        VoiceAlertShow.customForTotal(message: String(bytes: str_titleCellData.map{equalJump(space: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(str_modelTitle.prefix(6))).localized, rightBtnTitle: (str_buttonDataValue.replacingOccurrences(of: "share", with: "D") + String(str_mineValue)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            VoiceAlertShow.bias()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            VoiceAlertShow.bias()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: MediaGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            StandardProgressHUD.indexRoom()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            QuantityeractionThen.package(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                StandardProgressHUD.giftTo()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.inheritThroughAssemblageData()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension HandleButtonViewDelegate {
    //: @objc func addButtonClick() {
    @objc func buttonAction() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.atShow(showMsg: String(bytes: str_framePathTryData.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = CustomReactiveCompatible(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.sumLab()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.array(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(str_mediumName.replacingOccurrences(of: "size", with: "t"))] = 2
            //: dict["text"] = tempStr
            dict[(str_lengthTitle.replacingOccurrences(of: "show", with: "x"))] = tempStr
            //: ProgressHUD.show()
            StandardProgressHUD.indexRoom()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            QuantityeractionThen.finishError(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                StandardProgressHUD.giftTo()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.successMsg(showMsg: String(bytes: str_statusValue.map{$0^104}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.inheritThroughAssemblageData()
                //: self.recordView?.dismiss()
                self.recordView?.blockDown()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func array(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(str_shareCellTitle) + str_titleData.replacingOccurrences(of: "size", with: "v")) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension HandleButtonViewDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.liveAttention(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: kLet_senseContent - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.liveAttention(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetTextCell"
        let identifier = (String(str_successName.suffix(6)) + "gGreet" + String(str_colorData.suffix(7)) + str_modelValue.lowercased())
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: NameThen = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! NameThen

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.teeUpActionPast(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (str_buttonDataValue.replacingOccurrences(of: "share", with: "D") + String(str_mineValue)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.shared(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(str_normalToName.prefix(7))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.shared(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.nearCounterval(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.nearCounterval(name: (String(str_actionShowData.prefix(8)) + String(str_shareText.suffix(4)) + "delete" + String(str_labelTitle.suffix(4)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = LabelWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension HandleButtonViewDelegate {
    //: private func setupEmptyDataView() {
    private func flag() {
        //: var style = EmptyStyle()
        var style = StackEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: str_maxValue.map{whiteGift(icon: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .air()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func actionCurrent() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: "TalkingGreetTextCell")
        MainTable.register(NameThen.self, forCellReuseIdentifier: (String(str_successName.suffix(6)) + "gGreet" + String(str_colorData.suffix(7)) + str_modelValue.lowercased()))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: kLet_senseContent, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.inexpensiveness()
        label.textColor = UIColor.inexpensiveness()
        //: label.font = UIFont.unprecedented(fontSize: 15)
        label.font = UIFont.unprecedented(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: str_dataKitTitle.map{userText(with: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
