
//: Declare String Begin

/*: "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$" :*/
fileprivate let str_clickMomentData:[UInt8] = [0xa7,0xd1,0xa2,0x98,0xd4,0x83,0xb8,0xd4,0xa3,0xc9,0xd4,0xc0,0xa6,0xa5,0xd7,0xd4,0xa4,0xd2,0xd0,0xb9,0xd1,0xa2,0xa5,0x9d,0x98,0xd4,0x83,0xa5,0xd7,0xd4,0xa4,0xd2,0xd0,0xa5,0xd7,0xd1,0xa2,0x98,0xd4,0x83,0xa5,0xd7,0xa4,0x82,0xcb,0xd5,0xcf,0x84,0xd0,0xdd]

private func saintElmoSLight(title num: UInt8) -> UInt8 {
    return num ^ 249
}

/*: "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$" :*/
fileprivate let str_modeValue:[UInt8] = [0x40,0x36,0x36,0x2f,0x45,0x2d,0x2a,0x2b,0x29,0x26,0x43,0x45,0x2e,0x33,0x27,0x43,0x65,0x27,0x63,0x37,0x62,0x36,0x36,0x2e,0x42,0x7a,0x65,0x2c,0x63,0x33,0x42,0x7a,0x65,0x26,0x63,0x37,0x62,0x36,0x2e,0x42,0x7a,0x65,0x2d,0x63,0x33,0x42,0x7a,0x65,0x29,0x32,0x26,0x63,0x37,0x62,0x36,0x2e,0x42,0x7a,0x65,0x2f,0x2e,0x32,0x2f,0x2f,0x63,0x37,0x37,0x3a]

private func coatButton(clear num: UInt8) -> UInt8 {
    return num ^ 30
}

/*: "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)" :*/
fileprivate let str_attributeText:[UInt8] = [0x29,0x24,0x7d,0x30,0x31,0x2c,0x32,0x7b,0x5d,0x5f,0x39,0x2d,0x30,0x7a,0x2d,0x61,0x5a,0x2d,0x41,0x35,0x41,0x46,0x39,0x75,0x5c,0x2d,0x30,0x30,0x45,0x34,0x75,0x5c,0x5b,0x28,0x5e]

/*: "^[0-8]\\d{5}(?!\\d)$" :*/
fileprivate let str_moveTitle:String = "^[0-8]"
fileprivate let str_finishEnableModeName:String = "?!\\d)$"

/*: "^[a-zA-Z0-9]{6,20}+$" :*/
fileprivate let str_modelData:String = "^[a-click fire self"
fileprivate let str_giftName:String = "0-9]{6,"
fileprivate let str_fromName:String = "20}+$"

/*: "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$" :*/
fileprivate let str_pathValue:[UInt8] = [0x24,0x3f,0x2f,0x5c,0x2a,0x29,0x2a,0x5d,0x2d,0x2e,0x5c,0x20,0x77,0x5c,0x2f,0x5c,0x5b,0x28,0x29,0x7d,0x36,0x2c,0x32,0x7b,0x5d,0x2e,0x5c,0x7a,0x2d,0x61,0x5b,0x28,0x2e,0x5c,0x29,0x2b,0x5d,0x2d,0x2e,0x5c,0x7a,0x2d,0x61,0x64,0x5c,0x5b,0x28,0x3f,0x29,0x2f,0x5c,0x2f,0x5c,0x3a,0x3f,0x73,0x70,0x74,0x74,0x68,0x28,0x5e]

/*: "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$" :*/
fileprivate let str_contentSizeTryData:[UInt8] = [0x88,0x52,0x69,0x64,0x52,0x69,0x64,0x5c,0x5f,0x85,0x5a,0x57,0x5f,0x87,0xa6,0x5c,0x85,0x5a,0x57,0x5e,0x87,0x85,0x5a,0x57,0x63,0x87,0xa6,0x85,0x5a,0x5b,0x87,0x69,0x85,0x5a,0x57,0x63,0x87,0x85,0x5a,0x57,0x63,0x87,0x69,0x53,0x86,0x58,0x53,0x4a,0xa5,0x5d,0xa7,0x52,0x69,0x64,0x5c,0x5f,0x85,0x5a,0x57,0x5f,0x87,0xa6,0x5c,0x85,0x5a,0x57,0x5e,0x87,0x85,0x5a,0x57,0x63,0x87,0xa6,0x85,0x5a,0x5b,0x87,0x69,0x85,0x5a,0x57,0x63,0x87,0x85,0x5a,0x57,0x63,0x87,0x69,0x53,0x4e]

fileprivate func touchAll(live num: UInt8) -> UInt8 {
    let value = Int(num) - 42
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)" :*/
fileprivate let str_gameCenterData:[UInt8] = [0x44,0x32,0x37,0x5c,0x41,0x55,0x31,0x17,0x5d,0x59,0x11,0x48,0x45,0x10,0x44,0x37,0x5c,0x41,0x55,0x31,0x17,0x5d,0x5b,0x11,0x44,0x37,0x5c,0x41,0x55,0x31,0x10,0x37,0x14,0x34,0x31,0x45,0x48,0x45]

/*: "SELF MATCHES %@" :*/
fileprivate let str_actionTitle:String = "SELF MATCtrue if"
fileprivate let str_inputData:[Character] = ["H","E","S"," ","%","@"]

/*: "Feedback" :*/
fileprivate let str_areaErrorData:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "Enter your feedback…" :*/
fileprivate let str_nearData:String = "Enter indicator height color self add"
fileprivate let str_lastData:String = " feeview make"
fileprivate let str_hiddenValue:[Character] = ["d","b","a","c","k","…"]

/*: "#999999" :*/
fileprivate let str_aspectData:[Character] = ["#","9","9","9","9","9"]
fileprivate let str_factorName:[Character] = ["9"]

/*: "0/ :*/
fileprivate let str_makeName:String = "0/"

/*: "Your email (Required) " :*/
fileprivate let str_buttonModelName:String = "video color selfYour e"
fileprivate let str_countTitle:String = "quiadd"

/*: "icon_me_feelback_star" :*/
fileprivate let str_overTitle:String = "layer gift coloricon_me"
fileprivate let str_colorCrushText:[Character] = ["_","f","e","e","l","b","a","c","k"]
fileprivate let str_errorData:[Character] = ["_","s","t","a","r"]

/*: "#CCCCCC" :*/
fileprivate let str_indexName:String = "var extension#CCCCCC"

/*: "Send" :*/
fileprivate let str_playData:String = "Sendview user"

/*: "#D0D0D0" :*/
fileprivate let str_errorKindData:String = "make"
fileprivate let str_appValue:String = "style0style0style0"

/*: "Please fill in the content" :*/
fileprivate let str_serverContent:String = "self origin selfPlease"
fileprivate let str_intimateData:String = "l in title size then view"
fileprivate let str_numberValue:[Character] = ["t"]
fileprivate let str_topData:[Character] = ["h","e"," ","c","o","n","t","e","n","t"]

/*: "Please enter the correct email address" :*/
fileprivate let str_collectionData:[UInt8] = [0xa5,0x99,0x90,0x94,0x86,0x90,0xd5,0x90,0x9b,0x81,0x90,0x87,0xd5,0x81,0x9d,0x90,0xd5,0x96,0x9a,0x87,0x87,0x90,0x96,0x81,0xd5,0x90,0x98,0x94,0x9c,0x99,0xd5,0x94,0x91,0x91,0x87,0x90,0x86,0x86]

/*: "content" :*/
fileprivate let str_behindName:String = "CONTENT"

/*: "contactWay" :*/
fileprivate let str_addItemData:String = "showntact"
fileprivate let str_panTopTitle:String = "let itemWay"

/*: "platform" :*/
fileprivate let str_leadingValue:String = "dismiss"
fileprivate let str_tempName:[Character] = ["l","a","t","f","o","r","m"]

/*: "iphone" :*/
fileprivate let str_bringTitle:String = "iphtimee"

/*: "version" :*/
fileprivate let str_currentViewValue:String = "vertexton"

/*: "type" :*/
fileprivate let str_managerData:[Character] = ["t","y","p","e"]

/*: "Operation succeeded" :*/
fileprivate let str_bottomValue:[Character] = ["O","p","e","r","a","t","i","o","n"," "]
fileprivate let str_barActionText:[Character] = ["s","u","c","c","e","e","d","e","d"]

/*: / :*/
fileprivate let str_indexText:String = "/"

/*: "\n" :*/
fileprivate let str_mainValue:String = "\n"

/*: "Problem statements" :*/
fileprivate let str_errorValue:String = "show count border push typeProb"
fileprivate let str_toData:[Character] = ["e","m","e","n","t","s"]

/*: "Feature advice" :*/
fileprivate let str_requestValue:String = "Featushare gesture"
fileprivate let str_containerData:[Character] = ["r","e"," ","a","d","v","i","c","e"]

/*: "Operation questions" :*/
fileprivate let str_nameBlockValue:String = "Operalet path self type moment"
fileprivate let str_makeUpName:String = "querequest"

/*: "Others" :*/
fileprivate let str_backTitle:String = "o"
fileprivate let str_hiddenName:[Character] = ["t","h","e","r","s"]

/*: "#EFEDFF" :*/
fileprivate let str_ofValue:String = "#EFEDFright if var kit"
fileprivate let str_willName:[Character] = ["F"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EmptyReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/15.
//

//: import UIKit
import UIKit

//: enum Validate {
enum StateValidate {
    //: case email(_: String)
    case email(_: String) //  邮箱
    //: case phoneNumber(_: String)
    case phoneNumber(_: String) //  手机号
    //: case userName(_: String)
    case userName(_: String) //  用户名
    //: case password(_: String)
    case password(_: String) //  密码
    //: case nickName(_: String)
    case nickName(_: String) //  昵称
    //: case postalCode(_: String)
    case postalCode(_: String) //  邮编
    //: case URL(_: String)
    case URL(_: String) //  URL
    //: case IP(_: String)
    case IP(_: String) //  IP
    //: case isChinese(_: String)
    case isChinese(_: String) //  是否全是中文字符
    //: case idNumber(_: String)
    case idNumber(_: String) //  身份证号

    //: var isRight: Bool {
    var isRight: Bool {
        //: var predicateStr: String!
        var predicateStr: String!
        //: var currentObject: String!
        var currentObject: String!
        //: switch self {
        switch self {
        //: case let .email(str):
        case let .email(str):
            //: predicateStr = "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$"
            predicateStr = String(bytes: str_clickMomentData.map{saintElmoSLight(title: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .phoneNumber(str):
        case let .phoneNumber(str):
            //: predicateStr = "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$"
            predicateStr = String(bytes: str_modeValue.map{coatButton(clear: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .userName(str):
        case let .userName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: str_attributeText.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .postalCode(str):
        case let .postalCode(str):
            //: predicateStr = "^[0-8]\\d{5}(?!\\d)$"
            predicateStr = (str_moveTitle.capitalized + "\\d{5}(" + str_finishEnableModeName)
            //: currentObject = str
            currentObject = str
        //: case let .password(str):
        case let .password(str):
            //: predicateStr = "^[a-zA-Z0-9]{6,20}+$"
            predicateStr = (String(str_modelData.prefix(4)) + "zA-Z" + str_giftName.capitalized + str_fromName.capitalized)
            //: currentObject = str
            currentObject = str
        //: case let .nickName(str):
        case let .nickName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: str_attributeText.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .URL(str):
        case let .URL(str):
            //: predicateStr = "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$"
            predicateStr = String(bytes: str_pathValue.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .IP(str):
        case let .IP(str):
            //: predicateStr = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
            predicateStr = String(bytes: str_contentSizeTryData.map{touchAll(live: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let . isChinese(str):
        case let .isChinese(str):
            //: predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            //: currentObject = str
            currentObject = str
        //: case let . idNumber(str):
        case let .idNumber(str):
            //: predicateStr = "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)"
            predicateStr = String(bytes: str_gameCenterData.map{$0^108}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        }
        //: let predicate =  NSPredicate(format: "SELF MATCHES %@", predicateStr)
        let predicate = NSPredicate(format: (String(str_actionTitle.prefix(9)) + String(str_inputData)), predicateStr)
        //: return predicate.evaluate(with: currentObject)
        return predicate.evaluate(with: currentObject)
    }
}

//: class TalkingFeedbackVC: TalkingBaseViewController {
class EmptyReactiveCompatible: NameViewController {
    //: let limitCount = 300
    let limitCount = 300
    //: let EmialCount = 50
    let EmialCount = 50
    //: var seleteBtn: UIButton?
    var seleteBtn: UIButton?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()

        //: self.title = "Feedback".localized
        self.title = (String(str_areaErrorData)).localized
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: designView()
        observer()
        //: addTapGestureRecognizer()
        ascendingColon()
        //: addKeyboardNotification()
        keyboard()
    }

    // MARK: - Lazy load

    //: lazy var backView: UIScrollView = {
    lazy var backView: UIScrollView = {
        //: let view = UIScrollView.init()
        let view = UIScrollView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.showsHorizontalScrollIndicator = false
        view.showsHorizontalScrollIndicator = false
        //: view.showsVerticalScrollIndicator = false
        view.showsVerticalScrollIndicator = false
        //: return view
        return view
        //: }()
    }()

    //: lazy var contView: UIView = {
    lazy var contView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var inputTView: UIView = {
    lazy var inputTView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: view.backgroundColor = .appBgColor()
        view.backgroundColor = .air()
        //: return view
        return view
        //: }()
    }()

    //: lazy var textView: TalkingTextView = {
    lazy var textView: EqualLeadingReactiveCompatible = {
        //: let textView = TalkingTextView.init()
        let textView = EqualLeadingReactiveCompatible()
        //: textView.placeholder = "Enter your feedback…".localized
        textView.placeholder = (String(str_nearData.prefix(6)) + "your" + String(str_lastData.prefix(4)) + String(str_hiddenValue)).localized
        //: textView.placeholderColor = UIColor.init(hex: "#999999")!
        textView.placeholderColor = UIColor(hex: (String(str_aspectData) + String(str_factorName)))!
        //: textView.font = UIFont.liveAttention(type: .Regular, fontSize: 15)
        textView.font = UIFont.liveAttention(type: .Regular, fontSize: 15)
        //: textView.textColor = UIColor.occurrence()
        textView.textColor = UIColor.occurrence()
        //: textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)
        textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)

        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textContainer.lineBreakMode = .byTruncatingTail
        textView.textContainer.lineBreakMode = .byTruncatingTail
        //: textView.delegate = self
        textView.delegate = self
        //: textView.returnKeyType = UIReturnKeyType.done
        textView.returnKeyType = UIReturnKeyType.done
        //: textView.bounces = false
        textView.bounces = false
        //: textView.backgroundColor = .appBgColor()
        textView.backgroundColor = .air()
        //: numberLB.text =  "0/\(limitCount)"
        numberLB.text = "0/\(limitCount)"
        //: return textView
        return textView
        //: }()
    }()

    //: lazy var numberLB: UILabel = {
    lazy var numberLB: UILabel = {
        //: let LB = UILabel.init()
        let LB = UILabel()
        //: LB.textColor = UIColor.init(hex: "#999999")
        LB.textColor = UIColor(hex: (String(str_aspectData) + String(str_factorName)))
        //: LB.font = UIFont.liveAttention(type: .Regular, fontSize: 14)
        LB.font = UIFont.liveAttention(type: .Regular, fontSize: 14)
        //: LB.text = "0/\(limitCount)"
        LB.text = "0/\(limitCount)"
        //: LB.textAlignment = .right
        LB.textAlignment = .right
        //: return LB
        return LB
        //: }()
    }()

    //: lazy var emailTF: UITextField = {
    lazy var emailTF: UITextField = {
        //: let tf = UITextField.init()
        let tf = UITextField()
        //: tf.backgroundColor = .appBgColor()
        tf.backgroundColor = .air()
        //: tf.textColor = UIColor.occurrence()
        tf.textColor = UIColor.occurrence()
        //: tf.font = UIFont.liveAttention(type: .Regular, fontSize: 16)
        tf.font = UIFont.liveAttention(type: .Regular, fontSize: 16)
        //: tf.delegate = self
        tf.delegate = self
        //: tf.returnKeyType = UIReturnKeyType.done
        tf.returnKeyType = UIReturnKeyType.done
        //: tf.attributedPlaceholder = NSAttributedString.init(string: "Your email (Required) ".localized, attributes: [
        tf.attributedPlaceholder = NSAttributedString(string: (String(str_buttonModelName.suffix(6)) + "mail (Re" + str_countTitle.replacingOccurrences(of: "add", with: "r") + "ed) ").localized, attributes: [
            //: NSAttributedString.Key.foregroundColor: UIColor.init(hex: "#999999")!])
            NSAttributedString.Key.foregroundColor: UIColor(hex: (String(str_aspectData) + String(str_factorName)))!])
        //: tf.clipsToBounds = true
        tf.clipsToBounds = true
        //: tf.layer.cornerRadius = 8
        tf.layer.cornerRadius = 8
        //: if LanguageManager.shared.direction == .rightToLeft {
        if MeanSunLanguageManager.shared.direction == .rightToLeft {
            //: tf.textAlignment = .right
            tf.textAlignment = .right
        }
        //: let leftView = UIView()
        let leftView = UIView()
        //: leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))
        leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))

        //: let leftImgView = UIImageView.init()
        let leftImgView = UIImageView()
        //: leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        //: leftImgView.image = UIImage.nearCounterval(name: "icon_me_feelback_star")
        leftImgView.image = UIImage.nearCounterval(name: (String(str_overTitle.suffix(7)) + String(str_colorCrushText) + String(str_errorData)))
        //: leftView.addSubview(leftImgView)
        leftView.addSubview(leftImgView)
        //: tf.leftView = leftView
        tf.leftView = leftView
        //: tf.leftViewMode = .always
        tf.leftViewMode = .always
        //: return tf
        return tf
        //: }()
    }()

    //: lazy var doneBtn: UIButton = {
    lazy var doneBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#CCCCCC")!, forState: .disabled)
        btn.backgroundBlock(color: UIColor(hex: (String(str_indexName.suffix(7))))!, forState: .disabled)
        //: btn.setBackgroundColor(color: UIColor.alright(), forState: .normal)
        btn.backgroundBlock(color: UIColor.alright(), forState: .normal)
        //: btn.setTitle("Send".localized, for: .normal)
        btn.setTitle((String(str_playData.prefix(4))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 17)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(equalOf), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFeedbackVC {
extension EmptyReactiveCompatible {
    //: @objc func TagBtnClick(sender: UIButton) {
    @objc func row(sender: UIButton) {
        //: seleteBtn?.isSelected = false
        seleteBtn?.isSelected = false
        //: seleteBtn?.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
        seleteBtn?.layer.borderColor = UIColor(hex: (str_errorKindData.replacingOccurrences(of: "make", with: "#") + str_appValue.replacingOccurrences(of: "style", with: "D")))?.cgColor
        //: sender.isSelected = !sender.isSelected
        sender.isSelected = !sender.isSelected
        //: seleteBtn = sender
        seleteBtn = sender
        //: seleteBtn!.layer.borderColor = UIColor.clear.cgColor
        seleteBtn!.layer.borderColor = UIColor.clear.cgColor
    }

    //: @objc func doneBtnClick() {
    @objc func equalOf() {
        //: if seleteBtn == nil || textView.text.count<=0 || emailTF.text!.count<=0 {
        if seleteBtn == nil || textView.text.count <= 0 || emailTF.text!.count <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please fill in the content".localized)
            self.atShow(showMsg: (String(str_serverContent.suffix(6)) + " fil" + String(str_intimateData.prefix(5)) + String(str_numberValue) + String(str_topData)).localized)
            //: return
            return
        }

        //: if LanguageManager.shared.direction == .leftToRight {
        if MeanSunLanguageManager.shared.direction == .leftToRight {
            //: if !Validate.email(emailTF.text!).isRight {
            if !StateValidate.email(emailTF.text!).isRight {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please enter the correct email address".localized)
                self.atShow(showMsg: String(bytes: str_collectionData.map{$0^245}, encoding: .utf8)!.localized)
                //: return
                return
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["content"] = textView.text
        dict[(str_behindName.lowercased())] = textView.text
        //: dict["contactWay"] = emailTF.text
        dict[(str_addItemData.replacingOccurrences(of: "show", with: "co") + String(str_panTopTitle.suffix(3)))] = emailTF.text
        //: dict["platform"] = "iphone"
        dict[(str_leadingValue.replacingOccurrences(of: "dismiss", with: "p") + String(str_tempName))] = (str_bringTitle.replacingOccurrences(of: "time", with: "on"))
        //: dict["version"] = AppVersion
        dict[(str_currentViewValue.replacingOccurrences(of: "text", with: "si"))] = kLet_streamValue
        //: dict["type"] = String((seleteBtn?.tag ?? 100)-100)
        dict[(String(str_managerData))] = String((seleteBtn?.tag ?? 100) - 100)

        //: ProgressHUD.show()
        StandardProgressHUD.indexRoom()

        //: TalkingMeRequestTool.req_Feedback(params: dict) { succeed, result, errorModel in
        QuantityeractionThen.searched(params: dict) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            StandardProgressHUD.giftTo()
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Operation succeeded".localized)
                self.successMsg(showMsg: (String(str_bottomValue) + String(str_barActionText)).localized)
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)
            }
        }
    }

    //: func addKeyboardNotification() {
    func keyboard() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillShow(notification:)),
                                               selector: #selector(self.notification(notification:)),
                                               //: name: UIResponder.keyboardWillShowNotification,
                                               name: UIResponder.keyboardWillShowNotification,
                                               //: object: nil)
                                               object: nil)
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillHide(notification:)),
                                               selector: #selector(file(notification:)),
                                               //: name: UIResponder.keyboardWillHideNotification,
                                               name: UIResponder.keyboardWillHideNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func keyboardWillShow(notification: Notification) {
    @objc func notification(notification: Notification) {
        //: guard emailTF.isFirstResponder else { return }
        guard emailTF.isFirstResponder else { return }
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        //: let frame = value?.cgRectValue ?? .zero
        let frame = value?.cgRectValue ?? .zero
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        //: guard (emailTF.bottom + kLet_likeTitle) > frame.minY else { return }
        guard (emailTF.bottom + kLet_likeTitle) > frame.minY else { return }

        //: let y = (emailTF.bottom + kLet_likeTitle) - frame.minY
        let y = (emailTF.bottom + kLet_likeTitle) - frame.minY
        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
            self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
        }
    }

    //: @objc func keyboardWillHide(notification: Notification) {
    @objc func file(notification: Notification) {
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25

        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = .identity
            self.backView.transform = .identity
        }
    }
}

// MARK: - UITextViewDelegate, UITextFieldDelegate

//: extension TalkingFeedbackVC: UITextViewDelegate, UITextFieldDelegate {
extension EmptyReactiveCompatible: UITextViewDelegate, UITextFieldDelegate {
    //: public func textViewDidChange(_ textView: UITextView) {
    public func textViewDidChange(_ textView: UITextView) {
        //: if textView.text.count > limitCount {
        if textView.text.count > limitCount {
            // 获得已输出字数与正输入字母数
            //: let selectRange = textView.markedTextRange
            let selectRange = textView.markedTextRange

            // 获取高亮部分 － 如果有联想词则解包成功
            //: if let selectRange = selectRange {
            if let selectRange = selectRange {
                //: let position =  textView.position(from: (selectRange.start), offset: 0)
                let position = textView.position(from: selectRange.start, offset: 0)
                //: if (position != nil) {
                if position != nil {
                    //: return
                    return
                }
            }

            //: let textContent = textView.text ?? ""
            let textContent = textView.text ?? ""
            //: let textNum = textContent.count
            let textNum = textContent.count

            // 截取
            //: if textNum > limitCount && limitCount > 0 {
            if textNum > limitCount && limitCount > 0 {
                //: textView.text = string_prefix(index: limitCount, text: textContent)
                textView.text = exaggerate(index: limitCount, text: textContent)
            }
        }
        //: numberLB.text =  "\(textView.text.count)/\(limitCount)"
        numberLB.text = "\(textView.text.count)/\(limitCount)"
    }

    //: public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
    public func textViewShouldBeginEditing(_: UITextView) -> Bool {
        //: numberLB.isHidden = false
        numberLB.isHidden = false
        //: return true
        return true
    }

    //: public func textViewDidEndEditing(_ textView: UITextView) {
    public func textViewDidEndEditing(_ textView: UITextView) {
        //: if textView.text.isEmpty {
        if textView.text.isEmpty {
            //: numberLB.isHidden = false
            numberLB.isHidden = false
            //: } else {
        } else {
            //: numberLB.isHidden = true
            numberLB.isHidden = true
        }
    }

    //: public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
    public func textView(_ textView: UITextView, shouldChangeTextIn _: NSRange, replacementText text: String) -> Bool {
        //: if textView.returnKeyType == .done {
        if textView.returnKeyType == .done {
            //: if text == "\n" {
            if text == "\n" {
                //: textView.resignFirstResponder()
                textView.resignFirstResponder()
            }
            //: return true
            return true
        }
        //: return true
        return true
    }

    // 字符串的截取 从头截取到指定index
    //: private func string_prefix(index: Int,text:String) -> String {
    private func exaggerate(index: Int, text: String) -> String {
        //: if text.count <= index {
        if text.count <= index {
            //: return text
            return text
            //: } else {
        } else {
            //: let index = text.index(text.startIndex, offsetBy: index)
            let index = text.index(text.startIndex, offsetBy: index)
            //: let str = text.prefix(upTo: index)
            let str = text.prefix(upTo: index)
            //: return String(str)
            return String(str)
        }
    }

    //: func textFieldShouldReturn(_ textField: UITextField) -> Bool {
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        //: textField.resignFirstResponder()
        textField.resignFirstResponder()

        //: return true
        return true
    }

    //: func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
    func textField(_ textField: UITextField, shouldChangeCharactersIn _: NSRange, replacementString string: String) -> Bool {
        //: if textField.text!.count+string.count>EmialCount || textField.text!.count+string.count <= 0 {
        if textField.text!.count + string.count > EmialCount || textField.text!.count + string.count <= 0 {
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - UI

//: extension TalkingFeedbackVC {
extension EmptyReactiveCompatible {
    //: func designView() {
    func observer() {
        //: self.view.addSubview(backView)
        self.view.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: backView.addSubview(contView)
        backView.addSubview(contView)
        //: contView.snp.makeConstraints { make in
        contView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(backView)
            make.leading.trailing.top.bottom.equalTo(backView)
        }

        //: contView.addSubview(inputTView)
        contView.addSubview(inputTView)
        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(contView).offset(128)
            make.top.equalTo(contView).offset(128)
            //: make.height.equalTo(210)
            make.height.equalTo(210)
        }

        //: inputTView.addSubview(textView)
        inputTView.addSubview(textView)
        //: inputTView.addSubview(numberLB)
        inputTView.addSubview(numberLB)
        //: textView.snp.makeConstraints { make in
        textView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(inputTView)
            make.leading.trailing.top.equalTo(inputTView)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
        }
        //: numberLB.snp.makeConstraints { make in
        numberLB.snp.makeConstraints { make in
            //: make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: contView.addSubview(emailTF)
        contView.addSubview(emailTF)
        //: emailTF.snp.makeConstraints { make in
        emailTF.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(inputTView.snp.bottom).offset(16)
            make.top.equalTo(inputTView.snp.bottom).offset(16)
            //: make.height.equalTo(49)
            make.height.equalTo(49)
        }

        //: contView.addSubview(doneBtn)
        contView.addSubview(doneBtn)
        //: doneBtn.snp.makeConstraints { make in
        doneBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(54)
            make.leading.equalTo(contView).offset(54)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-54)
            make.trailing.equalTo(contView.snp.trailing).offset(-54)
            //: make.top.equalTo(emailTF.snp.bottom).offset(111)
            make.top.equalTo(emailTF.snp.bottom).offset(111)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }

        //: let arr = ["Problem statements".localized,
        let arr = [(String(str_errorValue.suffix(4)) + "lem stat" + String(str_toData)).localized,
                   //: "Feature advice".localized,
                   (String(str_requestValue.prefix(5)) + String(str_containerData)).localized,
                   //: "Operation questions".localized,
                   (String(str_nameBlockValue.prefix(5)) + "tion " + str_makeUpName.replacingOccurrences(of: "request", with: "s") + "tions").localized,
                   //: "Others".localized]
                   (str_backTitle.uppercased() + String(str_hiddenName)).localized]

        //: for i in 0..<arr.count {
        for i in 0 ..< arr.count {
            //: let xz = 15
            let xz = 15
            //: let yz = 16
            let yz = 16
            //: let Width = (Int(ScreenWidth)-xz*3)/2
            let Width = (Int(kLet_senseContent) - xz * 3) / 2
            //: let height = 38
            let height = 38
            //: let X = xz+(xz + Width)*(i%2)
            let X = xz + (xz + Width) * (i % 2)
            //: let Y = yz+(yz+height)*(i/2)
            let Y = yz + (yz + height) * (i / 2)

            //: let btn = UIButton.init()
            let btn = UIButton()
            //: btn.frame =  CGRect.init(x: X, y: Y , width: Width, height: height)
            btn.frame = CGRect(x: X, y: Y, width: Width, height: height)
            //: btn.setTitle(arr[i], for: .normal)
            btn.setTitle(arr[i], for: .normal)
            //: btn.setTitleColor(.black, for: .normal)
            btn.setTitleColor(.black, for: .normal)
            //: btn.setTitleColor(.alright(), for: .selected)
            btn.setTitleColor(.alright(), for: .selected)

            //: btn.titleLabel?.font = UIFont.liveAttention(type: .Regular, fontSize: 15)
            btn.titleLabel?.font = UIFont.liveAttention(type: .Regular, fontSize: 15)
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            if DoingAddrTool.share.interfaceLang == ConditionCollection.es.rawValue || DoingAddrTool.share.interfaceLang == ConditionCollection.pt.rawValue {
                //: btn.titleLabel?.font = UIFont.liveAttention(type: .Regular, fontSize: 13)
                btn.titleLabel?.font = UIFont.liveAttention(type: .Regular, fontSize: 13)
            }
            //: btn.layer.cornerRadius = 19
            btn.layer.cornerRadius = 19
            //: btn.layer.borderWidth = 1
            btn.layer.borderWidth = 1
            //: btn.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
            btn.layer.borderColor = UIColor(hex: (str_errorKindData.replacingOccurrences(of: "make", with: "#") + str_appValue.replacingOccurrences(of: "style", with: "D")))?.cgColor
            //: btn.setBackgroundColor(color: UIColor.init(hex: "#EFEDFF")!, forState: .selected)
            btn.backgroundBlock(color: UIColor(hex: (String(str_ofValue.prefix(6)) + String(str_willName)))!, forState: .selected)
            //: btn.setBackgroundColor(color: .white, forState: .normal)
            btn.backgroundBlock(color: .white, forState: .normal)
            //: btn.tag = 100+i
            btn.tag = 100 + i
            //: btn.addTarget( self, action: #selector(TagBtnClick(sender:)), for: .touchUpInside)
            btn.addTarget(self, action: #selector(row(sender:)), for: .touchUpInside)
            //: contView.addSubview(btn)
            contView.addSubview(btn)
        }

        //: contView.snp.remakeConstraints { make in
        contView.snp.remakeConstraints { make in
            //: make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            //: make.width.equalTo(backView)
            make.width.equalTo(backView)
        }
    }
}
