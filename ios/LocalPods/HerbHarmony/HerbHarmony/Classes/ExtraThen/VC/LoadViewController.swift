
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_voiceEqualContent:[UInt8] = [0x3b,0x3c,0x3b,0x26,0x7a,0x31,0x3d,0x36,0x37,0x20,0x68,0x7b,0x72,0x3a,0x33,0x21,0x72,0x3c,0x3d,0x26,0x72,0x30,0x37,0x37,0x3c,0x72,0x3b,0x3f,0x22,0x3e,0x37,0x3f,0x37,0x3c,0x26,0x37,0x36]

private func ofName(play num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "Voice greeting" :*/
fileprivate let str_stackData:String = "Voicself line lab in"
fileprivate let str_viewData:String = "ebottoming"

/*: "Add" :*/
fileprivate let str_requestValue:String = "Addvar app return"

/*: "type" :*/
fileprivate let str_centerFromText:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let str_placeData:[Character] = ["a","d","d","S","t","a","t","u","s"]

/*: "list" :*/
fileprivate let str_mText:String = "litemt"

/*: "audio" :*/
fileprivate let str_userManagerText:String = "audimatch"

/*: "length" :*/
fileprivate let str_pathName:String = "LENGTH"

/*: "url" :*/
fileprivate let str_hiddenValue:[Character] = ["u","r","l"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let str_sizeSourceText:[UInt8] = [0x78,0x5e,0x48,0x48,0x4e,0x58,0x58,0x4d,0x5e,0x47,0x47,0x52,0xb,0x4a,0x4f,0x4f,0x4e,0x4f,0x7,0xb,0x5b,0x47,0x4e,0x4a,0x58,0x4e,0xb,0x5c,0x4a,0x42,0x5f,0xb,0x4d,0x44,0x59,0xb,0x4a,0x5b,0x5b,0x59,0x44,0x5d,0x4a,0x47]

private func behindDetailCenter(make num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "Are you sure to delete this message？" :*/
fileprivate let str_bringModelName:[UInt8] = [0x40,0x73,0x64,0x21,0x78,0x6e,0x74,0x21,0x72,0x74,0x73,0x64,0x21,0x75,0x6e,0x21,0x65,0x64,0x6d,0x64,0x75,0x64,0x21,0x75,0x69,0x68,0x72,0x21,0x6c,0x64,0x72,0x72,0x60,0x66,0x64,0xee,0xbd,0x9e]

/*: "Cancel" :*/
fileprivate let str_inputTitle:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let str_fillTitle:String = "Deleteuser size raw show online"

/*: "id" :*/
fileprivate let str_layerValue:String = "ilabel"

/*: "Please wait for the audit result" :*/
fileprivate let str_roomValue:[UInt8] = [0x74,0x6c,0x75,0x73,0x65,0x72,0x20,0x74,0x69,0x64,0x75,0x61,0x20,0x65,0x68,0x74,0x20,0x72,0x6f,0x66,0x20,0x74,0x69,0x61,0x77,0x20,0x65,0x73,0x61,0x65,0x6c,0x50]

/*: "The upper limit has been reached" :*/
fileprivate let str_cellValue:[UInt8] = [0x57,0x6b,0x66,0x23,0x76,0x73,0x73,0x66,0x71,0x23,0x6f,0x6a,0x6e,0x6a,0x77,0x23,0x6b,0x62,0x70,0x23,0x61,0x66,0x66,0x6d,0x23,0x71,0x66,0x62,0x60,0x6b,0x66,0x67]

private func clickVideo(make num: UInt8) -> UInt8 {
    return num ^ 3
}

/*: "filePath" :*/
fileprivate let str_trackText:[Character] = ["f","i","l","e","P"]
fileprivate let str_resultValue:[Character] = ["a","t","h"]

/*: "time" :*/
fileprivate let str_shadowValue:[Character] = ["t","i","m","e"]

/*: "TokenReactiveCompatible" :*/
fileprivate let str_pageTitle:String = "succeed video letTalk"
fileprivate let str_timeBroadValue:[Character] = ["e","t","A","u","d","i","o","C","e","l","l"]

/*: "#FF506D" :*/
fileprivate let str_equalValue:[Character] = ["#","F","F","5","0","6"]
fileprivate let str_itemName:String = "color"

/*: "btn_message_delete_nor" :*/
fileprivate let str_userData:String = "btn_gift actual"
fileprivate let str_addTransactionTitle:String = "else where withage_delet"
fileprivate let str_errorText:String = "e_norcolor self"

/*: "Currently mute" :*/
fileprivate let str_norData:[Character] = ["C","u","r","r","e","n","t","l","y"," ","m","u","t","e"]

/*: "Edit Title" :*/
fileprivate let str_toLeadingValue:[Character] = ["E","d","i","t"]
fileprivate let str_imageData:[Character] = [" ","T","i","t","l","e"]

/*: "OK" :*/
fileprivate let str_valueName:String = "bag"

/*: "remark" :*/
fileprivate let str_colorName:String = "errormark"

/*: "Click the button below to add\ncontent" :*/
fileprivate let str_minData:[UInt8] = [0x99,0xb6,0xb3,0xb9,0xb1,0xfa,0xae,0xb2,0xbf,0xfa,0xb8,0xaf,0xae,0xae,0xb5,0xb4,0xfa,0xb8,0xbf,0xb6,0xb5,0xad,0xfa,0xae,0xb5,0xfa,0xbb,0xbe,0xbe,0xd0,0xb9,0xb5,0xb4,0xae,0xbf,0xb4,0xae]

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let str_contentButtonValue:[UInt8] = [0x2e,0x64,0x65,0x74,0x69,0x64,0x65,0x20,0x65,0x62,0x20,0x6e,0x61,0x63,0x20,0x65,0x6c,0x74,0x69,0x74,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x68,0x74,0x20,0x64,0x6e,0x61,0x20,0x2c,0x74,0x69,0x20,0x65,0x74,0x65,0x6c,0x65,0x64,0x20,0x6f,0x74,0x20,0x61,0x65,0x72,0x61,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x68,0x74,0x20,0x6f,0x74,0x20,0x65,0x64,0x69,0x6c,0x73,0x20,0x2c,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x68,0x74,0x20,0x79,0x61,0x6c,0x70,0x20,0x6f,0x74,0x20,0x65,0x6c,0x62,0x62,0x75,0x62,0x20,0x65,0x68,0x74,0x20,0x6b,0x63,0x69,0x6c,0x43,0xa,0x3b,0x73,0x6e,0x72,0x75,0x74,0x20,0x79,0x62,0x20,0x73,0x65,0x67,0x61,0x73,0x73,0x65,0x6d,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x64,0x6e,0x65,0x73,0x20,0x6c,0x6c,0x69,0x77,0x20,0x6d,0x65,0x74,0x73,0x79,0x73,0x20,0x65,0x68,0x74,0x20,0x64,0x6e,0x61,0x20,0x2c,0x73,0x67,0x6e,0x69,0x74,0x65,0x65,0x72,0x67,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x6c,0x70,0x69,0x74,0x6c,0x75,0x6d,0x20,0x64,0x64,0x41]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LoadViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class LoadViewController: NameViewController {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: IndexThen?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: TalkingPlayerDelegate?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: TokenReactiveCompatible?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_voiceEqualContent.map{ofName(play: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(str_stackData.prefix(4)) + "e gre" + str_viewData.replacingOccurrences(of: "bottom", with: "t")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.air()
        //: designView()
        nebArteriaLacrimalisPanoramicView()
        //: getAudioListData()
        orientation()
        //: setMutedDetection()
        viewSub()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(count(notification:)), name: kLet_succeedTitle, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(gift(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(userBuild),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: kLet_succeedTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.hearer()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [MediaGreetModel] = {
        //: var array = [TalkingGreetModel]()
        var array = [MediaGreetModel]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(str_requestValue.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.nameColors(colors: UIColor.modelColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(clickColumn), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension LoadViewController {
    //: func getAudioListData() {
    func orientation() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(str_centerFromText))] = 1
        //: ProgressHUD.show()
        StandardProgressHUD.indexRoom()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        QuantityeractionThen.tagCompletion(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            StandardProgressHUD.giftTo()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.exhibit()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(str_placeData))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(str_mText.replacingOccurrences(of: "item", with: "is"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [MediaGreetModel] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<MediaGreetModel>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func data(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(str_centerFromText))] = 1
        //: dict["audio"] = data
        dict[(str_userManagerText.replacingOccurrences(of: "match", with: "o"))] = data
        //: dict["length"] = Int(time)
        dict[(str_pathName.lowercased())] = Int(time)

        //: ProgressHUD.show()
        StandardProgressHUD.indexRoom()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        QuantityeractionThen.finishError(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            StandardProgressHUD.giftTo()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(String(str_hiddenValue))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.successMsg(showMsg: String(bytes: str_sizeSourceText.map{behindDetailCenter(make: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.orientation()
                //: self.recordView?.dismiss()
                self.recordView?.customFinish()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func cellLock(index: Int) {
        //: let config = ShowAlertConfig()
        let config = ManagerAlertConfig()
        //: config.textFont = UIFont.liveAttention(type: .Medium, fontSize: 18)
        config.textFont = UIFont.liveAttention(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        VoiceAlertShow.customForTotal(message: String(bytes: str_bringModelName.map{$0^1}, encoding: .utf8)!.localized, leftBtnTitle: (String(str_inputTitle)).localized, rightBtnTitle: (String(str_fillTitle.prefix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            VoiceAlertShow.bias()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            VoiceAlertShow.bias()
            //: self.stopPlay()
            self.userBuild()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: MediaGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            QuantityeractionThen.package(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.orientation()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension LoadViewController {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func count(notification _: NSNotification) {
        //: stopPlay()
        userBuild()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func gift(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            userBuild()
        }
    }

    //: @objc func addButtonClick() {
    @objc func clickColumn() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard DataSocketDelegate.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.atShow(showMsg: kLet_keyTitle)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        LaissezPasserThen.withLoad(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.atShow(showMsg: kLet_viewData)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.atShow(showMsg: String(bytes: str_roomValue.reversed(), encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.atShow(showMsg: String(bytes: str_cellValue.map{clickVideo(make: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.userBuild()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = IndexThen(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.piece()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(str_trackText) + String(str_resultValue))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.data(data: data!, time: dict[(String(str_shadowValue))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension LoadViewController {
    //: func setMutedDetection() {
    func viewSub() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        Query.shared.doingline()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        Query.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension LoadViewController: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetAudioCell"
        let identifier = (String(str_pageTitle.suffix(4)) + "ingGre" + String(str_timeBroadValue))
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: TokenReactiveCompatible = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TokenReactiveCompatible

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.bottle(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.objectAdd(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.objectAdd(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            Query.shared.doingline()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.makingLove(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.userBuild()
            //: self.editTitle(index: indexPath.section)
            self.task(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(str_fillTitle.prefix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.cellLock(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(str_equalValue) + str_itemName.replacingOccurrences(of: "color", with: "D")))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.cellLock(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.nearCounterval(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.nearCounterval(name: (String(str_userData.prefix(4)) + "mess" + String(str_addTransactionTitle.suffix(9)) + String(str_errorText.prefix(5)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = LabelWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func makingLove(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isTalking else {
        guard !DataSocketDelegate.shared.isTalking else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.atShow(showMsg: kLet_keyTitle)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.hearer()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TokenReactiveCompatible
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.objectAdd(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.atShow(showMsg: (String(str_norData)).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = TalkingPlayerDelegate.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.hearer()
        //: self.player?.play(urlStr: model.content)
        self.player?.barCheck(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TokenReactiveCompatible
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.objectAdd(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func task(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = MiniDataViewDelegate(title: (String(str_toLeadingValue) + String(str_imageData)).localized, leftBtnTitle: (String(str_inputTitle)).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.medicine()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            StandardProgressHUD.indexRoom()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: MediaGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(str_colorName.replacingOccurrences(of: "error", with: "re"))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            QuantityeractionThen.towardModifyView(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                StandardProgressHUD.giftTo()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func userBuild() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.hearer()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.objectAdd(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension LoadViewController: LeadingToolDelegate {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func conversationStatus(status: GiftNumeric) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            userBuild()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            userBuild()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension LoadViewController {
    //: private func setupEmptyDataView() {
    private func exhibit() {
        //: var style = EmptyStyle()
        var style = StackEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: str_minData.map{$0^218}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .air()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func nebArteriaLacrimalisPanoramicView() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: "TalkingGreetAudioCell")
        MainTable.register(TokenReactiveCompatible.self, forCellReuseIdentifier: (String(str_pageTitle.suffix(4)) + "ingGre" + String(str_timeBroadValue)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.inexpensiveness()
        label.textColor = UIColor.inexpensiveness()
        //: label.font = UIFont.unprecedented(fontSize: 15)
        label.font = UIFont.unprecedented(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: str_contentButtonValue.reversed(), encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
