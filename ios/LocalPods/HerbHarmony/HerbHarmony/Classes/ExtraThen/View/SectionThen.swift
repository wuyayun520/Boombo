
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_skipErrorData:[UInt8] = [0xe2,0xe5,0xe2,0xff,0xa3,0xe8,0xe4,0xef,0xee,0xf9,0xb1,0xa2,0xab,0xe3,0xea,0xf8,0xab,0xe5,0xe4,0xff,0xab,0xe9,0xee,0xee,0xe5,0xab,0xe2,0xe6,0xfb,0xe7,0xee,0xe6,0xee,0xe5,0xff,0xee,0xef]

private func userView(tab num: UInt8) -> UInt8 {
    return num ^ 139
}

/*: "Cancel" :*/
fileprivate let str_nearRemoveTitle:[Character] = ["C","a","n","c","e","l"]

/*: "Submit" :*/
fileprivate let str_indicatorTitleValue:String = "to make managerSubmit"

/*: "Please tell us your problems                   " :*/
fileprivate let str_lineName:[UInt8] = [0x5,0x39,0x30,0x34,0x26,0x30,0x75,0x21,0x30,0x39,0x39,0x75,0x20,0x26,0x75,0x2c,0x3a,0x20,0x27,0x75,0x25,0x27,0x3a,0x37,0x39,0x30,0x38,0x26,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75,0x75]

private func bottomSection(content num: UInt8) -> UInt8 {
    return num ^ 85
}

/*: "You need to choose at least one." :*/
fileprivate let str_changeValue:[UInt8] = [0x2e,0x65,0x6e,0x6f,0x20,0x74,0x73,0x61,0x65,0x6c,0x20,0x74,0x61,0x20,0x65,0x73,0x6f,0x6f,0x68,0x63,0x20,0x6f,0x74,0x20,0x64,0x65,0x65,0x6e,0x20,0x75,0x6f,0x59]

/*: "Please tell us your problems." :*/
fileprivate let str_tagName:String = "interaction as headPleas"
fileprivate let str_viewValue:String = "super image var interactionll us yo"
fileprivate let str_labelData:[Character] = ["u","r"," ","p","r"]
fileprivate let str_colorName:[Character] = ["o","b","l","e","m","s","."]

/*: "app/userReport" :*/
fileprivate let str_borderText:String = "app/else to"
fileprivate let str_labPageTitleName:String = "eporcover"

/*: "id" :*/
fileprivate let str_firstEqualData:[UInt8] = [0x12,0x1f]

private func maskTextMode(key num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "reason" :*/
fileprivate let str_moreTitle:[Character] = ["r","e","a","s","o","n"]

/*: "desc" :*/
fileprivate let str_stageCoinTitle:[UInt8] = [0x3f,0x3e,0x28,0x38]

/*: "mf/moment/report" :*/
fileprivate let str_backValue:String = "mf/mocell block send"
fileprivate let str_buttonData:[Character] = ["r","e","p","o","r","t"]

/*: "momentId" :*/
fileprivate let str_callTitle:[UInt8] = [0xdd,0xdf,0xdd,0xd5,0xde,0xc4,0xf9,0xd4]

private func nameAt(send num: UInt8) -> UInt8 {
    return num ^ 176
}

/*: "mf/momentReply/report" :*/
fileprivate let str_pushTitle:[Character] = ["m","f","/","m","o","m","e"]
fileprivate let str_toData:[Character] = ["n","t","R","e","p","l","y","/","r","e","p","o","r","t"]

/*: "app/videoReport" :*/
fileprivate let str_meName:[Character] = ["a","p","p","/","v","i","d","e","o","R","e","p"]
fileprivate let str_soundValue:[Character] = ["o","r","t"]

/*: "logId" :*/
fileprivate let str_mmValue:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "Submitted successfully" :*/
fileprivate let str_photoData:String = "Submitdetail leading limit self self"
fileprivate let str_pushName:String = "sharedessfu"
fileprivate let str_viewTitle:[Character] = ["l","l","y"]

/*: "Nudity" :*/
fileprivate let str_timeDataValue:[Character] = ["N"]
fileprivate let str_succeedName:[Character] = ["u","d","i","t","y"]

/*: "Violence" :*/
fileprivate let str_needData:[Character] = ["V","i","o","l","e","n","c","e"]

/*: "Harassment" :*/
fileprivate let str_onData:String = "model equal try name menuHar"
fileprivate let str_greetKeyValue:[Character] = ["a","s","s","m","e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let str_leadingNaturalTitle:String = "direction super user inSuic"
fileprivate let str_imageData:String = "view returnr Self"
fileprivate let str_pushValue:[Character] = ["u","r","y"]

/*: "False Information" :*/
fileprivate let str_listNameData:[Character] = ["F","a","l","s","e"," ","I","n","f","o","r","m","a","t"]
fileprivate let str_userText:String = "cardn"

/*: "Spam" :*/
fileprivate let str_viewData:[Character] = ["S","p","a","m"]

/*: "Hate Speech" :*/
fileprivate let str_sourceTitle:[Character] = ["H"]
fileprivate let str_scaleName:[Character] = ["a","t","e"," ","S","p","e","e","c","h"]

/*: "Terrorism" :*/
fileprivate let str_calculationData:String = "Terroripath make false lab count"
fileprivate let str_styleEqualValue:String = "result"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let str_viewToValue:[UInt8] = [0x0,0xe,0x19,0x12,0xcd,0x1c,0x13,0xcd,0xf6,0x19,0x19,0x12,0x14,0xe,0x19,0xcd,0x1c,0x1f,0xcd,0xff,0x12,0x14,0x22,0x19,0xe,0x21,0x12,0x11,0xcd,0xf4,0x1c,0x1c,0x11,0x20]

fileprivate func forBottom(edge num: UInt8) -> UInt8 {
    let value = Int(num) + 83
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let str_pathHiddenText:String = "kind viewSend"
fileprivate let str_ofContent:String = "ifileu"
fileprivate let str_imageText:String = "leading to catchg me"
fileprivate let str_gestureLastColorData:String = "quotequoteagequote"

/*: "Using photos of nudity or drugs" :*/
fileprivate let str_bottomText:[UInt8] = [0x48,0x6e,0x74,0x73,0x7a,0x3d,0x6d,0x75,0x72,0x69,0x72,0x6e,0x3d,0x72,0x7b,0x3d,0x73,0x68,0x79,0x74,0x69,0x64,0x3d,0x72,0x6f,0x3d,0x79,0x6f,0x68,0x7a,0x6e]

private func makeControlTitle(file num: UInt8) -> UInt8 {
    return num ^ 29
}

/*: "Harassing or sending ads" :*/
fileprivate let str_canEditName:String = "Harasscreen guard mid video var"
fileprivate let str_dateValue:String = "image constraint modelr se"
fileprivate let str_nowData:String = "intimates"

/*: "Defraud or using fake info" :*/
fileprivate let str_numberContent:[Character] = ["D","e","f","r","a","u","d"," ","o","r"," "]
fileprivate let str_splitTitle:String = "textsin"
fileprivate let str_errorContent:[Character] = ["g"," ","f","a","k","e"," ","i","n","f","o"]

/*: "Others" :*/
fileprivate let str_cornerTitle:[Character] = ["O","t","h","e","r","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SectionThen.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import UIKit
import UIKit

//: import Then
import Then

//: enum TalkingReportAlertType: CaseIterable {
enum ScaleAlertType: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class SectionThen: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: ScaleAlertType?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [StackReactiveCompatible] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = StackReactiveCompatible()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: ScaleAlertType?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        toEqual()
        //: setupSubViewsConstraint()
        videoImage()
        //: setupReasons()
        theme()
        //: bindInteraction()
        search()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_skipErrorData.map{userView(tab: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(str_nearRemoveTitle)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.alright(), for: .normal)
        $0.setTitleColor(UIColor.alright(), for: .normal)
        //: $0.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.alright().cgColor
        $0.layer.borderColor = UIColor.alright().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(blockDisplayWith), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(str_indicatorTitleValue.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.nameColors(colors: UIColor.modelColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(submit), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: kLet_senseContent - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.equalRgba(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.inexpensiveness(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = LyricView(placeholder: String(bytes: str_lineName.map{bottomSection(content: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.inexpensiveness(), frame: CGRect(x: 10, y: 10, width: kLet_senseContent - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .liveAttention(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .liveAttention(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.occurrence()
        $0.placeholdertextView.textColor = UIColor.occurrence()
        //: $0.placeholdertextView.font = .liveAttention(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .liveAttention(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.equalRgba(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(FrameThen.self, forCellReuseIdentifier: FrameThen.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension SectionThen {
    //: @objc func func__TapGestureRecognizer() {
    @objc func comment() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func format(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = IconThen.getWindow()
            let window = IconThen.getBind()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func worldView(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func blockDisplayWith() {
        //: self.dismissReportView(succeed: false)
        self.worldView(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func submit() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.atShow(showMsg: String(bytes: str_changeValue.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.atShow(showMsg: (String(str_tagName.suffix(5)) + "e te" + String(str_viewValue.suffix(8)) + String(str_labelData) + String(str_colorName)).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(str_borderText.prefix(4)) + "userR" + str_labPageTitleName.replacingOccurrences(of: "cover", with: "t"))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (String(str_moreTitle)): selectedModel.type, String(bytes: str_stageCoinTitle.map{$0^91}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(str_backValue.prefix(5)) + "ment/" + String(str_buttonData))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: str_callTitle.map{nameAt(send: $0)}, encoding: .utf8)!: tempId, (String(str_moreTitle)): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(str_pushTitle) + String(str_toData))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (String(str_moreTitle)): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(str_meName) + String(str_soundValue))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: str_mmValue.reversed(), encoding: .utf8)!: tempId, (String(str_moreTitle)): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        UnambiguousThen.everyPath(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.successMsg(showMsg: (String(str_photoData.prefix(6)) + "ted su" + str_pushName.replacingOccurrences(of: "shared", with: "cc") + String(str_viewTitle)).localized)
                //: self.dismissReportView(succeed: true)
                self.worldView(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension SectionThen: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: FrameThen.className()) as! FrameThen
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.imageCreate(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension SectionThen {
    /// 添加视图
    //: private func setupSubviews() {
    private func toEqual() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func videoImage() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func theme() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(str_timeDataValue) + String(str_succeedName)).localized,
                           //: "Violence".localized,
                           (String(str_needData)).localized,
                           //: "Harassment".localized,
                           (String(str_onData.suffix(3)) + String(str_greetKeyValue)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(str_leadingNaturalTitle.suffix(4)) + "ide o" + String(str_imageData.suffix(6)) + "-inj" + String(str_pushValue)).localized,
                           //: "False Information".localized,
                           (String(str_listNameData) + str_userText.replacingOccurrences(of: "card", with: "io")).localized,
                           //: "Spam".localized,
                           (String(str_viewData)).localized,
                           //: "Hate Speech".localized,
                           (String(str_sourceTitle) + String(str_scaleName)).localized,
                           //: "Terrorism".localized,
                           (String(str_calculationData.prefix(7)) + str_styleEqualValue.replacingOccurrences(of: "result", with: "sm")).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: str_viewToValue.map{forBottom(edge: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(str_pathHiddenText.suffix(4)) + "ing d" + str_ofContent.replacingOccurrences(of: "file", with: "sg") + "stin" + String(str_imageText.suffix(4)) + str_gestureLastColorData.replacingOccurrences(of: "quote", with: "s")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: str_bottomText.map{makeControlTitle(file: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(str_canEditName.prefix(5)) + "sing o" + String(str_dateValue.suffix(4)) + "nding a" + str_nowData.replacingOccurrences(of: "intimate", with: "d")).localized,
                           //: "Defraud or using fake info".localized,
                           (String(str_numberContent) + str_splitTitle.replacingOccurrences(of: "text", with: "u") + String(str_errorContent)).localized,
                           //: "Others".localized]
                           (String(str_cornerTitle)).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = WithAdministratorAppManager.share.appConfigMode.videoReport["reason"] {
            if let reason = WithAdministratorAppManager.share.appConfigMode.videoReport[(String(str_moreTitle))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = StackReactiveCompatible()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func search() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
