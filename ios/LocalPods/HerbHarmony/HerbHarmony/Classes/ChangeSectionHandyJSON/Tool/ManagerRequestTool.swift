
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let str_colorData:String = "mf/momeempty path black model text"
fileprivate let str_onlineName:[Character] = ["n","t","/","l","i","s","t"]

/*: "mf/moment/moments" :*/
fileprivate let str_cellServiceText:String = "for count firstmf/momen"
fileprivate let str_kitTitle:[Character] = ["t","/","m","o","m","e","n","t","s"]

/*: "mf/moment/like" :*/
fileprivate let str_indexVideoValue:String = "clear time selfmf/mo"
fileprivate let str_moreData:String = "/likstart view begin"
fileprivate let str_interactionData:[Character] = ["e"]

/*: "mf/crush/send" :*/
fileprivate let str_addText:String = "mf/cequal in image"

/*: "mf/moment/view" :*/
fileprivate let str_singleTitle:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let str_appearData:String = "lab since addt/view"

/*: "mf/momentReply/list" :*/
fileprivate let str_buttonName:String = "mf/momself nor let cover plain"
fileprivate let str_outputContent:String = "ly/ltext view aspect"
fileprivate let str_indicatorTitle:[Character] = ["i","s","t"]

/*: "mf/momentReply/save" :*/
fileprivate let str_pushTitlePermissionName:String = "self name returnmf/mom"
fileprivate let str_intimateData:[Character] = ["e","p","l","y","/","s","a","v","e"]

/*: "mf/moment/videoSign" :*/
fileprivate let str_layerValue:[Character] = ["m","f","/","m","o","m","e","n","t","/","v"]
fileprivate let str_signText:String = "idbyo"

/*: "dynamicPic.zip" :*/
fileprivate let str_buttonValue:[Character] = ["d","y","n","a"]
fileprivate let str_viewValue:String = "micPicheight let view sense"

/*: "pic" :*/
fileprivate let str_targetValue:[UInt8] = [0x40,0x59,0x53]

private func originCamera(make num: UInt8) -> UInt8 {
    return num ^ 48
}

/*: "content" :*/
fileprivate let str_viewName:String = "modelnte"
fileprivate let str_statusTitle:[Character] = ["n","t"]

/*: "mf/moment/save" :*/
fileprivate let str_fileData:[Character] = ["m","f","/","m","o","m","e","n","t","/","s","a","v","e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let str_targetTitle:[UInt8] = [0xb5,0x90,0x87,0x89,0x8c,0x96,0x8d,0xc5,0xb6,0x90,0x86,0x86,0x80,0x96,0x96,0x83,0x90,0x89,0xc9,0xc5,0xa2,0x8a,0xc5,0x91,0x8a,0xc5,0xa8,0x9c,0xc5,0xa1,0x9c,0x8b,0x84,0x88,0x8c,0x86,0x96,0xc5,0x91,0x8a,0xc5,0x93,0x8c,0x80,0x92]

private func slideEqual(status num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "mf/moment/saveVideo" :*/
fileprivate let str_lineMessageCurrentContent:String = "left imf/m"
fileprivate let str_showText:String = "omeadd"
fileprivate let str_progressValue:String = "Videocoordinate button cell"

/*: .png" :*/
fileprivate let str_tableText:[Character] = [".","p","n","g"]

/*: "dynamicPic" :*/
fileprivate let str_giftTitle:String = "dyfemalemi"
fileprivate let str_fatalData:[Character] = ["c","P","i","c"]

/*: "freeImage" :*/
fileprivate let str_ofContent:[Character] = ["f","r","e","e","I","m","a","g","e"]

/*: "mf/momentReply/replylist" :*/
fileprivate let str_likeName:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","r","e","p","l","y","l","i"]
fileprivate let str_outputName:String = "sframe"

/*: "mf/moment/delete" :*/
fileprivate let str_userName:String = "let down manager head requestmf/mo"
fileprivate let str_imageValue:String = "ment/dequal text"

/*: "mf/momentReply/delete" :*/
fileprivate let str_equalValue:[Character] = ["m","f","/","m","o"]
fileprivate let str_normalData:[Character] = ["m","e","n","t","R","e","p","l"]
fileprivate let str_appearPlayName:[Character] = ["y","/","d","e","l","e","t","e"]

/*: "mf/moment/pin" :*/
fileprivate let str_indicatorValue:String = "selected device tomf/momen"
fileprivate let str_infoData:String = "t/pinself value top without"

/*: "mf/moment/remindNum" :*/
fileprivate let str_proName:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e","m"]
fileprivate let str_genderTitle:[Character] = ["i","n","d","N","u","m"]

/*: "mf/moment/remindList" :*/
fileprivate let str_domainDateData:String = "mf/momview super resign hidden size"
fileprivate let str_modelContent:String = "ent/gift all make"
fileprivate let str_topValue:String = "dListequal replacement up add height"

/*: "page" :*/
fileprivate let str_detailData:[UInt8] = [0xfd,0xec,0xea,0xe8]

/*: "cos/getCredential" :*/
fileprivate let str_failData:String = "let video labcos/"
fileprivate let str_modelName:[Character] = ["e","d","e","n","t","i","a","l"]

/*: "type" :*/
fileprivate let str_layerTitle:String = "tframee"

/*: "user/reportUserInfo" :*/
fileprivate let str_endValue:String = "usegift"
fileprivate let str_mediumStyleName:[Character] = ["r","t"]
fileprivate let str_infoName:String = "gift cell self make numberUserInfo"

/*: "uid" :*/
fileprivate let str_dismissToInputText:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let str_inputName:String = "dbottom"
fileprivate let str_errorTriggerValue:[Character] = ["v","i","c","e","I","d"]

/*: "deviceName" :*/
fileprivate let str_timeData:String = "deshow"
fileprivate let str_sexText:String = "mtitle"

/*: "iphone" :*/
fileprivate let str_roomTitle:String = "IPHONE"

/*: "fileUrl" :*/
fileprivate let str_managerName:String = "fileUrlself off let application"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ManagerRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import UIKit
import UIKit

//: import Adjust
import Adjust

//: class TalkingMomentRequestTool: NSObject {
class ManagerRequestTool: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func parShow(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(str_colorData.prefix(7)) + String(str_onlineName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func mortal(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(str_cellServiceText.suffix(8)) + String(str_kitTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func likeData(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(str_indexVideoValue.suffix(5)) + "ment" + String(str_moreData.prefix(4)) + String(str_interactionData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func repressCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(str_addText.prefix(4)) + "rush/send")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func expensiveness(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(str_singleTitle) + String(str_appearData.suffix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func giftTalk(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(str_buttonName.prefix(6)) + "entRep" + String(str_outputContent.prefix(4)) + String(str_indicatorTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func response(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(str_pushTitlePermissionName.suffix(6)) + "entR" + String(str_intimateData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func showStart(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(str_layerValue) + str_signText.replacingOccurrences(of: "by", with: "e") + "Sign")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func less(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        cleanToFile()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            timeTotaleraction(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((String(str_buttonValue) + String(str_viewValue.prefix(6)) + ".zip"))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: str_targetValue.map{originCamera(make: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(str_viewName.replacingOccurrences(of: "model", with: "co") + String(str_statusTitle))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = NumberryMReactiveCompatible()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(str_fileData))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.successMsg(showMsg: String(bytes: str_targetTitle.map{slideEqual(status: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.cleanToFile()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func allowUpriseFullPhaseOfTheMoon(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(str_lineMessageCurrentContent.suffix(4)) + str_showText.replacingOccurrences(of: "add", with: "nt") + "/save" + String(str_progressValue.prefix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func timeTotaleraction(index: Int, image: UIImage) {
        //: let data = image.lengthPicAccountingData()
        let data = image.lengthPicAccountingData()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(str_tableText))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((str_giftTitle.replacingOccurrences(of: "female", with: "na") + String(str_fatalData)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func cleanToFile() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(str_ofContent)))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func free(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(str_likeName) + str_outputName.replacingOccurrences(of: "frame", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func equalBy(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(str_userName.suffix(5)) + String(str_imageValue.prefix(6)) + "elete")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func box(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(str_equalValue) + String(str_normalData) + String(str_appearPlayName))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func diorama(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(str_indicatorValue.suffix(8)) + String(str_infoData.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func callUpTo(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(str_proName) + String(str_genderTitle))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func abovePath(page: Int, completion: @escaping (_ data: [VoiceListModel]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(str_domainDateData.prefix(6)) + String(str_modelContent.prefix(4)) + "remin" + String(str_topValue.prefix(5)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: str_detailData.map{$0^141}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [VoiceListModel] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<VoiceListModel>.deserialize(from: arr as? Array) as? [VoiceListModel] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func paging(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(str_failData.suffix(4)) + "getCr" + String(str_modelName))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(str_layerTitle.replacingOccurrences(of: "frame", with: "yp")): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func outsideResumeColor(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestServer = WithAdministratorAppManager.share.appConfigMode.reportDomain
        reqModel.requestServer = WithAdministratorAppManager.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (str_endValue.replacingOccurrences(of: "gift", with: "r") + "/repo" + String(str_mediumStyleName) + String(str_infoName.suffix(8)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = WithAdministratorAppManager.share.loginUserMode.userID
        params[(String(str_dismissToInputText))] = WithAdministratorAppManager.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(str_inputName.replacingOccurrences(of: "bottom", with: "e") + String(str_errorTriggerValue))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(str_timeData.replacingOccurrences(of: "show", with: "vi") + "ceNa" + str_sexText.replacingOccurrences(of: "title", with: "e"))] = (str_roomTitle.lowercased())
        //: params["type"] = 1
        params[(str_layerTitle.replacingOccurrences(of: "frame", with: "yp"))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(str_managerName.prefix(7)))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
