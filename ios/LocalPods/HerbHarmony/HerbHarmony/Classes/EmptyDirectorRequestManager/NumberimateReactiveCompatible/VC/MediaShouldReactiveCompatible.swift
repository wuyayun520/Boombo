
//: Declare String Begin

/*: "cmd" :*/
fileprivate let str_endValue:[UInt8] = [0x4e,0x40,0x49]

private func toUp(view num: UInt8) -> UInt8 {
    return num ^ 45
}

/*: "cancelCall" :*/
fileprivate let str_sizeByText:[Character] = ["c","a","n","c","e"]
fileprivate let str_attentionName:String = "lCallcollection kit make"

/*: "data" :*/
fileprivate let str_viewArrayTitle:[UInt8] = [0x7e,0x7b,0x6e,0x7b]

private func playerAddSource(text num: UInt8) -> UInt8 {
    return num ^ 26
}

/*: "logId" :*/
fileprivate let str_colorMaxData:String = "logIdstart start"

/*: "#322870" :*/
fileprivate let str_itemName:String = "#"
fileprivate let str_voiceData:String = "322870"

/*: "Tap to start video chatting" :*/
fileprivate let str_behindData:String = "message to action top cellTap "
fileprivate let str_toData:String = "art vmake me equal let color"
fileprivate let str_makeSwitchlineData:String = "chatticolor"
fileprivate let str_environmentValue:String = "end"

/*: "icon_close_nor" :*/
fileprivate let str_headName:String = "model to anyicon_c"

/*: "icon_views_number" :*/
fileprivate let str_labelData:[Character] = ["i","c","o","n","_","v","i","e","w","s","_","n","u","m"]
fileprivate let str_locationTitle:String = "bcountr"

/*: "%@ chatting" :*/
fileprivate let str_fillTitle:[Character] = ["%","@"]
fileprivate let str_norStageCenterTitle:[Character] = [" ","c","h","a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let str_statusData:[Character] = ["i"]
fileprivate let str_valueDeviceName:String = "model type social flow presscon_"

/*: "icon_rank_coin" :*/
fileprivate let str_sizeTitle:String = "fatal endicon_ran"
fileprivate let str_appData:String = "model image truek_coin"

/*: "vip_btn_top" :*/
fileprivate let str_cellViewValue:String = "greet truevip_btn"
fileprivate let str_labData:String = "succeed key device index_top"

/*: "btn_nearby_settings_nor" :*/
fileprivate let str_sinceToolData:[Character] = ["b","t","n","_","n","e","a","r","b","y","_","s","e","t","t"]
fileprivate let str_coinName:[Character] = ["i","n","g","s","_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let str_viewText:String = "%@ Coins/self give name allow"
fileprivate let str_makeValue:[Character] = ["m","i","n"]

/*: "get json error" :*/
fileprivate let str_fromReportData:String = "image classget js"
fileprivate let str_roomTitle:[Character] = ["o","n"," ","e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let str_fileNextValue:[Character] = ["m","a"]
fileprivate let str_headValue:[Character] = ["t","c","h","I","d"]

/*: "errno" :*/
fileprivate let str_instanceName:[UInt8] = [0x82,0x8f,0x8f,0x8b,0x8c]

fileprivate func recreationRoom(text num: UInt8) -> UInt8 {
    let value = Int(num) - 29
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "VIPFee" :*/
fileprivate let str_timeText:[Character] = ["V"]
fileprivate let str_scaleBackData:String = "IPFeereturn make view"

/*: "Matching now" :*/
fileprivate let str_actionTitle:[Character] = ["M","a","t","c","h","i","n","g"," ","n","o","w"]

/*: "userTouch" :*/
fileprivate let str_dataValue:String = "userToushare to style time in"
fileprivate let str_hiddenName:[Character] = ["c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let str_borderValue:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V","i","d"]
fileprivate let str_detailName:String = "eoInfoview let"

/*: "fromType" :*/
fileprivate let str_dataName:[UInt8] = [0xd7,0xc3,0xde,0xdc,0xe5,0xc8,0xc1,0xd4]

private func typeCount(view num: UInt8) -> UInt8 {
    return num ^ 177
}

/*: "You've been barred from receiving calls" :*/
fileprivate let str_labName:[UInt8] = [0xfc,0x12,0x18,0xca,0x19,0x8,0xc3,0x5,0x8,0x8,0x11,0xc3,0x5,0x4,0x15,0x15,0x8,0x7,0xc3,0x9,0x15,0x12,0x10,0xc3,0x15,0x8,0x6,0x8,0xc,0x19,0xc,0x11,0xa,0xc3,0x6,0x4,0xf,0xf,0x16]

fileprivate func styleGift(equal num: UInt8) -> UInt8 {
    let value = Int(num) - 163
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "source" :*/
fileprivate let str_mText:[UInt8] = [0xbc,0xa0,0xba,0xbd,0xac,0xaa]

private func administratorPrice(add num: UInt8) -> UInt8 {
    return num ^ 207
}

/*: "type" :*/
fileprivate let str_fillToTitle:[UInt8] = [0xfd,0xf0,0xf9,0xec]

/*: "uid" :*/
fileprivate let str_objectText:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let str_layerData:String = "fromFextension for"
fileprivate let str_behindHostAppContent:String = "component"

/*: "requestCall" :*/
fileprivate let str_onTransitionValue:String = "retableu"
fileprivate let str_requestValue:[Character] = ["l","l"]

/*: "onVideoMatch" :*/
fileprivate let str_firstCellData:String = "onVideleading button extension"
fileprivate let str_numberData:String = "center true maxoMatch"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let str_upEqualData:[UInt8] = [0xcf,0xd5,0xe1,0xe0,0xfb,0xf9,0xf5,0xe0,0xfd,0xf7,0xb4,0xe2,0xfd,0xf0,0xf1,0xfb,0xc9,0xb4,0xc6,0xf1,0xf7,0xf1,0xfd,0xe2,0xf1,0xb4,0xe2,0xfd,0xf0,0xf1,0xfb,0xb4,0xf7,0xf5,0xf8,0xf8,0xb4,0xfd,0xfa,0xf7,0xfb,0xf9,0xfd,0xfa,0xf3]

private func inCenter(progress num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: "refuseCall" :*/
fileprivate let str_nearText:String = "restatusus"

/*: "onCancelCall" :*/
fileprivate let str_viewTitle:String = "if targetonCan"
fileprivate let str_onValue:[Character] = ["c","e","l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let str_countGestureText:String = "retryAreceive else block"

/*: "onRequestCall" :*/
fileprivate let str_titleValue:[Character] = ["o","n","R","e","q","u"]
fileprivate let str_ofGoTransportValue:String = "content screen typeestCall"

/*: "failType" :*/
fileprivate let str_arcValue:[Character] = ["f","a","i","l","T"]
fileprivate let str_remindViewName:[Character] = ["y","p","e"]

/*: "callWaitingTime" :*/
fileprivate let str_frameText:[Character] = ["c","a","l","l","W","a","i"]
fileprivate let str_playerContent:String = "else handle string totingTime"

/*: "toUid" :*/
fileprivate let str_sendLabData:String = "toUidplayer female"

/*: "Free 1 min" :*/
fileprivate let str_postName:String = "leading class age equal firstFree 1 "
fileprivate let str_modelValue:String = "micollection"

/*: "%@/min" :*/
fileprivate let str_valueCenterTitle:String = "%@/minmodel model class var"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MediaShouldReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class MediaShouldReactiveCompatible: NameViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.priceModel()
            }
        }
        //: self.resetCamera()
        self.polaroidLandCamera()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        videoFrom()
        //: setupSubViewsConstraint()
        sufficientBring()
        //: bindInteraction()
        infoAcrossPush()
        //: videoManager.startPreview(isMatch: true)
        videoManager.answerFile(isMatch: true)
        //: setPlayer(isRandon: false)
        clickRandon(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            searchVideo()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_endValue.map{toUp(view: $0)}, encoding: .utf8)!: (String(str_sizeByText) + String(str_attentionName.prefix(5))), String(bytes: str_viewArrayTitle.map{playerAddSource(text: $0)}, encoding: .utf8)!: [(String(str_colorMaxData.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        makeTimer()
        //: if TalkingSocketManager.shared.isTalking == false {
        if DataSocketDelegate.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            DataSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            DataSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            DataSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            DataSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: SmartCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = SmartCloudDelegate()
        //: let info = TalkingVideoChatModel()
        let info = StandardChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .occurrence()
        v.backgroundColor = .occurrence()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(searchVideo), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kLet_senseContent + 1, height: kLet_environmentClickValue + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (str_itemName.capitalized + str_voiceData.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(str_behindData.suffix(4)) + "to st" + String(str_toData.prefix(5)) + "ideo " + str_makeSwitchlineData.replacingOccurrences(of: "color", with: "n") + str_environmentValue.replacingOccurrences(of: "end", with: "g")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nearCounterval(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.nearCounterval(name: (String(str_headName.suffix(6)) + "lose_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(loadEqual), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: RoomThen = {
        //: let btn = TalkingButton()
        let btn = RoomThen()
        //: btn.setImage(UIImage.nearCounterval(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.nearCounterval(name: (String(str_labelData) + str_locationTitle.replacingOccurrences(of: "count", with: "e"))), for: .normal)
        //: btn.setImage(UIImage.nearCounterval(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.nearCounterval(name: (String(str_labelData) + str_locationTitle.replacingOccurrences(of: "count", with: "e"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.liveAttention(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.liveAttention(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(str_fillTitle) + String(str_norStageCenterTitle)).localizedArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.nearCounterval(name: "icon_lounge")
        img.image = UIImage.nearCounterval(name: (String(str_statusData) + String(str_valueDeviceName.suffix(4)) + "lounge"))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.nearCounterval(name: "icon_rank_coin")
        img.image = UIImage.nearCounterval(name: (String(str_sizeTitle.suffix(8)) + String(str_appData.suffix(6))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.liveAttention(type: .Regular, fontSize: 15)
        lab.font = UIFont.liveAttention(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.nearCounterval(name: "vip_btn_top")
        imgV.image = UIImage.nearCounterval(name: (String(str_cellViewValue.suffix(7)) + String(str_labData.suffix(4))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: RoomThen = {
        //: let btn = TalkingButton()
        let btn = RoomThen()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(clickTo), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.nearCounterval(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.nearCounterval(name: (String(str_sinceToolData) + String(str_coinName))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.nearCounterval(name: "icon_rank_coin")
        imgV.image = UIImage.nearCounterval(name: (String(str_sizeTitle.suffix(8)) + String(str_appData.suffix(6))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .insert()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(str_viewText.prefix(9)) + String(str_makeValue)).localizedArguments(vipFee)
        //: lab.font = UIFont.liveAttention(type: .Medium, fontSize: 15)
        lab.font = UIFont.liveAttention(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: ShouldThen = //: return ShouldThen(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension MediaShouldReactiveCompatible {
    //: private func setPlayer(isRandon: Bool) {
    private func clickRandon(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = DigitiserReactiveCompatible.default.game(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_fromReportData.suffix(6)) + String(str_roomTitle)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func randomUp(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(str_fileNextValue) + String(str_headValue)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: str_endValue.map{toUp(view: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: str_instanceName.map{recreationRoom(text: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        NumberimateReactiveCompatible.towardChange(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.capture()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_fileNextValue) + String(str_headValue))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func sizeFee() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        NumberimateReactiveCompatible.dismissAction { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(str_timeText) + String(str_scaleBackData.prefix(5)))].intValue
            //: self.refreshVideoCallPrice()
            self.priceModel()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension MediaShouldReactiveCompatible {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func clickTo() {
        //: LeadingEqualReactiveCompatible.share.func__pushToSubscribeAlert()
        LeadingEqualReactiveCompatible.share.nowKeyLog()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func loadEqual() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        circumferent()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kLet_voiceText.quantityingTime(eventID: kLet_recordSkipData)

        //: if WithAdministratorAppManager.share.appConfigMode.displayMatchV4PopUp, WithAdministratorAppManager.share.loginUserMode.freeCallTimes > 0,
        if WithAdministratorAppManager.share.appConfigMode.displayMatchV4PopUp, WithAdministratorAppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.afterwardCeremony()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        actionController()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func searchVideo() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard DataSocketDelegate.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.atShow(showMsg: kLet_keyTitle)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        afterColorWalkTo()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(str_actionTitle)).localized
        //: setPlayer(isRandon: true)
        clickRandon(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if WithAdministratorAppManager.share.loginUserMode.randomVideoType == 2 {
        if WithAdministratorAppManager.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kLet_voiceText.quantityingTime(eventID: kLet_successData)
            //: req_getVideoInfo()
            buttonStart()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kLet_voiceText.quantityingTime(eventID: kLet_halfText)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            DataSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            DataSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            DataSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            DataSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            changeInErrno()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func changeInErrno(cmd: String = (String(str_dataValue.prefix(7)) + String(str_hiddenName)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        makeTimer()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        randomUp(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func capture() {
        // 停止定时器
        //: destroyWaitingTimer()
        makeTimer()
        //: showCloseBtn()
        circumferent()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(str_behindData.suffix(4)) + "to st" + String(str_toData.prefix(5)) + "ideo " + str_makeSwitchlineData.replacingOccurrences(of: "color", with: "n") + str_environmentValue.replacingOccurrences(of: "end", with: "g")).localized
        //: setPlayer(isRandon: false)
        clickRandon(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isTalking == false {
        if DataSocketDelegate.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            DataSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            DataSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            DataSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            DataSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        priceModel()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func makeTimer() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func buttonStart() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(str_borderValue) + String(str_detailName.prefix(6)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: str_dataName.map{typeCount(view: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.capture()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: WithAdministratorAppManager.share.loginUserMode.randomVideoType = 1
                    WithAdministratorAppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.searchVideo()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.atShow(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = TitleMeasurable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = ButtonPlayerManager()
                //: player.setMute(bEnable: true)
                player.manager(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.drunk(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                MoveReactiveCompatible.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                LaissezPasserThen.polaroidCameraMicrophone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.addPut()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func addPut() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kLet_voiceText.quantityingTime(eventID: kLet_viewProgressData)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard MoveReactiveCompatible.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.atShow(showMsg: String(bytes: str_labName.map{styleGift(equal: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            MoveReactiveCompatible.shared.toCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.capture()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = VestryObjectProtocol()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.actionController()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.capture()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func afterColorWalkTo() {
        //: if WithAdministratorAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if WithAdministratorAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(WithAdministratorAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(WithAdministratorAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func circumferent() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func polaroidLandCamera() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.answerFile()
        }
    }
}

// MARK: - TotalMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension MediaShouldReactiveCompatible: TotalMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func discredit(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = TargetMeasurable.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(str_fileNextValue) + String(str_headValue)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: str_mText.map{administratorPrice(add: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: str_fillToTitle.map{$0^137}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: str_objectText.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = SumryEnableNumeric.randow
        //: if WithAdministratorAppManager.share.loginUserMode.freeCallTimes > 0 && WithAdministratorAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if WithAdministratorAppManager.share.loginUserMode.freeCallTimes > 0 && WithAdministratorAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(str_layerData.prefix(5)) + "reeCal" + str_behindHostAppContent.replacingOccurrences(of: "component", with: "l")))
            //: TalkingSocketManager.shared.isFreeCall = true
            DataSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: str_endValue.map{toUp(view: $0)}, encoding: .utf8)!: (str_onTransitionValue.replacingOccurrences(of: "table", with: "q") + "estCa" + String(str_requestValue)), String(bytes: str_viewArrayTitle.map{playerAddSource(text: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        DataSocketDelegate.shared.demonstrateInfo(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_endValue.map{toUp(view: $0)}, encoding: .utf8)!: (String(str_sizeByText) + String(str_attentionName.prefix(5))), String(bytes: str_viewArrayTitle.map{playerAddSource(text: $0)}, encoding: .utf8)!: [(String(str_colorMaxData.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.changeInErrno(cmd: (String(str_firstCellData.prefix(6)) + String(str_numberData.suffix(6))))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func giftCircle(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(str_colorMaxData.prefix(5)))].intValue
    }
}

// MARK: - MediaCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension MediaShouldReactiveCompatible: MediaCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func fileNameMode(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func backgroundTo(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func star(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func callArray(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            ButtonDelegateHelper.semipublicSledge(body: String(bytes: str_upEqualData.map{inCenter(progress: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = TargetMeasurable.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            clubMember(toUid: String(uid))
        }
        //: stop_randomVideo()
        capture()
        //: WithAdministratorAppManager.share.start1v1TalkCall(info: data, situation: .Calling)
        WithAdministratorAppManager.share.pointDelay(info: data, situation: .Calling)
    }
}

// MARK: - InstanceObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension MediaShouldReactiveCompatible: InstanceObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func noneFileShow(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        changeInErrno(cmd: (str_nearText.replacingOccurrences(of: "status", with: "f") + "eCall"))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func designModel(data _: [String: Any]) {}
}

// MARK: - StartErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension MediaShouldReactiveCompatible: StartErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func number1(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(str_viewTitle.suffix(5)) + String(str_onValue)) { // 取消通话异常
            //: stop_randomVideo()
            capture()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(str_firstCellData.prefix(6)) + String(str_numberData.suffix(6))), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            capture()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_fileNextValue) + String(str_headValue))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.atShow(showMsg: msg)
            //: uploadFailTime(type: 2)
            moveType(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(str_firstCellData.prefix(6)) + String(str_numberData.suffix(6))), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(str_countGestureText.prefix(6)) + "fter")].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(str_fileNextValue) + String(str_headValue))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.changeInErrno(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(str_titleValue) + String(str_ofGoTransportValue.suffix(7))), errorNo == Salmagundi.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            capture()
            //: guard WithAdministratorAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.normal.rawValue else { return }
            //: LeadingEqualReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            LeadingEqualReactiveCompatible.share.toParams(clickEvent: kLet_roomTableData, sufficient: false)
            //: uploadFailTime(type: 1)
            moveType(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        changeInErrno(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension MediaShouldReactiveCompatible {
    //: func uploadFailTime(type: Int) {
    func moveType(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(str_arcValue) + String(str_remindViewName))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(str_frameText) + String(str_playerContent.suffix(8)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kLet_voiceText.workingPapers(eventID: kLet_acceptTitle, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func clubMember(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(str_frameText) + String(str_playerContent.suffix(8)))] = time
        //: parm["toUid"] = toUid
        parm[(String(str_sendLabData.prefix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kLet_voiceText.workingPapers(eventID: kLet_robotData, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension MediaShouldReactiveCompatible {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func priceModel() {
        //: guard WithAdministratorAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !WithAdministratorAppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !WithAdministratorAppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = SumryEnableNumeric.randow
        //: if WithAdministratorAppManager.share.loginUserMode.freeCallTimes > 0 && WithAdministratorAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if WithAdministratorAppManager.share.loginUserMode.freeCallTimes > 0, WithAdministratorAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if WithAdministratorAppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if WithAdministratorAppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(str_postName.suffix(7)) + str_modelValue.replacingOccurrences(of: "collection", with: "n")).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = WithAdministratorAppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = WithAdministratorAppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(str_valueCenterTitle.prefix(6))).localizedArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.behindUpRoom(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(str_valueCenterTitle.prefix(6))).localizedArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.behindUpRoom(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = WithAdministratorAppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = WithAdministratorAppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = WithAdministratorAppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = WithAdministratorAppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !WithAdministratorAppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !WithAdministratorAppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func videoFrom() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func sufficientBring() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(kLet_messageContent + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func infoAcrossPush() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(priceModel),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: kLet_keyText,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(sizeFee),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: kLet_nameData,
                                               //: object: nil)
                                               object: nil)
    }
}
