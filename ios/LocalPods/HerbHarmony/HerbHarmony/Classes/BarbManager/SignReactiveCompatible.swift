
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_currentValue:[UInt8] = [0xa7,0xac,0xa7,0xb2,0x66,0xa1,0xad,0xa2,0xa3,0xb0,0x78,0x67,0x5e,0xa6,0x9f,0xb1,0x5e,0xac,0xad,0xb2,0x5e,0xa0,0xa3,0xa3,0xac,0x5e,0xa7,0xab,0xae,0xaa,0xa3,0xab,0xa3,0xac,0xb2,0xa3,0xa2]

fileprivate func dataStopEqual(room num: UInt8) -> UInt8 {
    let value = Int(num) - 62
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Load..." :*/
fileprivate let str_modelValue:[Character] = ["L","o","a","d",".",".","."]

/*: "Skip" :*/
fileprivate let str_equalValue:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let str_videoTitle:[UInt8] = [0x19,0x45,0x45,0x41,0x25,0x40,0x40,0x3d,0xff,0x1f,0x32,0x45,0x3a,0x47,0x36,0x25,0x40,0x1b,0x44,0xf9,0xf8,0x43,0x36,0x34,0x39,0x32,0x43,0x38,0x36,0xf8,0xfa]

fileprivate func managerTask(color num: UInt8) -> UInt8 {
    let value = Int(num) - 209
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let str_arrayDataFirstValue:[UInt8] = [0x6b,0x75,0x72,0x78,0x73,0x6b,0x32,0x73,0x72,0x4c,0x7d,0x7b,0x79,0x4f,0x74,0x73,0x6b,0x3a,0x3a,0x73,0x72,0x4c,0x7d,0x7b,0x79,0x4f,0x74,0x73,0x6b,0x34,0x35,0x27]

/*: "jsEvent(onPageShow):  :*/
fileprivate let str_fieldValue:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e","S","h","o","w",")",":"," "]

/*: --- :*/
fileprivate let str_screenLikeValue:String = "---"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let str_fillName:[UInt8] = [0x51,0x43,0x48,0x3e,0x49,0x51,0x8,0x49,0x48,0x2a,0x3b,0x41,0x3f,0x22,0x43,0x3e,0x3f,0x0,0x0,0x49,0x48,0x2a,0x3b,0x41,0x3f,0x22,0x43,0x3e,0x3f,0x2,0x3,0x15]

fileprivate func viewSelf(request num: UInt8) -> UInt8 {
    let value = Int(num) - 218
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let str_meData:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e"]
fileprivate let str_infoName:[Character] = ["H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let str_callName:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let str_finishTitle:[Character] = ["c","a","n","G","o","B","a","c"]
fileprivate let str_labelData:String = "K"

/*: "PHPSESSID= :*/
fileprivate let str_dragValue:[Character] = ["P","H","P","S","E","S","S"]
fileprivate let str_roomName:[Character] = ["I","D","="]

/*: ";uid= :*/
fileprivate let str_shareSkipSuperName:String = ";uid=active video view return return"

/*: "Cookie" :*/
fileprivate let str_cleanContentData:String = "data self image file imageCookie"

/*: "nav_back_black_nor" :*/
fileprivate let str_tableData:String = "nav_bleast view progress view"
fileprivate let str_toTitle:String = "make colorlack_nor"

/*: "ico_intimate_list_sm" :*/
fileprivate let str_modelContent:String = "equal make dataico_in"
fileprivate let str_atName:[Character] = ["t","e","_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let str_tarData:[UInt8] = [0x27,0x29,0x30,0x2c,0x30,0x2c,0x30,0x2c,0x30,0x28,0x61,0x62,0x67,0x72,0x27,0x3d,0x64,0x6e,0x75,0x6f,0x72,0x67,0x6b,0x63,0x61,0x62,0x2e,0x65,0x6c,0x79,0x74,0x73,0x2e,0x5d,0x30,0x5b,0x29,0x27,0x79,0x64,0x6f,0x62,0x27,0x28,0x65,0x6d,0x61,0x4e,0x67,0x61,0x54,0x79,0x42,0x73,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x45,0x74,0x65,0x67,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64]

/*: "Alert" :*/
fileprivate let str_dataValue:[Character] = ["A","l","e","r","t"]

/*: "OK" :*/
fileprivate let str_socialValue:String = "Ostore"

/*: "Cancle" :*/
fileprivate let str_itemValue:[Character] = ["C"]
fileprivate let str_blockTitle:[Character] = ["a","n","c","l","e"]

/*: "Done" :*/
fileprivate let str_addData:String = "text layerDone"

/*: "js call method name =  :*/
fileprivate let str_viewName:String = "js castatus type shared contact"
fileprivate let str_upLayerData:String = "thawake"
fileprivate let str_shEnterValue:String = " "

/*: , message =  :*/
fileprivate let str_formatInsideData:String = "hidden tool, me"
fileprivate let str_arrayTitle:[Character] = [" ","="," "]

/*: "http://" :*/
fileprivate let str_indexTitle:String = "http://self number"

/*: "https://" :*/
fileprivate let str_colorContent:String = "H"
fileprivate let str_loadPageValue:String = "ttps://share progress view data live"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let str_iconName:[UInt8] = [0x58,0x4f,0x5c,0xe,0x4d,0x41,0x41,0x45,0x47,0x4b,0x60,0x4f,0x43,0x4b,0x5d,0xe,0x13,0xe,0x4a,0x41,0x4d,0x5b,0x43,0x4b,0x40,0x5a,0x0,0x4d,0x41,0x41,0x45,0x47,0x4b,0x0,0x5d,0x5e,0x42,0x47,0x5a,0x6,0x9,0x15,0xe,0x9,0x7,0x0,0x43,0x4f,0x5e,0x6,0x48,0x5b,0x40,0x4d,0x5a,0x47,0x41,0x40,0x6,0x4d,0x41,0x41,0x45,0x47,0x4b,0x7,0xe,0x55,0xe,0x5c,0x4b,0x5a,0x5b,0x5c,0x40,0xe,0x4d,0x41,0x41,0x45,0x47,0x4b,0x0,0x5d,0x5e,0x42,0x47,0x5a,0x6,0x9,0x13,0x9,0x7,0x75,0x1e,0x73,0xe,0x53,0xe,0x7,0x15,0x24]

private func sharedSend(constraint num: UInt8) -> UInt8 {
    return num ^ 46
}

/*: "uid= :*/
fileprivate let str_colorText:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let str_skipText:[Character] = [";","d","o"]
fileprivate let str_tableValue:[Character] = ["m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let str_indicatorGiftTitle:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let str_groupTitle:String = "docfirst"
fileprivate let str_appContent:[Character] = ["k","i","e","=","\'"]

/*: ';\n" :*/
fileprivate let str_indexData:String = "\';\n"

/*: ;domin= :*/
fileprivate let str_onlineName:String = "leading share;domin="

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let str_imageTitle:[UInt8] = [0xfd,0xf2,0xb4,0xbc,0xf7,0xfb,0xfb,0xff,0xfd,0xf1,0xda,0xf5,0xf9,0xf1,0xe7,0xba,0xfd,0xfa,0xf0,0xf1,0xec,0xdb,0xf2,0xbc,0xb3,0xc4,0xdc,0xc4,0xc7,0xd1,0xc7,0xc7,0xdd,0xd0,0xb3,0xbd,0xb4,0xa9,0xa9,0xb4,0xb9,0xa5,0xbd,0xb4,0xef,0xb4,0xf0,0xfb,0xf7,0xe1,0xf9,0xf1,0xfa,0xe0,0xba,0xf7,0xfb,0xfb,0xff,0xfd,0xf1,0xa9,0xb3]

/*: '; };\n" :*/
fileprivate let str_userData:String = "\'; };\n"

/*: '" :*/
fileprivate let str_normalViewName:String = "\'"

/*: "h5Domain =  :*/
fileprivate let str_leadingContent:String = "h5Dpath make"
fileprivate let str_makeContent:[Character] = ["o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let str_viewTitle:[Character] = ["g","e","t","A","l","l","C","o"]
fileprivate let str_tagValue:[Character] = ["o","k","i","e","s"," ","="," "]

/*: "PHPSESSID" :*/
fileprivate let str_textToData:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "uid" :*/
fileprivate let str_bottomName:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let str_pathData:String = "self inside var view heightappCoo"

/*: "UID" :*/
fileprivate let str_panData:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let str_numberValue:String = "nnameig"
fileprivate let str_gameText:String = "progress buttonuserA"
fileprivate let str_makeValue:String = "targetent"

/*: "xunwu_iphone" :*/
fileprivate let str_imageContentData:[Character] = ["x","u","n","w","u","_","i","p"]
fileprivate let str_videoValue:String = "HONE"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let str_targetValue:String = "xunwu_view value by extension"
fileprivate let str_endManagerName:String = "ne xstring index self let"
fileprivate let str_imageTitleInName:String = "model addrsion/"

/*:  xunwu_package/ :*/
fileprivate let str_collectionData:String = "to log icon video push xunwu_p"
fileprivate let str_barText:String = "temp"
fileprivate let str_videoLogModeText:String = "self returnkage/"

/*:  xunwu_bundleId/ :*/
fileprivate let str_clickBlockData:[Character] = [" ","x","u","n","w","u","_","b","u","n","d","l","e","I","d","/"]

/*:   webkit/1" :*/
fileprivate let str_originTitle:[Character] = [" "," ","w","e","b"]
fileprivate let str_kindPageValue:[Character] = ["k","i","t","/","1"]

/*: "  :*/
fileprivate let str_callViewName:String = " "

/*: " xunwu_device_id/ :*/
fileprivate let str_serverName:String = " xunwshow view data target let"
fileprivate let str_headRequestValue:String = "ce_id/arena path data self"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SignReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit

//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class SignReactiveCompatible: ScaleReactiveCompatible {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: HandleBladeConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: MBridgeCollection?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: MBridgeCollection?, webConfig: HandleBladeConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_currentValue.map{dataStopEqual(room: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        getStarted()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        lastHide()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(str_modelValue))
        //: addNotification()
        dataCurtain()
        //: func__checkConfig()
        ethics()
        //: func__setUpWebView()
        funcToView()
        //: func__registerObserver()
        mixIn()
        //: func__beginStartRequest()
        actionDoing()
        //: func_checkAppleIAP()
        detailFunc()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_meData, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            towardMainConfig(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.alright(), .font: UIFont.liveAttention(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.alright(), .font: UIFont.liveAttention(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_equalValue)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension SignReactiveCompatible {
    /// 添加通知
    //: private func addNotification() {
    private func dataCurtain() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(margin),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: kLet_rejectValue,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(getStarted),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func margin() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_videoTitle.map{managerTask(color: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func getStarted() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_arrayDataFirstValue.map{$0^28}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_fieldValue)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func lastHide() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_fillName.map{viewSelf(request: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_meData) + String(str_infoName)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension SignReactiveCompatible {
    //: func func__registerObserver() {
    func mixIn() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(str_callName)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(str_finishTitle) + str_labelData.lowercased()))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(discounts), name: kLet_errorName, object: nil)
    }

    //: func func__setUpWebView() {
    func funcToView() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        giveAndTakeAction()
        //: func__addBridgeMethod()
        groceryList()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.air()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            StandardProgressHUD.aEnablee(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(kLet_messageContent)
            }
        }
    }

    //: func func__beginStartRequest() {
    func actionDoing() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.nonevent()
            }
            //: } else {
        } else {
            //: func__startRequest()
            nonevent()
        }
    }

    //: func func__startRequest() {
    func nonevent() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = WithAdministratorAppManager.share.loginUid
        let uid = WithAdministratorAppManager.share.loginUid
        //: let ssid = WithAdministratorAppManager.share.loginSessionId
        let ssid = WithAdministratorAppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(str_dragValue) + String(str_roomName)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(str_shareSkipSuperName.prefix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(str_cleanContentData.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        trenchantColor()
        //: func__initRightButton()
        colouring()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func detailFunc() {
        //: if webUrlType == .RechargeFullPage ||
        if webUrlType == .RechargeFullPage ||
            //: webUrlType == .RechargeHalfPage ||
            webUrlType == .RechargeHalfPage ||
            //: urlStr.contains(WebUrlType.RechargeAlert.rawValue) ||
            urlStr.contains(MBridgeCollection.RechargeAlert.rawValue) ||
            //: urlStr.contains(WebUrlType.SubscribeAlert.rawValue) {
            urlStr.contains(MBridgeCollection.SubscribeAlert.rawValue)
        {
            //: AppleIAPManager.shared.completionHandle = { [weak self] type, reportMoney in
            MBankManagerTransactionObserver.shared.completionHandle = { [weak self] type, reportMoney in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if type == .veritySucceed {
                if type == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney)
                    self.marginalCost(price: reportMoney)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            MBankManagerTransactionObserver.shared.customDisplay()
        }
    }

    //: func func__initLeftButton() {
    func shirttailCoatButton() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.nearCounterval(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.nearCounterval(name: (String(str_tableData.prefix(5)) + "ack_b" + String(str_toTitle.suffix(8)))), style: .plain, target: self, action: #selector(funcSender(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func colouring() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(MBridgeCollection.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.nearCounterval(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.nearCounterval(name: (String(str_modelContent.suffix(6)) + "tima" + String(str_atName))), style: .plain, target: self, action: #selector(aboveFront))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func funcSender(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        colorMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.actionController() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: SignReactiveCompatible.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! SignReactiveCompatible).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! SignReactiveCompatible).getStarted()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func ethics() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func closeTo(sucess: Bool) {
        //: ProgressHUD.dismiss()
        StandardProgressHUD.giftTo()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func trenchantColor() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: str_tarData.reversed(), encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func tourl(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: LeadingEqualReactiveCompatible.share.func__pushToWebVC(urlStr: url)
            LeadingEqualReactiveCompatible.share.loadWith(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func comeEnable(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func discounts() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: kLet_errorName, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func aboveFront() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = SubmitUpView(frame: CGRect(x: 0, y: 0, width: kLet_senseContent, height: kLet_environmentClickValue))
        //: view.show()
        view.errorShow()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension SignReactiveCompatible: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            StandardProgressHUD.aEnablee(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            closeTo(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        shirttailCoatButton()
        //: func__clearJSBgColor()
        trenchantColor()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            closeTo(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            closeTo(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(str_dataValue)), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(str_itemValue) + String(str_blockTitle)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(str_addData.suffix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension SignReactiveCompatible: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(str_viewName.prefix(5)) + "ll me" + str_upLayerData.replacingOccurrences(of: "awake", with: "od") + " name =" + str_shEnterValue.capitalized) + "\(message.name)" + (String(str_formatInsideData.suffix(4)) + "ssage" + String(str_arrayTitle)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.stateMessage(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func groceryList() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ModelVolumeTableConvertible.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func colorMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ModelVolumeTableConvertible.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension SignReactiveCompatible {
    //: func func__addUserScript() {
    func afterEnable() {
        //: var htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(str_indexTitle.prefix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(str_indexTitle.prefix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((str_colorContent.lowercased() + String(str_loadPageValue.prefix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (str_colorContent.lowercased() + String(str_loadPageValue.prefix(7))), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = WithAdministratorAppManager.share.loginUserMode.userID
        let tempUid: String = WithAdministratorAppManager.share.loginUserMode.userID
        //: let tempSessionID: String = WithAdministratorAppManager.share.loginSessionId
        let tempSessionID: String = WithAdministratorAppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: str_iconName.map{sharedSend(constraint: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_colorText)) + "\(tempUid)" + (String(str_skipText) + String(str_tableValue)) + "\(htmlDomain)" + (String(str_indicatorGiftTitle))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((str_groupTitle.replacingOccurrences(of: "first", with: "u") + "ment.coo" + String(str_appContent)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(str_dragValue) + String(str_roomName)) + "\(tempSessionID)" + (String(str_onlineName.suffix(7))) + "\(htmlDomain)" + (String(str_indicatorGiftTitle))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((str_groupTitle.replacingOccurrences(of: "first", with: "u") + "ment.coo" + String(str_appContent)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: str_imageTitle.map{$0^148}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_colorText)) + "\(tempUid)" + (String(str_skipText) + String(str_tableValue)) + "\(htmlDomain)" + (String(str_indicatorGiftTitle))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((str_groupTitle.replacingOccurrences(of: "first", with: "u") + "ment.coo" + String(str_appContent)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_colorText)) + "\(tempSessionID)" + (String(str_skipText) + String(str_tableValue)) + "\(htmlDomain)" + (String(str_indicatorGiftTitle))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((str_groupTitle.replacingOccurrences(of: "first", with: "u") + "ment.coo" + String(str_appContent)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func towardMainConfig(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(str_leadingContent.prefix(3)) + String(str_makeContent)) + "\(htmlDomain)")
            //: let userID: String = WithAdministratorAppManager.share.loginUserMode.userID
            let userID: String = WithAdministratorAppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(str_viewTitle) + String(str_tagValue)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(str_textToData)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(str_bottomName)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.actionDoing()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.exceptImage(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func exceptImage(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = WithAdministratorAppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(str_leadingContent.prefix(3)) + String(str_makeContent)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: DoingAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(str_pathData.suffix(6)) + "kies = ") + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(str_panData)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(str_bottomName))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie = HTTPCookie(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(str_textToData)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(str_textToData))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie = HTTPCookie(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.actionDoing()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func giveAndTakeAction() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((str_numberValue.replacingOccurrences(of: "name", with: "av") + "ator." + String(str_gameText.suffix(5)) + str_makeValue.replacingOccurrences(of: "target", with: "g"))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (str_numberValue.replacingOccurrences(of: "name", with: "av") + "ator." + String(str_gameText.suffix(5)) + str_makeValue.replacingOccurrences(of: "target", with: "g")))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(str_imageContentData) + str_videoValue.lowercased())) {
                let str = "xunwu_iphone xunwu_version/\(kLet_timeTitle) xunwu_package/\(kLet_nameTopText) xunwu_bundleId/\(kLet_recordName)  webkit/1"
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.popTo { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(str_serverName.prefix(5)) + "u_devi" + String(str_headRequestValue.prefix(6))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
