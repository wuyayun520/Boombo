
//: Declare String Begin

/*: "formatType" :*/
fileprivate let str_hideValue:String = "style"
fileprivate let str_buttonName:[Character] = ["o","r","m","a","t","T","y","p","e"]

/*: "plain" :*/
fileprivate let str_nearName:[Character] = ["p","l","a","i","n"]

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_modelText:[UInt8] = [0x99,0x9e,0x99,0x84,0xd8,0x93,0x9f,0x94,0x95,0x82,0xca,0xd9,0xd0,0x98,0x91,0x83,0xd0,0x9e,0x9f,0x84,0xd0,0x92,0x95,0x95,0x9e,0xd0,0x99,0x9d,0x80,0x9c,0x95,0x9d,0x95,0x9e,0x84,0x95,0x94]

private func dipTotalo(lab num: UInt8) -> UInt8 {
    return num ^ 240
}

/*: "#FF2348" :*/
fileprivate let str_awakeTitle:[Character] = ["#","F","F","2","3","4","8"]

/*: "icon_lounge" :*/
fileprivate let str_atValue:String = "row name succeed for coloricon_"

/*: "icon_home_v" :*/
fileprivate let str_priceData:String = "lab"
fileprivate let str_styleValue:[Character] = ["c","o","n","_","h","o","m","e","_","v"]

/*: "icon_chat_list_new" :*/
fileprivate let str_sizeName:String = "icon_cmanager available"
fileprivate let str_managerData:String = "table let user true viewt_new"

/*: "Official" :*/
fileprivate let str_iconTitle:[Character] = ["O","f","f","i","c","i","a","l"]

/*: "#30D00B" :*/
fileprivate let str_callTitle:String = "object"
fileprivate let str_succeedValue:String = "30D00Benter or application at"

/*: "iv_message_honey" :*/
fileprivate let str_aspectContent:[Character] = ["i","v","_","m","e","s","s","a","g","e","_","h"]
fileprivate let str_mainMagnitudeData:[Character] = ["o","n","e","y"]

/*: "#FF60B9" :*/
fileprivate let str_valueAtExtensionData:String = "var make model share extension#FF60B9"

/*: "#EEEEEE" :*/
fileprivate let str_recordValue:[Character] = ["#","E","E","E","E","E","E"]

/*: "opacity" :*/
fileprivate let str_mainName:[Character] = ["o"]
fileprivate let str_backgroundData:String = "pacitsource"

/*: "path" :*/
fileprivate let str_modelTitle:String = "patindex"

/*: "extra" :*/
fileprivate let str_detailName:String = "extrrank"

/*: "msgInfo" :*/
fileprivate let str_mapData:String = "msgInfolet component remove"

/*: "content" :*/
fileprivate let str_tableValue:[Character] = ["c","o","n","t","e","n","t"]

/*: "messageType" :*/
fileprivate let str_valueHeadMakeName:String = "memakea"
fileprivate let str_videoTitle:String = "self size model text appgeType"

/*: "[Reply messages to get points]" :*/
fileprivate let str_modelContent:String = "[Repcontainer shared cover leading"
fileprivate let str_viewTitle:String = "pastages"
fileprivate let str_makeContent:[Character] = [" ","p"]
fileprivate let str_atData:String = "interrupt to info make ofoints]"

/*: "title" :*/
fileprivate let str_dateValue:[Character] = ["t","i","t","l","e"]

/*: "img" :*/
fileprivate let str_topTitle:String = "imadd"

/*: "[Photo]" :*/
fileprivate let str_managerValue:String = "path source[Photo"
fileprivate let str_bagTitle:[Character] = ["]"]

/*: "[Audio]" :*/
fileprivate let str_willOrientationValue:String = "model view in[Audio"
fileprivate let str_makeTitle:String = "equal"

/*: "rYMsgType" :*/
fileprivate let str_viewData:String = "let view makerYMsgType"

/*: "GJ:CallCustom" :*/
fileprivate let str_miniData:String = "GJ:Calin between false model view"
fileprivate let str_picValue:[Character] = ["t","o","m"]

/*: "callCmd" :*/
fileprivate let str_infoFromData:[Character] = ["c","a","l","l","C","m","d"]

/*: "missCall" :*/
fileprivate let str_tumValue:[Character] = ["m","i","s","s","C","a","l","l"]

/*: "[Missed Call]" :*/
fileprivate let str_nameData:String = "[Misseback view more me leading"
fileprivate let str_replyTitle:String = "d Call]constant report if video case"

/*: "logType" :*/
fileprivate let str_makeValue:String = "selected"
fileprivate let str_valueTitle:String = "table count table selfogType"

/*: "[Voice Call]" :*/
fileprivate let str_colorData:[Character] = ["[","V","o","i","c","e"," "]
fileprivate let str_cellName:String = "to class var make normalCall]"

/*: "[Video Call]" :*/
fileprivate let str_labStatusValue:[Character] = ["[","V","i","d","e","o"," "]
fileprivate let str_socialContent:[Character] = ["C","a","l","l","]"]

/*: "video" :*/
fileprivate let str_availableTitle:String = "vimakeeo"

/*: "contentType" :*/
fileprivate let str_imageEqualName:[Character] = ["c","o","n","t","e","n","t","T","y","p"]
fileprivate let str_cancelName:String = "photo"

/*: "VideoMsg" :*/
fileprivate let str_normalTextValue:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "[Short Video]" :*/
fileprivate let str_enableOutputCornerTitle:String = "[Shorchannel table if return"
fileprivate let str_likeName:String = "t Video]height class interval"

/*: "match" :*/
fileprivate let str_colorValue:[Character] = ["m","a","t","c","h"]

/*: "[Cupid messaging]" :*/
fileprivate let str_renderValue:[Character] = ["[","C","u","p","i"]
fileprivate let str_frameValue:[Character] = ["d"," "]
fileprivate let str_toValue:String = "mareas"

/*: "GJ:Sess:TxtCustom" :*/
fileprivate let str_directionContent:String = "GJ:Setype for request translate"
fileprivate let str_labText:String = "moment result range playerxtCustom"

/*: "isSend" :*/
fileprivate let str_nearTitle:[Character] = ["i","s","S","e","n","d"]

/*: "Sent Gift" :*/
fileprivate let str_giftData:[Character] = ["S","e","n","t"," ","G","i","f"]
fileprivate let str_userResultBackgroundValue:String = "location"

/*: "Receive Gift" :*/
fileprivate let str_minuteTitle:[Character] = ["R","e","c","e","i","v"]
fileprivate let str_actualText:[Character] = ["e"," ","G","i","f","t"]

/*: "uid" :*/
fileprivate let str_pointTitle:String = "UID"

/*: "gift" :*/
fileprivate let str_resultValue:[Character] = ["g","i","f","t"]

/*: "name" :*/
fileprivate let str_atShowName:String = "tableme"

/*: "num" :*/
fileprivate let str_inputErrorTitle:String = "namem"

/*:   :*/
fileprivate let str_factorText:String = " "

/*:  x :*/
fileprivate let str_viewFromName:String = "want self class count perform x"

/*: "imageUri" :*/
fileprivate let str_mName:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "imageData" :*/
fileprivate let str_eventValue:[Character] = ["i","m"]
fileprivate let str_timeData:String = "tab enable self label modelageData"

/*: "[Location]" :*/
fileprivate let str_sureData:String = "while action file[Loc"
fileprivate let str_equalData:String = "n]index let request"

/*: "<img>" :*/
fileprivate let str_frameTitle:String = "equal view<img>"

/*: "</img>" :*/
fileprivate let str_managerName:String = "equal"
fileprivate let str_messageData:String = "/img>make background if top"

/*: "wantGift" :*/
fileprivate let str_windowViewContent:[Character] = ["w","a","n","t","G","i"]
fileprivate let str_timeValue:[Character] = ["f","t"]

/*: "intimatePhoto" :*/
fileprivate let str_playData:[Character] = ["i","n","t","i","m","a","t","e","P","h","o"]
fileprivate let str_routeValue:String = "tpop"

/*: "audioData" :*/
fileprivate let str_errorUserName:[Character] = ["a","u","d","i","o","D","a"]
fileprivate let str_viewValue:[Character] = ["t","a"]

/*: "audioUri" :*/
fileprivate let str_nowTitle:[Character] = ["a"]
fileprivate let str_sizeFilterWaitTitle:String = "group item typeudioUri"

/*: "draftContent" :*/
fileprivate let str_searchData:String = "draview"
fileprivate let str_sectionClearTitle:[Character] = ["t"]

/*: "[draft]%@" :*/
fileprivate let str_userName:[Character] = ["[","d","r","a","f","t","]","%","@"]

/*: "New" :*/
fileprivate let str_aErrorName:[Character] = ["N","e","w"]

/*: "99+" :*/
fileprivate let str_makeName:String = "atat+"

/*: "Account is restricted！" :*/
fileprivate let str_selectName:String = "add right error countAc"
fileprivate let str_colonValue:[Character] = ["c","o","u","n","t"," ","i","s"," ","r","e","s","t","r","i","c","t","e","d","！"]

/*: "%@ Online" :*/
fileprivate let str_headName:[Character] = ["%","@"," ","O","n","l"]
fileprivate let str_playerContent:String = "iteme"

/*: "Messages not been replied here" :*/
fileprivate let str_primaryLabelValue:[Character] = ["M","e","s","s","a","g","e","s"," ","n","o","t"," "]
fileprivate let str_backgroundTitle:[Character] = ["b","e","e","n"," ","r","e","p","l","i","e"]
fileprivate let str_textLeadingData:String = "position view titled here"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StandardMainViewCell.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/12.
//

//: import UIKit
import UIKit

//: let RCSystemFormatType                 = "formatType"
let kLet_tabName = (str_hideValue.replacingOccurrences(of: "style", with: "f") + String(str_buttonName))
//: let RCSystemFormatTypePlain            = "plain"
let kLet_positionValue = (String(str_nearName))

//: class TalkingChatListTableCell: UITableViewCell {
class StandardMainViewCell: UITableViewCell {
    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: super.init(coder: coder)
        super.init(coder: coder)
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_modelText.map{dipTotalo(lab: $0)}, encoding: .utf8)!)
    }

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: createCellUI()
        beUi()
        //: createCellUIConstraints()
        fileEqual()
    }

    // MARK: - Lazy load

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let imageView = UIImageView.init()
        let imageView = UIImageView()
        //: imageView.layer.cornerRadius = 30
        imageView.layer.cornerRadius = 30
        //: imageView.clipsToBounds = true
        imageView.clipsToBounds = true
        //: imageView.backgroundColor = UIColor.white
        imageView.backgroundColor = UIColor.white
        //: imageView.contentMode = .scaleAspectFill
        imageView.contentMode = .scaleAspectFill
        //: return imageView
        return imageView
        //: }()
    }()

    //: private lazy var iconBorder: UIButton = {
    private lazy var iconBorder: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: self.addSubview(btn)
        self.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var badgeLab: UILabel = {
    private lazy var badgeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .liveAttention(type: .Medium, fontSize: 13)
        label.font = .liveAttention(type: .Medium, fontSize: 13)
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.textColor = .white
        label.textColor = .white
        //: label.backgroundColor = UIColor(hex: "#FF2348")
        label.backgroundColor = UIColor(hex: (String(str_awakeTitle)))
        //: label.layer.cornerRadius = 10
        label.layer.cornerRadius = 10
        //: label.layer.masksToBounds = true
        label.layer.masksToBounds = true
        //: label.layer.borderColor = UIColor.white.cgColor
        label.layer.borderColor = UIColor.white.cgColor
        //: label.layer.borderWidth = 1
        label.layer.borderWidth = 1
        //: label.isHidden = true
        label.isHidden = true
        //: return label
        return label
        //: }()
    }()

    //: private lazy var nickNameLabel: UILabel = {
    private lazy var nickNameLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .liveAttention(type: .Medium, fontSize: 18)
        label.font = .liveAttention(type: .Medium, fontSize: 18)
        //: label.textColor = .occurrence()
        label.textColor = .occurrence()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var msgShowLabel: UILabel = {
    private lazy var msgShowLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .liveAttention(type: .Regular, fontSize: 15)
        label.font = .liveAttention(type: .Regular, fontSize: 15)
        //: label.textColor = .inexpensiveness()
        label.textColor = .inexpensiveness()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .liveAttention(type: .Medium, fontSize: 12)
        label.font = .liveAttention(type: .Medium, fontSize: 12)
        //: label.textColor = .inexpensiveness()
        label.textColor = .inexpensiveness()
        //: return label
        return label
        //: }()
    }()

    //: lazy var loungeImgV: UIImageView = {
    lazy var loungeImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.nearCounterval(name: "icon_lounge")
        img.image = UIImage.nearCounterval(name: (String(str_atValue.suffix(5)) + "lounge"))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var authImgV: UIImageView = {
    private lazy var authImgV: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image = UIImage.nearCounterval(name: "icon_home_v")
        imgV.image = UIImage.nearCounterval(name: (str_priceData.replacingOccurrences(of: "lab", with: "i") + String(str_styleValue)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var newImgV: TalkingButton = {
    private lazy var newImgV: RoomThen = {
        //: let imgV = TalkingButton()
        let imgV = RoomThen()
        //: imgV.setBackgroundImage(UIImage.nearCounterval(name: "icon_chat_list_new"), for: .normal)
        imgV.setBackgroundImage(UIImage.nearCounterval(name: (String(str_sizeName.prefix(6)) + "hat_lis" + String(str_managerData.suffix(5)))), for: .normal)
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var officialImgV: UIButton = {
    private lazy var officialImgV: UIButton = {
        //: let img = UIButton()
        let img = UIButton()
        //: img.setBackgroundColor(color: UIColor.msgTipsColor(), forState: .normal)
        img.backgroundBlock(color: UIColor.userError(), forState: .normal)
        //: img.setTitle("Official".localized, for: .normal)
        img.setTitle((String(str_iconTitle)).localized, for: .normal)
        //: img.setTitleColor(.white, for: .normal)
        img.setTitleColor(.white, for: .normal)
        //: img.titleLabel?.font = UIFont.unprecedented(fontSize: 11)
        img.titleLabel?.font = UIFont.unprecedented(fontSize: 11)
        //: img.layer.cornerRadius = 8
        img.layer.cornerRadius = 8
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var onlineStatus: UIView = {
    private lazy var onlineStatus: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(hex: "#30D00B")
        v.backgroundColor = UIColor(hex: (str_callTitle.replacingOccurrences(of: "object", with: "#") + String(str_succeedValue.prefix(6))))
        //: v.layer.cornerRadius = 5
        v.layer.cornerRadius = 5
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var imtimateBtn: TalkingButton = {
    private lazy var imtimateBtn: RoomThen = {
        //: let btn = TalkingButton.init()
        let btn = RoomThen()
        //: btn.setImage(UIImage.nearCounterval(name: "iv_message_honey"), for: .normal)
        btn.setImage(UIImage.nearCounterval(name: (String(str_aspectContent) + String(str_mainMagnitudeData))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.titleLabel?.font = .liveAttention(type: .Medium, fontSize: 12)
        btn.titleLabel?.font = .liveAttention(type: .Medium, fontSize: 12)
        //: btn.setTitleColor(UIColor.init(hex: "#FF60B9"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_valueAtExtensionData.suffix(7)))), for: .normal)
        //: btn.contentHorizontalAlignment = .right
        btn.contentHorizontalAlignment = .right
        //: btn.isHidden = true
        btn.isHidden = true
        //: if LanguageManager.shared.direction == .rightToLeft {
        if MeanSunLanguageManager.shared.direction == .rightToLeft {
            //: btn.contentHorizontalAlignment = .left
            btn.contentHorizontalAlignment = .left
        }
        //: return btn
        return btn
        //: }()
    }()
}

//: extension TalkingChatListTableCell {
extension StandardMainViewCell {
    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        //: let animationColor = UIColor.init(hex: "#EEEEEE")
        let animationColor = UIColor(hex: (String(str_recordValue)))
        //: let animationDuration: Double = 0.4
        let animationDuration = 0.4

        //: super.touchesBegan(touches, with: event)
        super.touchesBegan(touches, with: event)
        //: let coverView = UIView(frame: bounds)
        let coverView = UIView(frame: bounds)
        //: coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: coverView.backgroundColor = UIColor.clear
        coverView.backgroundColor = UIColor.clear
        //: self.addSubview(coverView)
        self.addSubview(coverView)

        //: let touch = touches.first!
        let touch = touches.first!
        //: let point = touch.location(in: self)
        let point = touch.location(in: self)

        //: let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))
        let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))

        //: let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        //: let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        //: let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)
        let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)

        //: let rippleLayer = CAShapeLayer()
        let rippleLayer = CAShapeLayer()
        //: rippleLayer.opacity = 1.0
        rippleLayer.opacity = 1.0
        //: rippleLayer.fillColor = animationColor?.cgColor
        rippleLayer.fillColor = animationColor?.cgColor
        //: rippleLayer.path = circleMaskPathFinal.cgPath
        rippleLayer.path = circleMaskPathFinal.cgPath
        //: coverView.layer.addSublayer(rippleLayer)
        coverView.layer.addSublayer(rippleLayer)

        // fade up
        //: let fadeUp = CABasicAnimation(keyPath: "opacity")
        let fadeUp = CABasicAnimation(keyPath: (String(str_mainName) + str_backgroundData.replacingOccurrences(of: "source", with: "y")))
        //: fadeUp.beginTime = CACurrentMediaTime()
        fadeUp.beginTime = CACurrentMediaTime()
        //: fadeUp.duration = 0.1
        fadeUp.duration = 0.1
        //: fadeUp.toValue = 0.8
        fadeUp.toValue = 0.8
        //: fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fadeUp.fillMode = CAMediaTimingFillMode.forwards
        fadeUp.fillMode = CAMediaTimingFillMode.forwards
        //: fadeUp.isRemovedOnCompletion = false
        fadeUp.isRemovedOnCompletion = false
        //: rippleLayer.add(fadeUp, forKey: nil)
        rippleLayer.add(fadeUp, forKey: nil)

        // fade down
        //: let fade = CABasicAnimation(keyPath: "opacity")
        let fade = CABasicAnimation(keyPath: (String(str_mainName) + str_backgroundData.replacingOccurrences(of: "source", with: "y")))
        //: fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        //: fade.duration = 0.1
        fade.duration = 0.1
        //: fade.toValue = 0
        fade.toValue = 0
        //: fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fade.fillMode = CAMediaTimingFillMode.forwards
        fade.fillMode = CAMediaTimingFillMode.forwards
        //: fade.isRemovedOnCompletion = false
        fade.isRemovedOnCompletion = false
        //: rippleLayer.add(fade, forKey: nil)
        rippleLayer.add(fade, forKey: nil)

        // change path
        //: CATransaction.begin()
        CATransaction.begin()
        //: let maskLayerAnimation = CABasicAnimation(keyPath: "path")
        let maskLayerAnimation = CABasicAnimation(keyPath: (str_modelTitle.replacingOccurrences(of: "index", with: "h")))
        //: maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        //: maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        //: maskLayerAnimation.beginTime = CACurrentMediaTime()
        maskLayerAnimation.beginTime = CACurrentMediaTime()
        //: maskLayerAnimation.duration = 0.1
        maskLayerAnimation.duration = 0.1
        //: maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: CATransaction.setCompletionBlock({
        CATransaction.setCompletionBlock {
            //: coverView.removeFromSuperview()
            coverView.removeFromSuperview()
            //: })
        }
        //: rippleLayer.add(maskLayerAnimation, forKey: "path")
        rippleLayer.add(maskLayerAnimation, forKey: (str_modelTitle.replacingOccurrences(of: "index", with: "h")))
        //: CATransaction.commit()
        CATransaction.commit()
    }
}

// MARK: - Public Event

//: extension TalkingChatListTableCell {
extension StandardMainViewCell {
    /// 刷新cell
    /// - Parameter model: 模型
    //: func refreshCell(model: TalkingConversationModel?) {
    func discoView(model: MediaConversationModel?) {
        //: guard let model = model else { return }
        guard let model = model else { return }
        //: let isOther = (model.chatType == .system || model.chatType == .service ||
        let isOther = (model.chatType == .system || model.chatType == .service ||
            //: model.chatType == .group || model.chatType == .moreMsg)
            model.chatType == .group || model.chatType == .moreMsg)
        // 更新昵称、头像、vip标识
        //: updateUserInfo(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        consumerBackgroundOther(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        // 未读消息数
        //: updateUnreadNumber(unreadCount: Int(model.unreadCount))
        identityDown(unreadCount: Int(model.unreadCount))
        // 详情消息
        //: handleC2CMessage(model: model)
        handleDownEvent(model: model)

        //: if model.chatType == .group || model.chatType == .moreMsg {
        if model.chatType == .group || model.chatType == .moreMsg {
            //: self.accessoryType = .disclosureIndicator
            self.accessoryType = .disclosureIndicator // 添加箭头
            //: } else {
        } else {
            //: self.accessoryType = .none
            self.accessoryType = .none
        }
        // 时间
        //: timeLab.text = nil
        timeLab.text = nil
        //: if isOther == false && model.hasTimestamp {
        if isOther == false, model.hasTimestamp {
            //: let timestamp: Date? = model.timestamp as Date?
            let timestamp: Date? = model.timestamp as Date?
            //: if timestamp != nil && timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
            if timestamp != nil, timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
                //: timeLab.text = NSDate.tx_messageTimeString(showDetail: false, date: timestamp!)
                timeLab.text = NSDate.changeCharacter(showDetail: false, date: timestamp!)
            }
        }
        // 置顶背景色
        //: if model.isPinned == true {
        if model.isPinned == true {
            //: contentView.backgroundColor = UIColor(hex: "#EEEEEE")
            contentView.backgroundColor = UIColor(hex: (String(str_recordValue)))
            //: } else {
        } else {
            //: contentView.backgroundColor = (WithAdministratorAppManager.share.appStatus == AppSkinStatus.special.rawValue) ? .clear:.white
            contentView.backgroundColor = (WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.special.rawValue) ? .clear : .white
        }
        //: self.backgroundColor = contentView.backgroundColor
        self.backgroundColor = contentView.backgroundColor
    }

    /// 处理最新消息
    //: static func handleShowC2CDetailTextOfMessage(model: TalkingConversationModel) -> NSMutableAttributedString? {
    static func pairForData(model: MediaConversationModel) -> NSMutableAttributedString? {
//        messageType 消息类型：1系统消息 2官方小秘 3自动打招呼 4主动打招呼 5付费消息 6普通消息 7主动打招呼未回复 8.主打招呼回复 9，自动打招呼未回复 10, 自动打招呼回复11.发送礼物消息 12互相关注消息 13 取消关注 22家族邀请跳转消息 25索要礼物
        //: var detailAttrString: NSMutableAttributedString?
        var detailAttrString: NSMutableAttributedString?
        //: let change = false
        let change = false
        //: var speColor = [NSValue: UIColor]()
        var speColor = [NSValue: UIColor]()
        //: let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        //: let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing:.MsgDirectionIncoming
        let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing : .MsgDirectionIncoming
        //: var voice_msgInfo: JSON?
        var voice_msgInfo: JSON?

        //: guard model.draftText.isEmptyString else {
        guard model.draftText.isEmptyString else { // 草稿
            //: detailAttrString = self.getDraftString(draft: &model.draftText)
            detailAttrString = self.nickname(draft: &model.draftText)
            //: detailAttrString?.addAttribute(.font, value: UIFont.liveAttention(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            detailAttrString?.addAttribute(.font, value: UIFont.liveAttention(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            //: return detailAttrString
            return detailAttrString
        }

        //: let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        //: let json = JSON(parseJSON: extra!)
        let json = JSON(parseJSON: extra!)
        //: let extraDic = json["extra"]
        let extraDic = json[(str_detailName.replacingOccurrences(of: "rank", with: "a"))]
        //: let msgInfo = extraDic["msgInfo"]
        let msgInfo = extraDic[(String(str_mapData.prefix(7)))]
        //: var content = msgInfo["content"].stringValue.deleteHtmlTips()
        var content = msgInfo[(String(str_tableValue))].stringValue.deleteHtmlTips()
        //: let messageType = msgInfo["messageType"].intValue
        let messageType = msgInfo[(str_valueHeadMakeName.replacingOccurrences(of: "make", with: "ss") + String(str_videoTitle.suffix(6)))].intValue
        //: let formatTypeStr = extraDic["formatType"].stringValue
        let formatTypeStr = extraDic[(str_hideValue.replacingOccurrences(of: "style", with: "f") + String(str_buttonName))].stringValue

        //: if TalkingPrivateChatManager.isReplyToGetPointsMsg(direction, messageType, msgTime: model.listShowMessage?.timestamp) {
        if HandleThen.common(direction, messageType, msgTime: model.listShowMessage?.timestamp) { // 回复获取积分消息
            //: let content = "[Reply messages to get points]".localized
            let content = (String(str_modelContent.prefix(4)) + "ly me" + str_viewTitle.replacingOccurrences(of: "past", with: "ss") + " to get" + String(str_makeContent) + String(str_atData.suffix(6))).localized
            //: let color  = UIColor.appValueColor()
            let color = UIColor.colorPush()
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
            //: speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]
            speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]

            //: } else if model.chatType == .system {
        } else if model.chatType == .system { // 官方消息，只展示文本
            //: var content = extraDic["title"].stringValue
            var content = extraDic[(String(str_dateValue))].stringValue
            //: let sysContent = extraDic["content"].stringValue
            let sysContent = extraDic[(String(str_tableValue))].stringValue
            //: let sysImg = extraDic["img"].stringValue
            let sysImg = extraDic[(str_topTitle.replacingOccurrences(of: "add", with: "g"))].stringValue
            //: if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 {
            if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 { // 跳转系统消息
                //: if content.count == 0 {
                if content.count == 0 {
                    //: content.append(sysContent)
                    content.append(sysContent)
                }
                //: if sysImg.count > 0 {
                if sysImg.count > 0 {
                    //: content.append("[Photo]".localized)
                    content.append((String(str_managerValue.suffix(6)) + String(str_bagTitle)).localized)
                }

                //: } else {
            } else { // 默认系统消息
                //: content = msgInfo["content"].stringValue
                content = msgInfo[(String(str_tableValue))].stringValue
                //: if content.isEmptyString {
                if content.isEmptyString {
                    //: content = json["content"].stringValue
                    content = json[(String(str_tableValue))].stringValue
                }
                //: content = content.deleteHtmlTips()
                content = content.deleteHtmlTips()
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
        } else if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) { // 语音
            //: voice_msgInfo = msgInfo
            voice_msgInfo = msgInfo
            //: content = "[Audio]".localized
            content = (String(str_willOrientationValue.suffix(6)) + str_makeTitle.replacingOccurrences(of: "equal", with: "]")).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话
        } else if json[(String(str_viewData.suffix(9)))].stringValue == (String(str_miniData.prefix(6)) + "lCus" + String(str_picValue)) { // 音视频通话
            if msgInfo["callCmd"] == "missCall" { // 未接来电
                //: content = "[Missed Call]".localized
                content = (String(str_nameData.prefix(6)) + String(str_replyTitle.prefix(7))).localized
                //: } else {
            } else {
                //: if msgInfo["logType"].intValue == 1 { // 音频
                if msgInfo[(str_makeValue.replacingOccurrences(of: "selected", with: "l") + String(str_valueTitle.suffix(6)))].intValue == 1 { // 音频
                    //: content = "[Voice Call]".localized
                    content = (String(str_colorData) + String(str_cellName.suffix(5))).localized
                    //: } else {
                } else {
                    //: content = "[Video Call]".localized
                    content = (String(str_labStatusValue) + String(str_socialContent)).localized
                }
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

        } else if msgInfo["video"].exists() || msgInfo["contentType"] == "VideoMsg" { // 短视频
            //: content = "[Short Video]".localized
            content = (String(str_enableOutputCornerTitle.prefix(5)) + String(str_likeName.prefix(8))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if (extraDic["match"].stringValue == "1" || msgInfo["match"].stringValue == "1") { // 匹配消息
        } else if extraDic[(String(str_colorValue))].stringValue == "1" || msgInfo[(String(str_colorValue))].stringValue == "1" { // 匹配消息
            //: content = "[Cupid messaging]".localized
            content = (String(str_renderValue) + String(str_frameValue) + str_toValue.replacingOccurrences(of: "area", with: "es") + "aging]").localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:Sess:TxtCustom" { // 提示消息
        } else if json[(String(str_viewData.suffix(9)))].stringValue == (String(str_directionContent.prefix(5)) + "ss:T" + String(str_labText.suffix(8))) { // 提示消息
            //: content = json["content"].stringValue
            content = json[(String(str_tableValue))].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 11 {
        } else if messageType == 11 { // 送礼
            //: let isSend = msgInfo["isSend"].string
            let isSend = msgInfo[(String(str_nearTitle))].string
            //: var string1: String?
            var string1: String?
            //: if isSend != nil {
            if isSend != nil {
                //: string1 = isSend!.boolValue ? "Sent Gift".localized:"Receive Gift".localized
                string1 = isSend!.boolValue ? (String(str_giftData) + str_userResultBackgroundValue.replacingOccurrences(of: "location", with: "t")).localized : (String(str_minuteTitle) + String(str_actualText)).localized
                //: } else {
            } else {
                //: let uid = msgInfo["uid"].stringValue
                let uid = msgInfo[(str_pointTitle.lowercased())].stringValue
                //: string1 = (uid == WithAdministratorAppManager.share.loginUserMode.userID) ? "Sent Gift".localized:"Receive Gift".localized
                string1 = (uid == WithAdministratorAppManager.share.loginUserMode.userID) ? (String(str_giftData) + str_userResultBackgroundValue.replacingOccurrences(of: "location", with: "t")).localized : (String(str_minuteTitle) + String(str_actualText)).localized
            }
            //: let name = extraDic["gift"]["name"].stringValue
            let name = extraDic[(String(str_resultValue))][(str_atShowName.replacingOccurrences(of: "table", with: "na"))].stringValue
            //: let num = extraDic["gift"]["num"].stringValue
            let num = extraDic[(String(str_resultValue))][(str_inputErrorTitle.replacingOccurrences(of: "name", with: "nu"))].stringValue

            //: content = "\(string1!) \(name) x\(num)"
            content = "\(string1!) \(name) x\(num)"
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 4 {
        } else if messageType == 4 { // 主动打招呼
//            let uid = msgInfo["uid"].stringValue
//            let toUid = msgInfo["toUid"].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 3 {
        } else if messageType == 3 { // 自动打招呼
            //: let imageUri = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(str_mName))].stringValue
            //: let imageData = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(str_eventValue) + String(str_timeData.suffix(7)))].stringValue

            //: if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(str_willOrientationValue.suffix(6)) + str_makeTitle.replacingOccurrences(of: "equal", with: "]")).localized
                //: } else if (imageUri.count > 0 || imageData.count > 0) {
            } else if imageUri.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(str_managerValue.suffix(6)) + String(str_bagTitle)).localized
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: }else if messageType == 28 {
        } else if messageType == 28 { // 位置
            //: content = "[Location]".localized
            content = (String(str_sureData.suffix(4)) + "atio" + String(str_equalData.prefix(2))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if formatTypeStr == RCSystemFormatTypePlain {
        } else if formatTypeStr == kLet_positionValue {
            //: var content  = msgInfo["content"].stringValue
            var content = msgInfo[(String(str_tableValue))].stringValue
            //: var imageUrl = extraDic["imageUri"].stringValue
            var imageUrl = extraDic[(String(str_mName))].stringValue
            //: let imageData = extraDic["imageData"].stringValue
            let imageData = extraDic[(String(str_eventValue) + String(str_timeData.suffix(7)))].stringValue
            //: if imageUrl.count == 0 || imageData.count > 0 {
            if imageUrl.count == 0 || imageData.count > 0 {
                //: if content.hasPrefix("<img>") && content.hasSuffix("</img>") {
                if content.hasPrefix((String(str_frameTitle.suffix(5)))), content.hasSuffix((str_managerName.replacingOccurrences(of: "equal", with: "<") + String(str_messageData.prefix(5)))) {
                    //: var str = NSString(string: content)
                    var str = NSString(string: content)
                    //: str = str.replacingOccurrences(of: "<img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(str_frameTitle.suffix(5))), with: "") as NSString
                    //: str = str.replacingOccurrences(of: "</img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (str_managerName.replacingOccurrences(of: "equal", with: "<") + String(str_messageData.prefix(5))), with: "") as NSString
                    //: imageUrl = str as String
                    imageUrl = str as String
                }
            }

            //: if (imageUrl.count > 0 || imageData.count > 0) {
            if imageUrl.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(str_managerValue.suffix(6)) + String(str_bagTitle)).localized
                //: } else {
            } else {
                //: content = content.deleteHtmlTips()
                content = content.deleteHtmlTips()
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 25 {
        } else if messageType == 25 { // 索要礼物
            //: if let wantGift  = msgInfo["wantGift"].dictionary {
            if let wantGift = msgInfo[(String(str_windowViewContent) + String(str_timeValue))].dictionary {
                //: let content = wantGift["content"]?.string ?? ""
                let content = wantGift[(String(str_tableValue))]?.string ?? ""
                //: detailAttrString = NSMutableAttributedString(string: content)
                detailAttrString = NSMutableAttributedString(string: content)
            }

            //: } else {
        } else {
            // 图片

            //: let imageUri             = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(str_mName))].stringValue
            //: let imageData            = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(String(str_eventValue) + String(str_timeData.suffix(7)))].stringValue
            //: let intimatePhotoDict    = msgInfo["intimatePhoto"].dictionaryValue // 私密照片
            let intimatePhotoDict = msgInfo[(String(str_playData) + str_routeValue.replacingOccurrences(of: "pop", with: "o"))].dictionaryValue // 私密照片
            //: if (imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0) {
            if imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0 {
                //: content = "[Photo]".localized
                content = (String(str_managerValue.suffix(6)) + String(str_bagTitle)).localized
            }
            // 语音消息
            //: if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(str_willOrientationValue.suffix(6)) + str_makeTitle.replacingOccurrences(of: "equal", with: "]")).localized
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
        }

        //: if (!change) {
        if !change {
            //: if detailAttrString?.string == "[Audio]".localized {
            if detailAttrString?.string == (String(str_willOrientationValue.suffix(6)) + str_makeTitle.replacingOccurrences(of: "equal", with: "]")).localized {
                //: let sendUid = model.sender
                let sendUid = model.sender
                //: let mid = WithAdministratorAppManager.share.loginUserMode.userID
                let mid = WithAdministratorAppManager.share.loginUserMode.userID
                //: if sendUid == mid {
                if sendUid == mid { // 我发送的
                    //: } else {
                } else { // 别人发的
                    //: var downurl = ""
                    var downurl = ""
                    //: if (voice_msgInfo?["audioData"].stringValue.isEmptyString) != nil {
                    if (voice_msgInfo?[(String(str_errorUserName) + String(str_viewValue))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioData"].stringValue
                        downurl = voice_msgInfo![(String(str_errorUserName) + String(str_viewValue))].stringValue
                        //: } else if (voice_msgInfo?["audioUri"].stringValue.isEmptyString) != nil {
                    } else if (voice_msgInfo?[(String(str_nowTitle) + String(str_sizeFilterWaitTitle.suffix(7)))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioUri"].stringValue
                        downurl = voice_msgInfo![(String(str_nowTitle) + String(str_sizeFilterWaitTitle.suffix(7)))].stringValue
                    }
                }
                //: let unreadColor = UIColor.inexpensiveness()
                let unreadColor = UIColor.inexpensiveness()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

                //: } else {
            } else {
                //: let unreadColor = UIColor.inexpensiveness()
                let unreadColor = UIColor.inexpensiveness()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            }
        }
        //: if speColor.keys.count > 0 {
        if speColor.keys.count > 0 {
            //: for (key, value) in speColor {
            for (key, value) in speColor {
                //: detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
                detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
            }
        }
        //: detailAttrString?.addAttribute(.font, value: UIFont.liveAttention(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
        detailAttrString?.addAttribute(.font, value: UIFont.liveAttention(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

        //: return detailAttrString
        return detailAttrString
    }

    /// 处理草稿消息
    //: static func getDraftString(draft: inout String) -> NSMutableAttributedString {
    static func nickname(draft: inout String) -> NSMutableAttributedString {
        //: if draft.contains("draftContent") {
        if draft.contains((str_searchData.replacingOccurrences(of: "view", with: "ft") + "Conten" + String(str_sectionClearTitle))) {
            //: let dict = NSDictionary.init(jsonString: draft)
            let dict = NSDictionary(jsonString: draft)
            //: draft = "\(String(describing: dict["draftContent"]))"
            draft = "\(String(describing: dict[(str_searchData.replacingOccurrences(of: "view", with: "ft") + "Conten" + String(str_sectionClearTitle))]))"
        }
        //: let draftString = NSMutableAttributedString.init(string: "[draft]%@".localizedArguments(draft))
        let draftString = NSMutableAttributedString(string: (String(str_userName)).localizedArguments(draft))
        //: draftString.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: NSRange(location: 0, length: 7))
        draftString.addAttribute(.foregroundColor, value: UIColor.userError(), range: NSRange(location: 0, length: 7))
        //: return draftString
        return draftString
    }
}

// MARK: - Private Event

//: extension TalkingChatListTableCell {
extension StandardMainViewCell {
    /// 更新昵称、头像
    /// - Parameters:
    ///   - userInfo: 数据库信息
    ///   - model: tx数据
    ///   - isOther: 是否其他消息
    //: private func updateUserInfo(userInfo: CoverThen?, model: TalkingConversationModel, isOther: Bool) {
    private func consumerBackgroundOther(userInfo: CoverThen?, model: MediaConversationModel, isOther: Bool) {
        // 在线状态
        //: onlineStatus.isHidden = (model.listOnlineStatus == 0)
        onlineStatus.isHidden = (model.listOnlineStatus == 0)
        //: newImgV.isHidden = !model.isNewUser
        newImgV.isHidden = !model.isNewUser
        // 是否存在用户信息
        //: if userInfo == nil || userInfo?.uid == IconThen.getXiaoMiID() || userInfo?.uid == IconThen.getCustomerServiceID() {
        if userInfo == nil || userInfo?.uid == IconThen.finishBy() || userInfo?.uid == IconThen.motivation() {
            //: nickNameLabel.textColor = .occurrence()
            nickNameLabel.textColor = .occurrence()
            //: nickNameLabel.text = model.showName
            nickNameLabel.text = model.showName
            //: nickNameLabel.snp.updateConstraints { make in
            nickNameLabel.snp.updateConstraints { make in
                //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
                make.trailing.lessThanOrEqualToSuperview().offset(-40)
            }
            //: if model.chatType == .group || model.chatType == .moreMsg {
            if model.chatType == .group || model.chatType == .moreMsg {
                //: headImgView.image = UIImage.nearCounterval(name: model.faceUrl)
                headImgView.image = UIImage.nearCounterval(name: model.faceUrl)
                //: } else {
            } else {
                //: headImgView.setUrlImage(urlStr: model.faceUrl, placeImg: UIImage.placeSquareBigImgBanner())
                headImgView.commentGesture(urlStr: model.faceUrl, placeImg: UIImage.firstMake())
            }
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
            //: imtimateBtn.isHidden = true
            imtimateBtn.isHidden = true
            //: return
            return
        }
        //: guard let userInfo = userInfo else {
        guard let userInfo = userInfo else {
            //: return
            return
        }
        // 昵称
        //: nickNameLabel.textColor = userInfo.loungePlus ? .userVipColor():.occurrence()
        nickNameLabel.textColor = userInfo.loungePlus ? .insert() : .occurrence()
        //: nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname:model.userID
        nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname : model.userID
        // 头像
        //: headImgView.setUrlImage(urlStr: userInfo.headPic, placeImg: UIImage.placeHolderImage(sex: userInfo.sex))
        headImgView.commentGesture(urlStr: userInfo.headPic, placeImg: UIImage.footnoteSex(sex: userInfo.sex))
        // VIP头像框
        //: if isOther == false && !userInfo.headPicFrame.isEmptyString {
        if isOther == false, !userInfo.headPicFrame.isEmptyString {
            //: iconBorder.isHidden = false
            iconBorder.isHidden = false
            //: iconBorder.setHeadFrameUrlImage(urlStr: userInfo.headPicFrame)
            iconBorder.torso(urlStr: userInfo.headPicFrame)
            //: } else {
        } else {
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
        }
//        imtimateBtn.setTitle(model.totalIntimate > 99999 ? "99999+": "\(model.totalIntimate)", for: .normal)
//        imtimateBtn.isHidden = model.totalIntimate <= 0
//        if model.totalIntimate > 0 {
//            let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .liveAttention(type: .Medium, fontSize: 12)], context: nil)
//            imtimateBtn.snp.updateConstraints { make in
//                make.width.equalTo((size?.width ?? 50) + 20)
//            }
//        }
        // 是否其他消息
        //: if isOther == false {
        if isOther == false {
            //: loungeImgV.isHidden = !userInfo.loungePlus
            loungeImgV.isHidden = !userInfo.loungePlus
            //: authImgV.isHidden = !userInfo.tpAuth
            authImgV.isHidden = !userInfo.tpAuth
            //: officialImgV.isHidden = !userInfo.isOfficial
            officialImgV.isHidden = !userInfo.isOfficial
            //: } else {
        } else {
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
        }

        //: nickNameLabel.snp.updateConstraints { make in
        nickNameLabel.snp.updateConstraints { make in
            //: make.trailing.lessThanOrEqualToSuperview().offset( isOther == true ? -40 : -130)
            make.trailing.lessThanOrEqualToSuperview().offset(isOther == true ? -40 : -130)
        }
        //: loungeImgV.snp.updateConstraints { make in
        loungeImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0:4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(loungeImgV.isHidden == true ? 0:20)
            make.width.equalTo(loungeImgV.isHidden == true ? 0 : 20)
        }
        //: authImgV.snp.updateConstraints { make in
        authImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0:4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(authImgV.isHidden == true ? 0:16)
            make.width.equalTo(authImgV.isHidden == true ? 0 : 16)
        }
        //: let text = "New".localized
        let text = (String(str_aErrorName)).localized
        //: let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.behindUpRoom(fontSize: 11)], context: nil)
        let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.behindUpRoom(fontSize: 11)], context: nil)
        //: newImgV.snp.updateConstraints { make in
        newImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0:4)
            make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(newImgV.isHidden == true ? 0:rect.width+10)
            make.width.equalTo(newImgV.isHidden == true ? 0 : rect.width + 10)
        }
        //: officialImgV.snp.remakeConstraints { make in
        officialImgV.snp.remakeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.height.equalTo(15)
            make.height.equalTo(15)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0:4)
            make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0 : 4)
            //: if officialImgV.isHidden {
            if officialImgV.isHidden {
                //: make.width.equalTo(0)
                make.width.equalTo(0)
                //: } else {
            } else {
                //: make.width.greaterThanOrEqualTo(50)
                make.width.greaterThanOrEqualTo(50)
            }
        }
    }

    /// 更新未读数
    /// - Parameter unreadCount: 未读消息数
    //: private func updateUnreadNumber(unreadCount: Int) {
    private func identityDown(unreadCount: Int) {
        //: guard unreadCount > 0 else {
        guard unreadCount > 0 else {
            //: self.badgeLab.isHidden = true
            self.badgeLab.isHidden = true
            //: return
            return
        }

        //: self.badgeLab.isHidden = false
        self.badgeLab.isHidden = false
        //: var unreadCountStr = String(unreadCount)
        var unreadCountStr = String(unreadCount)
        //: if unreadCount > 99 {
        if unreadCount > 99 {
            //: unreadCountStr = String(unreadCount) + "+"
            unreadCountStr = String(unreadCount) + "+"
            //: badgeLab.text = "99+"
            badgeLab.text = "99+"
            //: } else {
        } else {
            //: badgeLab.text = unreadCountStr
            badgeLab.text = unreadCountStr
        }
        //: var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        //: if width < 20 { width = 20 }
        if width < 20 { width = 20 }
        //: badgeLab.snp.updateConstraints { make in
        badgeLab.snp.updateConstraints { make in
            //: make.width.greaterThanOrEqualTo(width)
            make.width.greaterThanOrEqualTo(width)
        }
    }

    /// 处理详情消息
    //: private func handleC2CMessage(model: TalkingConversationModel) {
    private func handleDownEvent(model: MediaConversationModel) {
        //: if model.userStatus == 5 {
        if model.userStatus == 5 {
            //: let str = "Account is restricted！".localized
            let str = (String(str_selectName.suffix(2)) + String(str_colonValue)).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.liveAttention(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.liveAttention(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.inexpensiveness()
                NSAttributedString.Key.foregroundColor: UIColor.inexpensiveness(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            //: return
            return
        }
        //: if model.listShowMessage != nil {
        if model.listShowMessage != nil {
            //: msgShowLabel.attributedText = TalkingChatListTableCell.handleShowC2CDetailTextOfMessage(model: model)
            msgShowLabel.attributedText = StandardMainViewCell.pairForData(model: model)
            //: } else if model.chatType == .group {
        } else if model.chatType == .group { // 公共聊天室入口
            //: let str = "%@ Online".localizedArguments(model.num)
            let str = (String(str_headName) + str_playerContent.replacingOccurrences(of: "item", with: "in")).localizedArguments(model.num)
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.liveAttention(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.liveAttention(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.inexpensiveness()
                NSAttributedString.Key.foregroundColor: UIColor.inexpensiveness(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else if model.chatType == .moreMsg {
        } else if model.chatType == .moreMsg { // 更多消息入口，无数据时，占位文案
            //: let str = "Messages not been replied here".localized
            let str = (String(str_primaryLabelValue) + String(str_backgroundTitle) + String(str_textLeadingData.suffix(6))).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.liveAttention(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.liveAttention(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.inexpensiveness()
                NSAttributedString.Key.foregroundColor: UIColor.inexpensiveness(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else {
        } else { // 草稿箱
            //: var attrStr: NSMutableAttributedString?
            var attrStr: NSMutableAttributedString?
            //: if !model.draftText.isEmptyString {
            if !model.draftText.isEmptyString {
                //: attrStr = TalkingChatListTableCell.getDraftString(draft: &model.draftText)
                attrStr = StandardMainViewCell.nickname(draft: &model.draftText)
            }
            //: attrStr?.addAttribute(.font, value: UIFont.liveAttention(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            attrStr?.addAttribute(.font, value: UIFont.liveAttention(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            //: msgShowLabel.attributedText = attrStr
            msgShowLabel.attributedText = attrStr
        }
    }
}

// MARK: - UI

//: extension TalkingChatListTableCell {
extension StandardMainViewCell {
    //: private func createCellUI() {
    private func beUi() {
        //: contentView.addSubview(headImgView)
        contentView.addSubview(headImgView)
        //: contentView.addSubview(iconBorder)
        contentView.addSubview(iconBorder)
        //: contentView.addSubview(onlineStatus)
        contentView.addSubview(onlineStatus)
        //: contentView.addSubview(badgeLab)
        contentView.addSubview(badgeLab)
        //: contentView.addSubview(nickNameLabel)
        contentView.addSubview(nickNameLabel)
        //: contentView.addSubview(loungeImgV)
        contentView.addSubview(loungeImgV)
        //: contentView.addSubview(authImgV)
        contentView.addSubview(authImgV)
        //: contentView.addSubview(newImgV)
        contentView.addSubview(newImgV)
        //: contentView.addSubview(officialImgV)
        contentView.addSubview(officialImgV)
        //: contentView.addSubview(msgShowLabel)
        contentView.addSubview(msgShowLabel)
        //: contentView.addSubview(timeLab)
        contentView.addSubview(timeLab)
        //: contentView.addSubview(imtimateBtn)
        contentView.addSubview(imtimateBtn)
    }

    //: private func createCellUIConstraints() {
    private func fileEqual() {
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.width.height.equalTo(60)
            make.width.height.equalTo(60)
        }
        //: iconBorder.snp.makeConstraints { make in
        iconBorder.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(69.5)
            make.width.height.equalTo(69.5)
        }
        //: onlineStatus.snp.makeConstraints { make in
        onlineStatus.snp.makeConstraints { make in
            //: make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            //: make.width.height.equalTo(10)
            make.width.height.equalTo(10)
        }
        //: badgeLab.snp.makeConstraints { make in
        badgeLab.snp.makeConstraints { make in
            //: make.top.equalTo(self.headImgView.snp.top).offset(-5)
            make.top.equalTo(self.headImgView.snp.top).offset(-5)
            //: make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
            //: make.width.greaterThanOrEqualTo(20)
            make.width.greaterThanOrEqualTo(20)
        }
        //: nickNameLabel.snp.makeConstraints { make in
        nickNameLabel.snp.makeConstraints { make in
            //: make.top.equalTo(headImgView).offset(6)
            make.top.equalTo(headImgView).offset(6)
            //: make.leading.equalTo(headImgView.snp.trailing).offset(10)
            make.leading.equalTo(headImgView.snp.trailing).offset(10)
            //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
            make.trailing.lessThanOrEqualToSuperview().offset(-40)
            //: make.height.equalTo(21)
            make.height.equalTo(21)
        }
        //: loungeImgV.snp.makeConstraints { make in
        loungeImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            //: make.height.width.equalTo(20)
            make.height.width.equalTo(20)
        }
        //: authImgV.snp.makeConstraints { make in
        authImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            //: make.width.equalTo(16)
            make.width.equalTo(16)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: newImgV.snp.makeConstraints { make in
        newImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(authImgV.snp.trailing).offset(4)
            make.leading.equalTo(authImgV.snp.trailing).offset(4)
            //: make.width.equalTo(26)
            make.width.equalTo(26)
            //: if LanguageManager.shared.direction == .rightToLeft {
            if MeanSunLanguageManager.shared.direction == .rightToLeft {
                //: make.height.equalTo(16)
                make.height.equalTo(16)
                //: } else {
            } else {
                //: make.height.equalTo(14)
                make.height.equalTo(14)
            }
        }
        //: officialImgV.snp.makeConstraints { make in
        officialImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(4)
            make.leading.equalTo(newImgV.snp.trailing).offset(4)
            //: make.width.greaterThanOrEqualTo(50)
            make.width.greaterThanOrEqualTo(50)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: imtimateBtn.snp.makeConstraints { make in
        imtimateBtn.snp.makeConstraints { make in
            //: make.centerY.equalTo(msgShowLabel)
            make.centerY.equalTo(msgShowLabel)
            //: make.width .equalTo(50)
            make.width.equalTo(50)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: msgShowLabel.snp.makeConstraints { make in
        msgShowLabel.snp.makeConstraints { make in
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            //: make.leading.equalTo(nickNameLabel)
            make.leading.equalTo(nickNameLabel)
            //: make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
            make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
        }
    }
}
