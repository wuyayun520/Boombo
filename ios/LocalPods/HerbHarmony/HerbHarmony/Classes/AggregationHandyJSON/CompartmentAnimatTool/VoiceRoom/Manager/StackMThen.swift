
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let str_toName:[UInt8] = [0x57,0x46,0x55,0x53,0x5e,0x58,0x54,0x53,0x46,0x53,0x52,0x54,0x58,0x44,0x4f,0x46,0x49,0x40,0x42,0x58,0x49,0x48,0x53,0x4e,0x41,0x4e,0x44,0x46,0x53,0x4e,0x48,0x49]

private func currentBackground(leading num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "p- :*/
fileprivate let str_selectedTitle:[Character] = ["p","-"]

/*: - :*/
fileprivate let str_dataAddValue:[Character] = ["-"]

/*: "redirectUrl" :*/
fileprivate let str_contextData:String = "redstyle"
fileprivate let str_videoBeyondData:String = "catch for enterectUrl"

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let str_atName:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","进"]
fileprivate let str_wrapData:String = "房模\u{578b}解"

/*: "Party room exception" :*/
fileprivate let str_playerControlContent:String = "Party to of"
fileprivate let str_freeData:String = "text mode all reading excepti"
fileprivate let str_leadingTitle:String = "detail"

/*: _ :*/
fileprivate let str_inputTitle:[Character] = ["_"]

/*: "male" :*/
fileprivate let str_sharedText:[UInt8] = [0xbf,0xb3,0xbe,0xb7]

private func curveUser(interaction num: UInt8) -> UInt8 {
    return num ^ 210
}

/*: "female" :*/
fileprivate let str_hiddenTitle:[Character] = ["f","e","m","a","l","e"]

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let str_afterTheVarName:String = "VcollectionC"
fileprivate let str_playerData:String = " \u{7fa4}聊进房失败。"
fileprivate let str_imageName:String = "return in model namegroupId: "

/*: ." :*/
fileprivate let str_theData:[Character] = ["."]

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let str_cellValue:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","\u{5207}","房"]
fileprivate let str_acrossMakeValue:String = "view"
fileprivate let str_normalTitle:String = "型解析失败。"

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let str_colorToLabelValue:String = "VOICcommon"
fileprivate let str_shareData:String = ": user切房"
fileprivate let str_cellData:String = "失\u{8d25}。gro"
fileprivate let str_dataContent:String = "upId: if the group view"

/*: "errCode" :*/
fileprivate let str_insideData:[UInt8] = [0x65,0x64,0x6f,0x43,0x72,0x72,0x65]

/*: "custom_kickout_message" :*/
fileprivate let str_labelTitle:String = "colorusto"
fileprivate let str_greetName:String = "kosomething"
fileprivate let str_nameData:[Character] = ["_","m","e","s","s","a","g","e"]

/*: "closeRoom" :*/
fileprivate let str_photoLabReturnContent:String = "clothee"
fileprivate let str_fromShareContent:[Character] = ["R","o","o","m"]

/*: "adminCloseRoom" :*/
fileprivate let str_layerName:[Character] = ["a","d","m","i","n","C"]
fileprivate let str_viewCenterName:String = "loseRoomequal draft view title super"

/*: "The room is closed, please go to another room" :*/
fileprivate let str_valueData:[UInt8] = [0x6d,0x6f,0x6f,0x72,0x20,0x72,0x65,0x68,0x74,0x6f,0x6e,0x61,0x20,0x6f,0x74,0x20,0x6f,0x67,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x73,0x6f,0x6c,0x63,0x20,0x73,0x69,0x20,0x6d,0x6f,0x6f,0x72,0x20,0x65,0x68,0x54]

/*: "OK" :*/
fileprivate let str_mineValue:[Character] = ["O","K"]

/*: "You were kicked out of the room" :*/
fileprivate let str_shareTitle:[UInt8] = [0x5a,0x70,0x76,0x21,0x78,0x66,0x73,0x66,0x21,0x6c,0x6a,0x64,0x6c,0x66,0x65,0x21,0x70,0x76,0x75,0x21,0x70,0x67,0x21,0x75,0x69,0x66,0x21,0x73,0x70,0x70,0x6e]

fileprivate func hiddenLayer(size num: UInt8) -> UInt8 {
    let value = Int(num) + 255
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let str_imageTitle:[Character] = ["V","O","I","C","E","R","O","O","M"]
fileprivate let str_tableValue:String = "：\u{8fdb}房失\u{8d25}，"
fileprivate let str_limitValue:String = "roomID: var voice turn"

/*: , error:  :*/
fileprivate let str_viewName:String = "self center, err"
fileprivate let str_positionTitlePageValue:String = "mode let with succeedor: "

/*: , reason:  :*/
fileprivate let str_imageData:String = ", revar data equal self corner"

/*: , extendedData:  :*/
fileprivate let str_playerText:[Character] = [","," ","e","x","t"]
fileprivate let str_infoValue:String = "endedDlayer model"

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let str_fatalName:String = "VmayIC"
fileprivate let str_dataValue:String = ": 当前用\u{6237}\u{63a8}流"
fileprivate let str_lengthText:[Character] = ["失","败","，","r","o","o","m","I","D",":"," "]

/*: , errorCode:  :*/
fileprivate let str_toContent:[Character] = [","," ","e","r","r","o"]
fileprivate let str_giftTitle:[Character] = ["r","C","o","d","e",":"," "]

/*: , streamID:  :*/
fileprivate let str_listText:String = "center count size room, stre"
fileprivate let str_managerName:[Character] = ["a","m","I","D",":"," "]

/*: "An error occurred, please try again" :*/
fileprivate let str_businessToValue:[UInt8] = [0x76,0x59,0x17,0x52,0x45,0x45,0x58,0x45,0x17,0x58,0x54,0x54,0x42,0x45,0x45,0x52,0x53,0x1b,0x17,0x47,0x5b,0x52,0x56,0x44,0x52,0x17,0x43,0x45,0x4e,0x17,0x56,0x50,0x56,0x5e,0x59]

private func lineLayer(main num: UInt8) -> UInt8 {
    return num ^ 55
}

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let str_scaleControlValue:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","拉","流","\u{5931}","\u{8d25}"]
fileprivate let str_centerMaxData:String = "，roocall text image view center"
fileprivate let str_localLocationContent:[Character] = ["m","I","D",":"," "]

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let str_atData:String = "VOImanager"
fileprivate let str_logToText:[Character] = ["R","O","O","M",":"," ","信","\u{4ee4}","解","析"]
fileprivate let str_titleEndValue:String = "\u{5931}败\u{ff0c}com"

/*: , roomID:  :*/
fileprivate let str_leadingLiveMatchTitle:String = ", roodata wrap to label"
fileprivate let str_pushName:String = "effect"

/*: "data" :*/
fileprivate let str_equalName:String = "textata"

/*: "position" :*/
fileprivate let str_fileName:String = "frame"
fileprivate let str_versionData:String = "osmodelion"

/*: "xxxx--  :*/
fileprivate let str_attentionValue:String = "xxxx-- shared time"

/*: "opType" :*/
fileprivate let str_reportName:String = "opTypelet if level in color"

/*: "party:changeMike" :*/
fileprivate let str_titleOfValue:[Character] = ["p","a","r","t","y",":","c","h","a","n","g","e"]
fileprivate let str_countName:[Character] = ["M","i","k","e"]

/*: "type" :*/
fileprivate let str_basicName:[Character] = ["t","y","p","e"]

/*: "toPosition" :*/
fileprivate let str_arrayName:String = "toPositheight user value self"
fileprivate let str_statusValue:[Character] = ["i","o","n"]

/*: "party:adminChangeMike" :*/
fileprivate let str_redModelName:String = "partcall"
fileprivate let str_observerValue:String = "hmng"

/*: "You were kicked off the seat" :*/
fileprivate let str_modelData:String = "attention match call loadYou "
fileprivate let str_countData:String = "icked lab room"
fileprivate let str_eventValue:String = "text interaction return color roomthe seat"

/*: "party:changeCoin" :*/
fileprivate let str_attachData:String = "party:inside manager extension self"
fileprivate let str_popTitle:String = "text"
fileprivate let str_toBeatName:String = "path titleeCoin"

/*: "party:closeStream" :*/
fileprivate let str_collectionPathNoneTitle:[Character] = ["p","a","r","t","y",":","c","l","o","s","e","S"]
fileprivate let str_makeData:[Character] = ["t","r","e","a","m"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StackMThen.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let kLet_clickKeyTitle = NSNotification.Name(rawValue: String(bytes: str_toName.map{currentBackground(leading: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let kLet_domainData = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum ClickConstantTarget: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class StackMThen: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = StateModelType()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_clickKeyTitle, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: StackMThen?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: UserViewController?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: SectionModelType]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = contentPosition()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func estimated() -> StackMThen {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = StackMThen()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func periodicTableDestroy() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if StackMThen._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            StackMThen._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: RoomShouldThen = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = RoomShouldThen.toALesserExtent()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.cellTarget()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension StackMThen {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func colorPosition(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        BindReqTool.greetPath(roomId: partyModel.roomId,
                                          //: uid: Int(WithAdministratorAppManager.share.loginUid) ?? 0,
                                          uid: Int(WithAdministratorAppManager.share.loginUid) ?? 0,
                                          //: type: type,
                                          type: type,
                                          //: position: position,
                                          position: position,
                                          //: toPos: toPosition)
                                          toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.mediumId()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = SectionModelType.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.modelForTo(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.mediumId()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func addPosition(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        BindReqTool.positionAdminLabChangeKindRoomAddCompletion(roomId: partyModel.roomId,
                                                                            //: uid: uid,
                                                                            uid: uid,
                                                                            //: type: type,
                                                                            type: type,
                                                                            //: position: position)
                                                                            position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func mediumId() -> String {
        //: return "p-\(WithAdministratorAppManager.share.loginUid)-\(partyModel.roomId)"
        return "p-\(WithAdministratorAppManager.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension StackMThen {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func makeRequest(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        LaissezPasserThen.withLoad(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.atShow(showMsg: kLet_viewData)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            BindReqTool.telephoneMessageHidden(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: LeadingEqualReactiveCompatible.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            LeadingEqualReactiveCompatible.share.loadWith(urlStr: result[(str_contextData.replacingOccurrences(of: "style", with: "ir") + String(str_videoBeyondData.suffix(6)))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.atShow(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isTalking == false else {
                guard DataSocketDelegate.shared.isTalking == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.atShow(showMsg: kLet_keyTitle)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = StateModelType.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    QueryThen.bringInstance(msg: (String(str_atName) + str_wrapData + "析失败。"))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.atShow(showMsg: (String(str_playerControlContent.prefix(6)) + "room" + String(str_freeData.suffix(8)) + str_leadingTitle.replacingOccurrences(of: "detail", with: "on")).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = UserViewController()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.navigation()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.transfer()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.beforeModel()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(WithAdministratorAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                kLet_voiceText.quantityingTime(eventID: "\(kLet_halfAppData)_\(WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.male.rawValue ? String(bytes: str_sharedText.map{curveUser(interaction: $0)}, encoding: .utf8)! : (String(str_hiddenTitle)))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                SumeractionReactiveCompatible.liveDownCompletion(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        QueryThen.bringInstance(msg: (str_afterTheVarName.replacingOccurrences(of: "collection", with: "OI") + "EROOM:" + str_playerData + String(str_imageName.suffix(9))) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.atShow(showMsg: (String(str_playerControlContent.prefix(6)) + "room" + String(str_freeData.suffix(8)) + str_leadingTitle.replacingOccurrences(of: "detail", with: "on")).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.chemistryLabResource()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func replyNameId(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        BindReqTool.sprechgesangAndCompletion(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.dischargeForPop()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == WithAdministratorAppManager.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == WithAdministratorAppManager.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = StateModelType.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                QueryThen.bringInstance(msg: (String(str_cellValue) + str_acrossMakeValue.replacingOccurrences(of: "view", with: "模") + str_normalTitle.capitalized))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.atShow(showMsg: (String(str_playerControlContent.prefix(6)) + "room" + String(str_freeData.suffix(8)) + str_leadingTitle.replacingOccurrences(of: "detail", with: "on")).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = UserViewController()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.navigation()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.transfer()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.voiceToGreenroom(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(WithAdministratorAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            kLet_voiceText.quantityingTime(eventID: "\(kLet_halfAppData)_\(WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.male.rawValue ? String(bytes: str_sharedText.map{curveUser(interaction: $0)}, encoding: .utf8)! : (String(str_hiddenTitle)))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            SumeractionReactiveCompatible.liveDownCompletion(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    QueryThen.bringInstance(msg: (str_colorToLabelValue.replacingOccurrences(of: "common", with: "E") + "ROOM" + str_shareData.replacingOccurrences(of: "user", with: "群聊") + str_cellData + String(str_dataContent.prefix(6))) + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.atShow(showMsg: (String(str_playerControlContent.prefix(6)) + "room" + String(str_freeData.suffix(8)) + str_leadingTitle.replacingOccurrences(of: "detail", with: "on")).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.chemistryLabResource()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension StackMThen {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func dataQueryed() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.roomAnimated()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func cellTarget() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = actionController() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: ClassTipsProtocol.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func chemistryLabResource() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.dischargeForPop() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.colorPosition(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            StackMThen.periodicTableDestroy() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension StackMThen {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func beforeModel() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = IconThen.getVoiceRoomAppId()
        profile.appID = IconThen.gainUpKey()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: WithAdministratorAppManager.share.loginUid)
        let user = ZegoUser(userID: WithAdministratorAppManager.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func voiceToGreenroom(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension StackMThen: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            BindReqTool.smallCompletion(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.atModel(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.empty()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == ClickConstantTarget.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.colorPosition(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                BindReqTool.smallCompletion(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.atModel(key: key, model: value)
                        //: if String(value.uid) == WithAdministratorAppManager.share.loginUid {
                        if String(value.uid) == WithAdministratorAppManager.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.mediumId()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.empty()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kLet_voiceText.workingPapers(eventID: kLet_viewPathName, parameterStr: [String(bytes: str_insideData.reversed(), encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(str_labelTitle.replacingOccurrences(of: "color", with: "c") + "m_kic" + str_greetName.replacingOccurrences(of: "something", with: "ut") + String(str_nameData))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (str_photoLabReturnContent.replacingOccurrences(of: "the", with: "s") + String(str_fromShareContent)) || value == (String(str_layerName) + String(str_viewCenterName.prefix(8))) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    chemistryLabResource()
                    //: let config = ShowAlertConfig()
                    let config = ManagerAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    VoiceAlertShow.customForTotal(message: String(bytes: str_valueData.reversed(), encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: kLet_replaceData, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    atShow(showMsg: String(bytes: str_shareTitle.map{hiddenLayer(size: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                QueryThen.bringInstance(msg: (String(str_imageTitle) + str_tableValue + String(str_limitValue.prefix(8))) + "\(roomID)" + (String(str_viewName.suffix(5)) + String(str_positionTitlePageValue.suffix(4))) + "\(errorCode)" + (String(str_imageData.prefix(4)) + "ason: ") + "\(reason)" + (String(str_playerText) + String(str_infoValue.prefix(6)) + "ata: ") + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                atShow(showMsg: (String(str_playerControlContent.prefix(6)) + "room" + String(str_freeData.suffix(8)) + str_leadingTitle.replacingOccurrences(of: "detail", with: "on")).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kLet_voiceText.workingPapers(eventID: kLet_viewPathName, parameterStr: [String(bytes: str_insideData.reversed(), encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            chemistryLabResource()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            QueryThen.bringInstance(msg: (str_fatalName.replacingOccurrences(of: "may", with: "O") + "EROOM" + str_dataValue + String(str_lengthText)) + "\(partyModel.roomId)" + (String(str_toContent) + String(str_giftTitle)) + "\(errorCode)" + (String(str_listText.suffix(6)) + String(str_managerName)) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            atShow(showMsg: String(bytes: str_businessToValue.map{lineLayer(main: $0)}, encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = imageCollectionPositions()
            //: for (position, model) in dict where String(model.uid) == WithAdministratorAppManager.share.loginUid {
            for (position, model) in dict where String(model.uid) == WithAdministratorAppManager.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                colorPosition(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                listModel(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                chemistryLabResource()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            QueryThen.bringInstance(msg: (String(str_scaleControlValue) + String(str_centerMaxData.prefix(4)) + String(str_localLocationContent)) + "\(partyModel.roomId)" + (String(str_toContent) + String(str_giftTitle)) + "\(errorCode)" + (String(str_listText.suffix(6)) + String(str_managerName)) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                chemistryLabResource()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            for zego in streamList {
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            for zego in streamList {
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.pointPath(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = imageCollectionPositions()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.pointPath(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func border(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            QueryThen.bringInstance(msg: (str_atData.replacingOccurrences(of: "manager", with: "CE") + String(str_logToText) + str_titleEndValue + "mand: ") + "\(command)" + (String(str_leadingLiveMatchTitle.prefix(5)) + "mID:" + str_pushName.replacingOccurrences(of: "effect", with: " ")) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(str_equalName.replacingOccurrences(of: "text", with: "d"))][(str_fileName.replacingOccurrences(of: "frame", with: "p") + str_versionData.replacingOccurrences(of: "model", with: "it"))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < kLet_domainData else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = SectionModelType.deserialize(from: json[(str_equalName.replacingOccurrences(of: "text", with: "d"))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(str_attentionValue.prefix(7))) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(str_reportName.prefix(6)))].stringValue == (String(str_titleOfValue) + String(str_countName)) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(str_equalName.replacingOccurrences(of: "text", with: "d"))][(String(str_basicName))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                modelForTo(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = accumulationKey(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    listModel(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                listModel(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(str_equalName.replacingOccurrences(of: "text", with: "d"))][(String(str_arrayName.prefix(7)) + String(str_statusValue))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                modelForTo(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                modelForTo(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.controlSystem()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(str_reportName.prefix(6)))].stringValue == (str_redModelName.replacingOccurrences(of: "call", with: "y") + ":adminC" + str_observerValue.replacingOccurrences(of: "m", with: "a") + "eMike") { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(str_equalName.replacingOccurrences(of: "text", with: "d"))][(String(str_basicName))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                modelForTo(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = accumulationKey(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    listModel(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.controlSystem()
                    //: guard String(oldModel.uid) == WithAdministratorAppManager.share.loginUid else { return }
                    guard String(oldModel.uid) == WithAdministratorAppManager.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    atShow(showMsg: (String(str_modelData.suffix(4)) + "were k" + String(str_countData.prefix(6)) + "off " + String(str_eventValue.suffix(8))).localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                modelForTo(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == WithAdministratorAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == WithAdministratorAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                modelForTo(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == WithAdministratorAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == WithAdministratorAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(str_reportName.prefix(6)))].stringValue == (String(str_attachData.prefix(6)) + "chan" + str_popTitle.replacingOccurrences(of: "text", with: "g") + String(str_toBeatName.suffix(5))) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            for (position, model) in StackMThen.estimated().imageCollectionPositions() {
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    modelForTo(position: position, model: model)
                    //: return
                    continue
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(str_reportName.prefix(6)))].stringValue == (String(str_collectionPathNoneTitle) + String(str_makeData)) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension StackMThen {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func modelForTo(position: Int, model: SectionModelType) {
        //: modifyMikePosition(key: position, model: model)
        atModel(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.afterEnd(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        countAdd(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func listModel(position: Int, model: SectionModelType) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        atModel(key: position, model: SectionModelType())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.afterEnd(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        countAdd(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func countAdd(uid: Int) {
        //: guard String(uid) == WithAdministratorAppManager.share.loginUid else { return }
        guard String(uid) == WithAdministratorAppManager.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = imageCollectionPositions()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.rectangle()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension StackMThen {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func contentPosition() -> [Int: SectionModelType] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< kLet_domainData {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            atModel(key: index, model: SectionModelType())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func atModel(key: Int, model: SectionModelType) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func accumulationKey(key: Int) -> SectionModelType? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func imageCollectionPositions() -> [Int: SectionModelType] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
