
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_centerData:[UInt8] = [0x57,0x5c,0x57,0x62,0x16,0x51,0x5d,0x52,0x53,0x60,0x28,0x17,0xe,0x56,0x4f,0x61,0xe,0x5c,0x5d,0x62,0xe,0x50,0x53,0x53,0x5c,0xe,0x57,0x5b,0x5e,0x5a,0x53,0x5b,0x53,0x5c,0x62,0x53,0x52]

fileprivate func messageColor(image num: UInt8) -> UInt8 {
    let value = Int(num) + 18
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Followers" :*/
fileprivate let str_iconPathName:[Character] = ["F","o","l"]
fileprivate let str_timeData:String = "loweby"

/*: "Following" :*/
fileprivate let str_actionData:String = "Followingone background"

/*: "#E6E6E6" :*/
fileprivate let str_videoContent:String = "#E6E6E6me let view return"

/*: "btn_message_more" :*/
fileprivate let str_ofText:[Character] = ["b"]
fileprivate let str_pageValue:String = "return direction frame maketn_m"
fileprivate let str_labelData:String = "shared aspectge_more"

/*: "UID:  :*/
fileprivate let str_viewData:String = "UID: color type select"

/*: "level_ :*/
fileprivate let str_cellValue:[Character] = ["l","e","v","e","l","_"]

/*: "Gift" :*/
fileprivate let str_coordinateTitle:[UInt8] = [0xda,0xf4,0xfb,0xe9]

private func makeTitle(make num: UInt8) -> UInt8 {
    return num ^ 157
}

/*: "Mike" :*/
fileprivate let str_dismissTitle:[UInt8] = [0xfb,0x17,0x19,0x13]

fileprivate func inRequest(on num: UInt8) -> UInt8 {
    let value = Int(num) - 174
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Follow" :*/
fileprivate let str_finishValue:[UInt8] = [0x11,0x38,0x3b,0x3b,0x38,0x20]

private func showTo(quote num: UInt8) -> UInt8 {
    return num ^ 87
}

/*: "gift_voice_card" :*/
fileprivate let str_hiddenDismissValue:String = "gift_make model right frame"
fileprivate let str_equalDateTitle:String = "let tool view_card"

/*: "btn_party_voice_nor" :*/
fileprivate let str_hiddenName:String = "btn_gift safe"
fileprivate let str_succeedFatalName:[Character] = ["i","c"]
fileprivate let str_headContent:String = "shared coste_nor"

/*: "btn_party_voice_pre" :*/
fileprivate let str_indexName:String = "push max index typebtn_"
fileprivate let str_screenData:String = "ce_pretext app self"

/*: "attentionUid" :*/
fileprivate let str_startData:[Character] = ["a","t","t","e","n","t","i","o"]
fileprivate let str_timeName:[Character] = ["n","U","i","d"]

/*: "source" :*/
fileprivate let str_lineValue:String = "style"
fileprivate let str_tableName:[Character] = ["o","u","r","c","e"]

/*: "Kick out" :*/
fileprivate let str_lengthTitle:String = "Kick outinstance return model room"

/*: "Unmute" :*/
fileprivate let str_headName:[Character] = ["U"]
fileprivate let str_bubbleData:String = "nmutsocial"

/*: "Mute" :*/
fileprivate let str_listenModelData:[Character] = ["M","u","t","e"]

/*: "Kick off" :*/
fileprivate let str_dataValue:String = "hide make let view elseKick "
fileprivate let str_stateText:String = "ofemale"

/*: "Block" :*/
fileprivate let str_sectionTitle:[Character] = ["B","l","o","c","k"]

/*: "Report" :*/
fileprivate let str_blockName:String = "Reportline info center at"

/*: "#F6F6F6" :*/
fileprivate let str_modelText:String = "label model tip to#F6F6F6"

/*: "Confirm to kick out the room?" :*/
fileprivate let str_titleValue:[Character] = ["C","o","n","f","i","r","m"," ","t","o"," ","k","i"]
fileprivate let str_menuViewData:[Character] = ["c","k"," ","o","u","t"," ","t","h","e"," ","r","o","o","m","?"]

/*: "Cancel" :*/
fileprivate let str_equalData:[Character] = ["C","a","n","c","e","l"]

/*: "Sure" :*/
fileprivate let str_viewEventButtonValue:String = "user from coinSure"

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let str_borderText:[UInt8] = [0x8f,0xbc,0xab,0xee,0xb7,0xa1,0xbb,0xee,0xbd,0xbb,0xbc,0xab,0xee,0xb7,0xa1,0xbb,0xee,0xb9,0xaf,0xa0,0xba,0xee,0xba,0xa1,0xee,0xad,0xaf,0xa0,0xad,0xab,0xa2,0xee,0xba,0xa6,0xab,0xee,0xa3,0xbb,0xba,0xab,0xf1]

private func toTitle(player num: UInt8) -> UInt8 {
    return num ^ 206
}

/*: "Confirm" :*/
fileprivate let str_miniName:[Character] = ["C"]
fileprivate let str_colorText:[Character] = ["o","n","f","i","r","m"]

/*: "The mute has been lifted~" :*/
fileprivate let str_popName:String = "The mvar send with to m"
fileprivate let str_cellName:[Character] = ["b","e","e","n"," ","l","i"]
fileprivate let str_toName:[Character] = ["f","t","e","d","~"]

/*: "Confirm to kick off the seat?" :*/
fileprivate let str_mName:[Character] = ["C","o","n","f","i","r","m"]
fileprivate let str_makeValue:[Character] = [" ","t","o"]
fileprivate let str_removeContent:String = "let return kick"
fileprivate let str_editLayerFirstText:String = " the equal color"

/*: "OK" :*/
fileprivate let str_modelActionValue:String = "custom"

/*: "Shielding Success" :*/
fileprivate let str_toTitle:String = "Shieadd if let left"
fileprivate let str_modelValue:[Character] = ["u","c","c","e","s","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TalkingMainStraddleThen.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol ConditionViewDelegate: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func everyNickname(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func modelUser(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func workflow(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func withCount(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class TalkingMainStraddleThen: UIView {
    //: var popView: TalkingPopView?
    var popView: TalkingRecognizerDelegate?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: TentPriorityTarget?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(StackMThen.estimated().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = MModelType()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: ConditionViewDelegate?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == WithAdministratorAppManager.share.loginUserMode.userID {
        if uid == WithAdministratorAppManager.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        setupSubviewsBind()
        //: setupSubViewsConstraint()
        notTo()
        //: reqUserCardInfo()
        wearerAccept()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_centerData.map{messageColor(image: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .liveAttention(type: .Regular, fontSize: 12)
        label.font = .liveAttention(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.inexpensiveness()
        label.textColor = UIColor.inexpensiveness()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(deform), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .liveAttention(type: .Semibold, fontSize: 15)
        label.font = .liveAttention(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.occurrence()
        label.textColor = UIColor.occurrence()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .liveAttention(type: .Regular, fontSize: 13)
        label.font = .liveAttention(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.inexpensiveness()
        label.textColor = UIColor.inexpensiveness()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .inexpensiveness()
        lab.textColor = .inexpensiveness()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(str_iconPathName) + str_timeData.replacingOccurrences(of: "by", with: "rs")).localized
        //: lab.font = UIFont.liveAttention(type: .Semibold, fontSize: 12)
        lab.font = UIFont.liveAttention(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .occurrence()
        lab.textColor = .occurrence()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.liveAttention(type: .Semibold, fontSize: 15)
        lab.font = UIFont.liveAttention(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .inexpensiveness()
        lab.textColor = .inexpensiveness()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(str_actionData.prefix(9))).localized
        //: lab.font = UIFont.liveAttention(type: .Semibold, fontSize: 12)
        lab.font = UIFont.liveAttention(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .occurrence()
        lab.textColor = .occurrence()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.liveAttention(type: .Semibold, fontSize: 15)
        lab.font = UIFont.liveAttention(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(str_videoContent.prefix(7))))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.nearCounterval(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.nearCounterval(name: (String(str_ofText) + String(str_pageValue.suffix(4)) + "essa" + String(str_labelData.suffix(7)))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(getRollingOf), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: FromThen = {
        //: let v = TalkingLiveRoomMuteView()
        let v = FromThen()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension TalkingMainStraddleThen {
    //: func reqUserCardInfo() {
    func wearerAccept() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        SectionPushListener.showCompletion(uid: self.uid, streamerUid: liveUserId, roomId: StackMThen.estimated().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<MModelType>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.naturalAccumulation()
            }
        }
    }

    //: func setUserCardData() {
    func naturalAccumulation() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(str_viewData.prefix(5))) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.howeverFinish(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.nearCounterval(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.nearCounterval(name: (String(str_cellValue)) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            priceViewSex(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func priceViewSex(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: str_coordinateTitle.map{makeTitle(make: $0)}, encoding: .utf8)!)
            //: if liveUserId == WithAdministratorAppManager.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == WithAdministratorAppManager.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: str_dismissTitle.map{inRequest(on: $0)}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: str_finishValue.map{showTo(quote: $0)}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.alright(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.alright(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: str_coordinateTitle.map{makeTitle(make: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.nearCounterval(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.nearCounterval(name: (String(str_hiddenDismissValue.prefix(5)) + "voice" + String(str_equalDateTitle.suffix(5)))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: str_dismissTitle.map{inRequest(on: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.nearCounterval(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.nearCounterval(name: (String(str_hiddenName.prefix(4)) + "party_vo" + String(str_succeedFatalName) + String(str_headContent.suffix(5)))), for: .normal)
                //: btn.setImage(UIImage.nearCounterval(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.nearCounterval(name: (String(str_indexName.suffix(4)) + "party_voi" + String(str_screenData.prefix(6)))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.behindUpRoom(fontSize: 15)
                btn.titleLabel?.font = UIFont.behindUpRoom(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(senderTouchItem(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(str_videoContent.prefix(7))))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func senderTouchItem(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: str_coordinateTitle.map{makeTitle(make: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.modelUser(uid: self.userModel.uid)
            //: self.dismiss()
            self.bignessAdd()
        //: case "Mike":
        case String(bytes: str_dismissTitle.map{inRequest(on: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.workflow(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.bignessAdd()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.everyNickname(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.bignessAdd()
        //: case "Follow":
        case String(bytes: str_finishValue.map{showTo(quote: $0)}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            premium(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func premium(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        queryCompletion(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.withCount(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func queryCompletion(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(String(str_startData) + String(str_timeName))] = uid
        //: dict["source"] = "2"
        dict[(str_lineValue.replacingOccurrences(of: "style", with: "s") + String(str_tableName))] = "2"
        //: BridgeReactiveCompatible.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        BridgeReactiveCompatible.namePlayer(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func deform() {
        //: LeadingEqualReactiveCompatible.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        LeadingEqualReactiveCompatible.share.bindDownFrom(uid: self.userModel.uid)
        //: self.dismiss()
        self.bignessAdd()
    }
}

//: extension TalkingVoiceRoomCardView {
extension TalkingMainStraddleThen {
    //: @objc func moreBtnClick() {
    @objc func getRollingOf() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == WithAdministratorAppManager.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == WithAdministratorAppManager.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(str_lengthTitle.prefix(8))).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(str_headName) + str_bubbleData.replacingOccurrences(of: "social", with: "e")).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(str_listenModelData)).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(str_dataValue.suffix(5)) + str_stateText.replacingOccurrences(of: "female", with: "ff")).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(str_sectionTitle)).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(str_blockName.prefix(6))).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.ConfigManager.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = BringHomeDataSource.stopAdd(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(str_modelText.suffix(7))))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func counterval() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = TalkingRecognizerDelegate(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: kLet_senseContent, height: kLet_environmentClickValue)
        //: popView?.initWithView(view: self)
        popView?.bluntEnd(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? IconThen.getWindow())
        popView?.inwards(view: self.actionController()?.view ?? IconThen.getBind())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func bignessAdd() {
        //: popView?.dismissView()
        popView?.viewCover()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension TalkingMainStraddleThen: IconChangeViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func postPush(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(str_lengthTitle.prefix(8))).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            VoiceAlertShow.enablelineMedium(title: nil, message: (String(str_titleValue) + String(str_menuViewData)).localized, leftBtnTitle: (String(str_equalData)).localized, rightBtnTitle: (String(str_viewEventButtonValue.suffix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                StandardProgressHUD.indexRoom()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                BindReqTool.equalCompletion(targetUid: self.userModel.uid, roomId: StackMThen.estimated().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    StandardProgressHUD.giftTo()
                    //: self.dismiss()
                    self.bignessAdd()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(str_headName) + str_bubbleData.replacingOccurrences(of: "social", with: "e")).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            VoiceAlertShow.enablelineMedium(title: nil, message: String(bytes: str_borderText.map{toTitle(player: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(str_equalData)).localized, rightBtnTitle: (String(str_miniName) + String(str_colorText)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                StandardProgressHUD.indexRoom()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                BindReqTool.groupImage(targetUid: self.userModel.uid, roomID: StackMThen.estimated().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    StandardProgressHUD.giftTo()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.successMsg(showMsg: (String(str_popName.prefix(5)) + "ute has " + String(str_cellName) + String(str_toName)).localized)
                    }
                    //: self.dismiss()
                    self.bignessAdd()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(str_listenModelData)).localized: // 设置禁言
            //: muteView.showView()
            muteView.asQueryedView()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.bignessAdd()

        //: case "Kick off".localized: /// 踢下麦
        case (String(str_dataValue.suffix(5)) + str_stateText.replacingOccurrences(of: "female", with: "ff")).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            VoiceAlertShow.enablelineMedium(title: nil, message: (String(str_mName) + String(str_makeValue) + String(str_removeContent.suffix(5)) + " off" + String(str_editLayerFirstText.prefix(5)) + "seat?").localized, leftBtnTitle: (String(str_equalData)).localized, rightBtnTitle: (String(str_viewEventButtonValue.suffix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                StackMThen.estimated().addPosition(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.bignessAdd()
            }

        //: case "Report".localized:
        case (String(str_blockName.prefix(6))).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = SectionThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.format(view: nil)
        //: case "Block".localized:
        case (String(str_sectionTitle)).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            VoiceAlertShow.enablelineMedium(title: nil, message: kLet_pathText, leftBtnTitle: (String(str_equalData)).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                VoiceAlertShow.bias()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                VoiceAlertShow.bias()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                UnambiguousThen.custom(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.successMsg(showMsg: (String(str_toTitle.prefix(4)) + "lding S" + String(str_modelValue)).localized)
                        //: self.dismiss()
                        self.bignessAdd()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension TalkingMainStraddleThen {
    // 添加视图
    //: private func setupSubviews() {
    private func setupSubviewsBind() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func notTo() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(kLet_senseContent / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-kLet_senseContent / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(kLet_senseContent / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.actionController()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.actionController()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.bignessAdd()
        }
    }
}
