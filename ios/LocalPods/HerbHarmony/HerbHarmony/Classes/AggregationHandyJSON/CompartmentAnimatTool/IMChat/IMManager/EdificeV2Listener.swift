
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let str_addUserTitle:[Character] = ["I","M","\u{9000}","\u{51fa}","登"]
fileprivate let str_amName:[Character] = ["\u{5f55}","\u{5931}","败"]
fileprivate let str_senseContent:[Character] = ["，","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let str_upBottomIfData:String = "show list, desc:"

/*: ." :*/
fileprivate let str_commentContent:String = "."

/*: "IM登录失败，code: :*/
fileprivate let str_pathText:String = "for message typeIM登录失败，"
fileprivate let str_byValue:String = "attention gift equal player tempcode:"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let str_equalValueContent:String = "IM\u{83b7}取未\u{8bfb}\u{6d88}"
fileprivate let str_makeValue:[Character] = ["息","总","数"]
fileprivate let str_endText:String = "\u{5931}败\u{ff0c}code:"

/*: "crushBadge" :*/
fileprivate let str_giftData:String = "crushBto window height size name"
fileprivate let str_labName:String = "amine"

/*: "badNumber" :*/
fileprivate let str_viewOffTitle:[UInt8] = [0x40,0x43,0x46,0x6c,0x57,0x4f,0x40,0x47,0x50]

private func actualDataMake(add num: UInt8) -> UInt8 {
    return num ^ 34
}

/*: "plaza" :*/
fileprivate let str_viewValue:String = "PLAZA"

/*: "roomNum" :*/
fileprivate let str_currentValue:String = "request need near text firstroomNu"
fileprivate let str_explainMeTitle:[Character] = ["m"]

/*: "num" :*/
fileprivate let str_toText:String = "NUM"

/*: "onlyOneRoomId" :*/
fileprivate let str_contentMessageValue:[Character] = ["o","n","l","y","O","n","e","R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let str_roomTitle:String = "live"

/*: "PARTY-CHAT" :*/
fileprivate let str_indexValue:[Character] = ["P","A","R","T","Y","-","C","H","A","T"]

/*: "extra" :*/
fileprivate let str_minLabelAcceptValue:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let str_makeName:[Character] = ["m","s","g","T","y"]
fileprivate let str_imageName:[Character] = ["p","e"]

/*: "newPkgGift" :*/
fileprivate let str_tempName:String = "newPkfile add else let if"

/*: "MF:ChatMentionMsg" :*/
fileprivate let str_modeTitle:String = "menu resume blackMF:C"
fileprivate let str_observerValue:String = "onMsgmodel image view mask"

/*: "uid" :*/
fileprivate let str_cellData:[UInt8] = [0x68,0x5c,0x57]

fileprivate func hormoneKit(row num: UInt8) -> UInt8 {
    let value = Int(num) - 243
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let str_createData:[UInt8] = [0x7,0x6,0x4,0x1,0x6,0xd,0x3b,0x1c,0x9,0x1c,0x1d,0x1b]

private func valueAction(accept num: UInt8) -> UInt8 {
    return num ^ 104
}

/*: "isNewUser" :*/
fileprivate let str_styleResultText:[UInt8] = [0x72,0x65,0x73,0x55,0x77,0x65,0x4e,0x73,0x69]

/*: "c2c_ :*/
fileprivate let str_userValue:String = "result varc2c_"

/*: "IM获取会话失败，convId: :*/
fileprivate let str_observerBeginVideoValue:String = "IM获取var type status let app"
fileprivate let str_backgroundWithTitle:String = "manager active appconvId:"

/*: , code: :*/
fileprivate let str_appValue:String = "add normal error left let, code:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let str_ofData:[Character] = ["I","M","插","入","消","息","失","\u{8d25}","，"]
fileprivate let str_nameData:[Character] = ["t","a","r","g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let str_locationName:String = "chaclose"
fileprivate let str_aboutName:String = "rectitextn"

/*: "rYMsgType" :*/
fileprivate let str_headColorValue:[Character] = ["r","Y","M","s"]
fileprivate let str_errorTitle:String = "if frame false var colorgType"

/*: "GJ:CallCustom" :*/
fileprivate let str_topContent:[Character] = ["G","J",":","C","a","l"]
fileprivate let str_nameMakeData:String = "lCustomreturn case model"

/*: "IM获取会话列表失败，code: :*/
fileprivate let str_tempData:String = "IM\u{83b7}取"
fileprivate let str_collectionValue:String = "play make voice败，code:"

/*: "msgInfo" :*/
fileprivate let str_interactionValue:[Character] = ["m","s","g","I","n","f","o"]

/*: "needFold" :*/
fileprivate let str_stopValue:String = "main selfneedF"
fileprivate let str_replyData:[Character] = ["o","l","d"]

/*: "group_ :*/
fileprivate let str_labValue:[Character] = ["g","r","o","u","p"]
fileprivate let str_allData:String = "model"

/*: "IM删除会话失败，convID: :*/
fileprivate let str_equalData:String = "IM删除会话sense let label photo"
fileprivate let str_userData:String = "onvID:make user at"

/*: "IM置顶会话失败，convId: :*/
fileprivate let str_colorValue:String = "IM\u{7f6e}顶"
fileprivate let str_messageText:[Character] = ["o","n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let str_modelText:[Character] = ["S","D","K"," ","正","在","连","\u{63a5}","到","\u{670d}","务"]
fileprivate let str_infoMainData:[Character] = ["器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let str_startName:String = "frame add actual type forSDK "
fileprivate let str_listTitle:[Character] = ["连","接","到","服","务","器"]

/*: "IM连接服务器失败，code: :*/
fileprivate let str_pathData:[Character] = ["I","M","连","\u{63a5}","服","\u{52a1}","\u{5668}","失","败","，","c","o","d","e",":"]

/*: , error: :*/
fileprivate let str_dataErrorValue:[Character] = [","," ","e","r","r","o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let str_playerName:String = "IM\u{8d26}号\u{88ab}挤\u{4e0b}\u{7ebf}"
fileprivate let str_toName:[Character] = ["。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let str_colorTitle:[UInt8] = [0x20,0x16,0xc,0xb,0x59,0x18,0x1a,0x1a,0x16,0xc,0x17,0xd,0x59,0x10,0xa,0x59,0x15,0x16,0x1e,0x1e,0x1c,0x1d,0x59,0x10,0x17,0x59,0x16,0x17,0x59,0x18,0x17,0x16,0xd,0x11,0x1c,0xb,0x59,0x1d,0x1c,0xf,0x10,0x1a,0x1c,0x59,0x18,0x17,0x1d,0x59,0x0,0x16,0xc,0x59,0x18,0xb,0x1c,0x59,0x1f,0x16,0xb,0x1a,0x1c,0x1d,0x59,0xd,0x16,0x59,0x1e,0x16,0x59,0x16,0x1f,0x1f,0x15,0x10,0x17,0x1c,0x58,0x59,0x30,0x1f,0x59,0x10,0xd,0x59,0x10,0xa,0x59,0x17,0x16,0xd,0x59,0x1d,0x16,0x17,0x1c,0x59,0x1b,0x0,0x59,0x14,0x1c,0x55,0x59,0xd,0x11,0x1c,0x59,0x9,0x18,0xa,0xa,0xe,0x16,0xb,0x1d,0x59,0x14,0x18,0x0,0x59,0x11,0x18,0xf,0x1c,0x59,0x1b,0x1c,0x1c,0x17,0x59,0x15,0x1c,0x18,0x12,0x1c,0x1d,0x55,0x59,0x18,0x17,0x1d,0x59,0x10,0xd,0x59,0x10,0xa,0x59,0xb,0x1c,0x1a,0x16,0x14,0x14,0x1c,0x17,0x1d,0x1c,0x1d,0x59,0xd,0x16,0x59,0x1a,0x11,0x18,0x17,0x1e,0x1c,0x59,0xd,0x11,0x1c,0x59,0x9,0x18,0xa,0xa,0xe,0x16,0xb,0x1d,0x57]

/*: "Offline Notification" :*/
fileprivate let str_responseValue:String = "table height else the infoOfflin"
fileprivate let str_toTitle:[Character] = ["e"," ","N"]
fileprivate let str_infoText:String = "oticontenti"

/*: "Cancel" :*/
fileprivate let str_rowPlayValue:String = "show"
fileprivate let str_winValue:String = "ancemodel"

/*: "Re Login" :*/
fileprivate let str_topTitle:String = "from self mediumRe Login"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EdificeV2Listener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol ToObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func plan(data: [MediaConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func errorNumber(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func messageDismiss(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum TotalDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class EdificeV2Listener: NSObject {
@objcMembers public class EdificeV2Listener: NSObject {
    //: public static let shared = EdificeV2Listener()
    public static let shared = EdificeV2Listener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [MediaConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [MediaConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [MediaConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [MediaConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [MediaConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: MediaConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.capacity()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func byInit() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(kLet_reasonName, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func capacity() {
        //: func__removeAllDelegate()
        stage()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.adddofficialShow()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func adddofficialShow() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = MediaConversationModel.explain(chatType: .system)
//        let serviceModel = MediaConversationModel.getPlaceholderModel(chatType: .service)
        //: self.officialList = [systemModel/|*, serviceModel*|/]
        self.officialList = [systemModel /* , serviceModel */ ]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func completeList() -> [MediaConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if WithAdministratorAppManager.share.appStatus == AppSkinStatus.special.rawValue {
        if WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == IconThen.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == IconThen.motivation() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension EdificeV2Listener {
extension EdificeV2Listener {
    //: func func__removeAllDelegate() {
    func stage() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func frameDelegate(_ delegate: ToObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func cellIn(_ delegate: ToObjectProtocol) {
        //: self.func__invokeDelegate {
        self.forepart {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func forepart(_ invocation: (ToObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! ToObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension EdificeV2Listener {
extension EdificeV2Listener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func upQuit() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            QueryThen.bringInstance(msg: (String(str_addUserTitle) + String(str_amName) + String(str_senseContent)) + "\(code)" + (String(str_upBottomIfData.suffix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.capacity()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func viewCompletion(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = WithAdministratorAppManager.share.appUserConfigMode.uSig
        let uSigStr = WithAdministratorAppManager.share.appUserConfigMode.uSig
        //: let uidStr = WithAdministratorAppManager.share.loginUserMode.userID
        let uidStr = WithAdministratorAppManager.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.numberUp()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            QueryThen.bringInstance(msg: (String(str_pathText.suffix(7)) + String(str_byValue.suffix(5))) + "\(code)" + (String(str_upBottomIfData.suffix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func operate() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || WithAdministratorAppManager.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || WithAdministratorAppManager.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.atShow(showMsg: kLet_detailMessageValue)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension EdificeV2Listener {
extension EdificeV2Listener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func numberUp() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            QueryThen.bringInstance(msg: (str_equalValueContent + String(str_makeValue) + str_endText) + "\(code)" + (String(str_upBottomIfData.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func beyondContext() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: BridgeReactiveCompatible.req_sysMessageList { _, result in
        BridgeReactiveCompatible.bottomContextCompletion { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(str_giftData.prefix(6)) + str_labName.replacingOccurrences(of: "min", with: "dg"))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: kLet_deviceLimitData, object: self, userInfo: [String(bytes: str_viewOffTitle.map{actualDataMake(add: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func everyAdd(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: BridgeReactiveCompatible.req_getChatRoomInfo { succeed, result, _ in
        BridgeReactiveCompatible.adopt { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(str_viewValue.lowercased())] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(str_currentValue.suffix(6)) + String(str_explainMeTitle))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = MediaConversationModel.explain(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(str_toText.lowercased())] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(str_contentMessageValue))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension EdificeV2Listener: V2TIMAdvancedMsgListener {
extension EdificeV2Listener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((str_roomTitle.uppercased() + "-CHAT")) || msg.groupID.contains((String(str_indexValue)))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            SumeractionReactiveCompatible.playerFor().showMsg(msg: msg)
            //: return
            return
        }
        //: if msg.sender == WithAdministratorAppManager.share.loginUserMode.userID { return }
        if msg.sender == WithAdministratorAppManager.share.loginUserMode.userID { return }

        //: let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(str_minLabelAcceptValue))] as! [AnyHashable: Any]
        //: let msgModel = DataNameModel(dic: extraDic)
        let msgModel = DataNameModel(dic: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: kLet_barCornerClickData, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(str_makeName) + String(str_imageName))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(str_tempName.prefix(5)) + "gGift") { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_sizeSaltTitle, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if TUIMsgChatManager.checkVoiceMessage(message: msg) {
            if TUIMsgChatManager.checkVoiceMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                //: MarkThen.db_inserVoiceMsgToDB(dic)
                MarkThen.inserSmall(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.closeDic(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            CompartmentAnimatTool.shared.shouldLoad(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel.msgInfo.messageType == .MessageTypeGift {
        if msgModel.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            CompartmentAnimatTool.shared.dropTo(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(str_modeTitle.suffix(4)) + "hatMenti" + String(str_observerValue.prefix(5))) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            StandardClearReactiveCompatible.share.received(msg: msg, extraInfo: extraDic)
        }

        //: if !msgModel.gift.unlockMsgId.isEmptyString {
        if !msgModel.gift.unlockMsgId.isEmptyString { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: HandleThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel.gift.unlockMsgId)
            array.append(msgModel.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: HandleThen.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != IconThen.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != IconThen.finishBy() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.forepart { $0.messageDismiss?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            MessageReactiveCompatible.shared.straitAndNarrow(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: str_cellData.map{hormoneKit(row: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: str_createData.map{valueAction(accept: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel.msgInfo.isNewUser] as [String: Any]
                                String(bytes: str_styleResultText.reversed(), encoding: .utf8)!: msgModel.msgInfo.isNewUser] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: kLet_likeValue,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: ChiefThen.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            ChiefThen.teenyWeeny(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            HandleThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: kLet_dataName, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
    @objc public func pathWith(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
        //: let imMsg = msgCellData.innerMessage
        let imMsg = msgCellData.innerMessage

        //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        self.bullSEye(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(str_userValue.suffix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.skip(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.tableDoing(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                QueryThen.bringInstance(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func bullSEye(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = WithAdministratorAppManager.share.loginUserMode.userID
        let senderStr = WithAdministratorAppManager.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                QueryThen.bringInstance(msg: (String(str_ofData) + String(str_nameData)) + "\(targetId)" + (String(str_appValue.suffix(7))) + "\(code)" + (String(str_upBottomIfData.suffix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func closeDic(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(str_locationName.replacingOccurrences(of: "close", with: "n") + "geDi" + str_aboutName.replacingOccurrences(of: "text", with: "o"))].boolValue
        if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && LeadingEqualReactiveCompatible.share.getEqualVc()!.isKind(of: ClassTipsProtocol.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: kLet_limitData) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: kLet_limitData)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension EdificeV2Listener: V2TIMConversationListener {
extension EdificeV2Listener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.skip(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.tableDoing(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.skip(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.tableDoing(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: WithAdministratorAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        WithAdministratorAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.forepart {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.errorNumber?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func tableDoing(conVersationList: [MediaConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if ChiefThen.shared().needReqUidsSet != nil {
            if ChiefThen.behindError().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: ChiefThen.shared().needReqUidsSet!.formUnion(set)
                ChiefThen.behindError().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.forepart {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.plan?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func control(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [MediaConversationModel] = []

        // V2TIMConversation->MediaConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = MediaConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.allViewModel(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [MediaConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = FromInfoTable.addSize(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.aboveMedium(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension EdificeV2Listener {
extension EdificeV2Listener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func roll(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.control(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.tableDoing(conVersationList: arr as? [MediaConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            QueryThen.bringInstance(msg: (str_tempData + "会话列\u{8868}失" + String(str_collectionValue.suffix(7))) + "\(code)" + (String(str_upBottomIfData.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func pushPossibility(targetId: String) -> MediaConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func skip(convList: [V2TIMConversation]) -> [MediaConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [MediaConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = MediaConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.allViewModel(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.aboveMedium(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func aboveMedium(convModel: MediaConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.adddofficialShow()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: TotalDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - WithAdministratorAppManager.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - WithAdministratorAppManager.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = ChiefThen.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = ChiefThen.cached(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(str_minLabelAcceptValue))][(String(str_interactionValue))][(String(str_stopValue.suffix(5)) + String(str_replyData))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.vanguardType(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.vanguardType(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func vanguardType(convList: [MediaConversationModel], dataType: TotalDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func tableTalk(targetId: String, dataType: TotalDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(str_userValue.suffix(4))) + "\(targetId)" : (String(str_labValue) + str_allData.replacingOccurrences(of: "model", with: "_")) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: ChiefThen.db_removeRecord(with: targetId)
            ChiefThen.imageWith(with: targetId)
            //: MarkThen.db_deleteVoiceMsg(WithAdministratorAppManager.share.loginUserMode.userID, toUid: targetId)
            MarkThen.communicationMenu(WithAdministratorAppManager.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.numberUp()

            //: } fail: { code, desc in
        } fail: { code, desc in
            QueryThen.bringInstance(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func addTime(convModel: MediaConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            QueryThen.bringInstance(msg: (str_colorValue + "会话失败，c" + String(str_messageText)) + "\(convModel.conversationID)" + (String(str_appValue.suffix(7))) + "\(code)" + (String(str_upBottomIfData.suffix(7))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.vanguardType(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.vanguardType(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension EdificeV2Listener: V2TIMSDKListener {
extension EdificeV2Listener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(str_modelText) + String(str_infoMainData)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(str_startName.suffix(4)) + "已经成\u{529f}" + String(str_listTitle)))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        QueryThen.bringInstance(msg: (String(str_pathData)) + "\(code)" + (String(str_dataErrorValue)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        QueryThen.bringInstance(msg: (str_playerName + String(str_toName)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        DataSocketDelegate.shared.approximateMatchOut(userLoginOut: true)
        //: EdificeV2Listener.shared.isConnection = false
        EdificeV2Listener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (ShouldKingfisherCompatible.shared as! ShouldKingfisherCompatible).toImage()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: str_colorTitle.map{$0^121}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = ManagerAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if DoingAddrTool.share.interfaceLang == ConditionCollection.ar.rawValue || DoingAddrTool.share.interfaceLang == ConditionCollection.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            DoingAddrTool.share.interfaceLang == ConditionCollection.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.behindUpRoom(fontSize: 13)
            config.rightFont = UIFont.behindUpRoom(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        VoiceAlertShow.enablelineMedium()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        VoiceAlertShow.enablelineMedium(title: (String(str_responseValue.suffix(6)) + String(str_toTitle) + str_infoText.replacingOccurrences(of: "content", with: "f") + "cation").localized, message: msg, leftBtnTitle: (str_rowPlayValue.replacingOccurrences(of: "show", with: "C") + str_winValue.replacingOccurrences(of: "model", with: "l")).localized, rightBtnTitle: (String(str_topTitle.suffix(8))).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            SectionPushListener.cover().toResource()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            StackMThen.estimated().chemistryLabResource()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            CellReactiveCompatible.pair { t in
                //: ProgressHUD.dismiss()
                StandardProgressHUD.giftTo()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_useTitle, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            CellReactiveCompatible.appCompletion { t in
                //: if t {
                if t {
                    //: EdificeV2Listener.shared.func__LogingIn { _ in
                    EdificeV2Listener.shared.viewCompletion { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if SectionPushListener.cover().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(SectionPushListener.cover().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    DataSocketDelegate.shared.prescription()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
