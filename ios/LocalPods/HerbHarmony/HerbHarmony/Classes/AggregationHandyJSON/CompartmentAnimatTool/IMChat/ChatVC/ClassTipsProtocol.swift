
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_lineTitle:[UInt8] = [0x9c,0xa1,0x9c,0xa7,0x5b,0x96,0xa2,0x97,0x98,0xa5,0x6d,0x5c,0x53,0x9b,0x94,0xa6,0x53,0xa1,0xa2,0xa7,0x53,0x95,0x98,0x98,0xa1,0x53,0x9c,0xa0,0xa3,0x9f,0x98,0xa0,0x98,0xa1,0xa7,0x98,0x97]

fileprivate func colorDevice(model num: UInt8) -> UInt8 {
    let value = Int(num) - 51
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "F5F5F5" :*/
fileprivate let str_tableValue:String = "valuevaluevalue"

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let str_successData:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","e"]
fileprivate let str_colorValue:String = "xtMsgfor var"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_mainTitle:String = "video con list returnTalkin"
fileprivate let str_layerContent:String = "tImagin m background return"
fileprivate let str_nameValue:String = "extension letellID"

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let str_constraintTitle:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","V"]
fileprivate let str_blowTitle:[Character] = ["i","d","e","o"]
fileprivate let str_managerTitle:String = "line centerMsgC"

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let str_oldValue:[Character] = ["T","a","l","k","i"]
fileprivate let str_countryText:[Character] = ["n","g","C","h","a","t","G","i","f","t","M","s"]
fileprivate let str_tempName:String = "gCellIDevent if view application date"

/*: "MagnitudeeractionMessageCell" :*/
fileprivate let str_toValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","s","k","F","o","r"]
fileprivate let str_alwaysName:[Character] = ["G","i","f","t","M"]
fileprivate let str_progressValue:[Character] = ["s","g","C","e","l","l"]

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_textContent:String = "make color return customTalkin"
fileprivate let str_sizeName:String = "ipsMsgCtrue name"
fileprivate let str_attentionValue:[Character] = ["e","l","l","I","D"]

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let str_modelLabValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","M","a","t","c","h","M","s","g","C"]
fileprivate let str_cellData:[Character] = ["e","l","l","I","D"]

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let str_upEqualData:String = "fatal model managerTalk"
fileprivate let str_failLoadData:String = "hatAutiming let video in"
fileprivate let str_buttonValue:String = "gCellIDraw attention"

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let str_modelText:[UInt8] = [0x16,0x23,0x2e,0x29,0x2b,0x2c,0x25,0x1,0x2a,0x23,0x36,0x11,0x3b,0x31,0x36,0x27,0x2f,0xc,0x2d,0x36,0x2b,0x24,0x8,0x37,0x2f,0x32,0xf,0x31,0x25,0x1,0x27,0x2e,0x2e,0xb,0x6]

private func keyLeading(format num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_makeContent:[Character] = ["T","S","y","s","t","e","m","M","e","s","s","a","g","e","C","e"]
fileprivate let str_viewData:String = "list info normal giftll_"
fileprivate let str_momentData:[Character] = ["R","e","u","s","e","I","d"]

/*: "TalkingChatMapMsgCellID" :*/
fileprivate let str_disappearValue:[Character] = ["T","a","l","k","i","n","g","C","h"]
fileprivate let str_agentName:String = "import message letatMapM"

/*: "#128CFF" :*/
fileprivate let str_countProfileData:String = "text self remove result object#128CFF"

/*: "Feedback" :*/
fileprivate let str_pressValue:String = "content"
fileprivate let str_nearTitle:String = "colorcolordback"

/*: "#8370FF" :*/
fileprivate let str_replyTitle:String = "#8370FFgroup var menu"

/*: "Quick Greeting" :*/
fileprivate let str_viewName:[Character] = ["Q","u","i","c","k"," "]
fileprivate let str_fromValue:String = "section for return time contentGreeting"

/*: "Beauty Photos" :*/
fileprivate let str_succeedText:String = "else makeBeauty Ph"
fileprivate let str_representationValue:[Character] = ["o","t","o","s"]

/*: "url" :*/
fileprivate let str_managerData:String = "urcenter"

/*: "unlockGift" :*/
fileprivate let str_cosTitle:String = "unlmatch"
fileprivate let str_errorValue:[Character] = ["c","k","G","i","f","t"]

/*: "%d" :*/
fileprivate let str_logName:[Character] = ["%","d"]

/*: "isBurn" :*/
fileprivate let str_itemName:[UInt8] = [0xd3,0xdd,0xac,0xdf,0xdc,0xd8]

fileprivate func refreshAllow(start num: UInt8) -> UInt8 {
    let value = Int(num) - 106
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let str_logValue:String = "giftIdprint of not"

/*: "rYMsgType" :*/
fileprivate let str_videoModelName:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let str_pushName:[Character] = ["G","J",":","C","a","l","l","C","u","s"]
fileprivate let str_contentName:[Character] = ["t","o","m"]

/*: "changeDirection" :*/
fileprivate let str_presentName:String = "ctimeang"
fileprivate let str_roomData:String = "ECTION"

/*: "extra" :*/
fileprivate let str_sizeValueTitle:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let str_cameraValue:[Character] = ["m","s"]
fileprivate let str_draftName:String = "gInfovar true return moment"

/*: "toUid" :*/
fileprivate let str_lineInstanceName:String = "gift for back collectiontoUid"

/*: "wantGift" :*/
fileprivate let str_pointData:[Character] = ["w","a","n","t","G","i"]
fileprivate let str_bottomSucceedValue:String = "usert"

/*: "jumpKey" :*/
fileprivate let str_applicationTitle:[Character] = ["j","u"]
fileprivate let str_createAddValue:[Character] = ["m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let str_backText:String = "truePersas add level component color"
fileprivate let str_indexName:String = "onAuthgreet mini data let"

/*: "logType" :*/
fileprivate let str_backgroundContent:String = "string in not instrumentlogType"

/*: "msgType" :*/
fileprivate let str_styleName:[UInt8] = [0xae,0xb0,0xa4,0x97,0xba,0xb3,0xa6]

private func numericalCount(actual num: UInt8) -> UInt8 {
    return num ^ 195
}

/*: "txt" :*/
fileprivate let str_aspectContent:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let str_atData:[Character] = ["t","y","p","e"]

/*: "QuickMsg" :*/
fileprivate let str_managerText:String = "mode imageQui"
fileprivate let str_tableName:String = "view can ask else giftckMsg"

/*: "params" :*/
fileprivate let str_shadowData:[UInt8] = [0x2,0x13,0x0,0x13,0x1f,0x1]

/*: "content" :*/
fileprivate let str_titleName:[UInt8] = [0x24,0x28,0x29,0x33,0x22,0x29,0x33]

/*: "id" :*/
fileprivate let str_bottomText:[Character] = ["i","d"]

/*: "txtId" :*/
fileprivate let str_broadcastTitle:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let str_playerValue:[UInt8] = [0x6f,0x67,0x71,0x71,0x63,0x65,0x67]

/*: "TxtMsg" :*/
fileprivate let str_videoValue:String = "t"
fileprivate let str_transformLabData:String = "screen height valuextMsg"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let str_enableControlTitle:[UInt8] = [0xee,0x7,0x1,0xfb,0xfd,0xb8,0x0,0xfd,0x4,0x4,0x7,0xb8,0xfc,0x7,0xf,0x6,0x4,0x7,0xf9,0xfc,0xb8,0xfe,0xf9,0x1,0x4,0xfd,0xfc,0xc6,0xb8,0xe8,0xa,0x7,0xfb,0xfd,0xfc,0xd,0xa,0xfd]

fileprivate func elseObserver(section num: UInt8) -> UInt8 {
    let value = Int(num) - 152
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let str_bottomValue:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let str_objectData:String = "AUDIO"

/*: "audioId" :*/
fileprivate let str_equalTitle:[Character] = ["a","u","d","i","o","I","d"]

/*: "audioLength" :*/
fileprivate let str_offName:[UInt8] = [0x68,0x74,0x67,0x6e,0x65,0x4c,0x6f,0x69,0x64,0x75,0x61]

/*: "audioSandbox" :*/
fileprivate let str_textImageValue:String = "aresult"
fileprivate let str_clickValue:[Character] = ["d","i","o","S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let str_userValue:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x6e,0x65,0x73]

/*: "AudioMsg" :*/
fileprivate let str_topTitle:String = "country"
fileprivate let str_popData:String = "udioMsgblock data gift view"

/*: "Can't send blank message" :*/
fileprivate let str_fillName:String = "Can\'t"
fileprivate let str_errorBarValue:String = "dismissank"

/*: "quoteMsgId" :*/
fileprivate let str_extraData:String = "cloudote"

/*: "quoteMsgInfo" :*/
fileprivate let str_stackInputViewContent:[Character] = ["q","u","o","t","e","M","s","g","I","n","f","o"]

/*: "contentType" :*/
fileprivate let str_requestIntervaleractionValue:[UInt8] = [0x48,0x44,0x45,0x5f,0x4e,0x45,0x5f,0x7f,0x52,0x5b,0x4e]

private func toDropImage(attention num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "ImgMsg" :*/
fileprivate let str_sexValue:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let str_nameTitle:String = "icolorg"

/*: "imageUri" :*/
fileprivate let str_fullData:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "customSend" :*/
fileprivate let str_makeValue:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "pic" :*/
fileprivate let str_hiddenData:String = "pipage"

/*: "VideoMsg" :*/
fileprivate let str_cornerDoinglineText:String = "player border color let pointVide"

/*: "video" :*/
fileprivate let str_normalTitle:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let str_delayData:[UInt8] = [0x27,0x33,0x3a,0x29,0x36,0xd,0x31,0x2b]

fileprivate func returnData(to num: UInt8) -> UInt8 {
    let value = Int(num) - 196
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "videoUrl" :*/
fileprivate let str_labelSexText:[UInt8] = [0xaf,0xb0,0xbd,0xbc,0xb6,0x8c,0xab,0xb5]

private func frameTalk(make num: UInt8) -> UInt8 {
    return num ^ 217
}

/*: "duration" :*/
fileprivate let str_textScaleData:[UInt8] = [0x6e,0x6f,0x69,0x74,0x61,0x72,0x75,0x64]

/*: "len" :*/
fileprivate let str_backCenterValue:[UInt8] = [0x14,0x1d,0x16]

private func labelShared(cover num: UInt8) -> UInt8 {
    return num ^ 120
}

/*: "map" :*/
fileprivate let str_makeName:[Character] = ["m","a","p"]

/*: "latlon" :*/
fileprivate let str_targetThanTitle:[UInt8] = [0x6e,0x6f,0x6c,0x74,0x61,0x6c]

/*: "messageType" :*/
fileprivate let str_playValueName:String = "mnumbers"
fileprivate let str_inputName:String = "main equal open edge varsageType"

/*: "MapMsg" :*/
fileprivate let str_itemValue:[Character] = ["M","a","p","M","s","g"]

/*: "Documents/User/voice/" :*/
fileprivate let str_managerName:String = "voice have var limitDocum"
fileprivate let str_labelCellName:[Character] = ["s","e","r","/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let str_sexBuildContent:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/","r","e"]
fileprivate let str_viewText:[Character] = ["c","o","r","d","/"]

/*: "msgIncome" :*/
fileprivate let str_gestureTitle:String = "make"
fileprivate let str_voiceData:String = "sgIncomefalse equal of"

/*: "mfBean" :*/
fileprivate let str_statuteValue:[UInt8] = [0x50,0x49,0x25,0x48,0x44,0x51]

fileprivate func keyTitle(mention num: UInt8) -> UInt8 {
    let value = Int(num) - 227
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "WantGift" :*/
fileprivate let str_popTitle:String = "WantGiftdata head of app"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let str_viewTitle:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t","T","i","p"]
fileprivate let str_fileName:[Character] = ["s","T"]
fileprivate let str_firstText:[Character] = ["i","t","l","e","_"]

/*: "msgId" :*/
fileprivate let str_addTitle:String = "msgIdshow text slide if"

/*: "totalMfCoin" :*/
fileprivate let str_cellTitle:[Character] = ["t","o","t","a","l","M","f","C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let str_yearName:String = "ATTACH"

/*: "intimatePhoto" :*/
fileprivate let str_saveText:String = "iuser"
fileprivate let str_toolTalkText:[Character] = ["t","i","m","a","t","e","P","h","o","t","o"]

/*: "&type= :*/
fileprivate let str_pageData:String = "&type=view data"

/*: _ :*/
fileprivate let str_insideName:String = "_"

/*: "totalIntimate" :*/
fileprivate let str_equalViewData:String = "totalIblue lab gift"
fileprivate let str_hiddenCellTitle:[Character] = ["t","e"]

/*: "uid" :*/
fileprivate let str_titleNumberData:[UInt8] = [0xa2,0xbe,0xb3]

/*: "intimate" :*/
fileprivate let str_insertData:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let str_containerText:[UInt8] = [0x72,0x50,0x5f,0x16,0x45,0x11,0x44,0x42,0x54,0x11,0x45,0x59,0x54,0x11,0x57,0x44,0x5f,0x52,0x45,0x58,0x5e,0x5f,0x11,0x55,0x44,0x54,0x11,0x45,0x5e,0x11,0x48,0x5e,0x44,0x43,0x11,0x5d,0x50,0x52,0x5a,0x11,0x5e,0x57,0x11,0x58,0x5f,0x45,0x58,0x5c,0x50,0x52,0x48,0x11,0x5d,0x54,0x47,0x54,0x5d,0x1f]

private func sourceStar(leading num: UInt8) -> UInt8 {
    return num ^ 49
}

/*: "giftNum" :*/
fileprivate let str_commentTitle:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let str_makeMicData:String = "pic else userpkgI"
fileprivate let str_mName:String = "image pair image nameetId"

/*: "from" :*/
fileprivate let str_labName:String = "frostyle"

/*: "wantGiftId" :*/
fileprivate let str_appText:String = "library status value hiddenwantGift"
fileprivate let str_tagValue:[Character] = ["I","d"]

/*: "WantGiftMsg" :*/
fileprivate let str_viewHiddenValue:[Character] = ["W","a","n","t","G","i","f","t","M","s"]
fileprivate let str_dataTitle:String = "shared"

/*: "gift" :*/
fileprivate let str_layerName:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let str_nameData:[Character] = ["V","i","e","w"," ","i","n","f","o"]

/*: "Notes" :*/
fileprivate let str_labQuoteDataText:String = "Notesstring layer gift room"

/*: "Report" :*/
fileprivate let str_communicationTitle:String = "cookie memoryReport"

/*: "View read receipts?" :*/
fileprivate let str_sharedData:[Character] = ["V","i","e","w"," ","r","e","a","d"," ","r","e","c","e","i","p","t","s","?"]

/*: "He" :*/
fileprivate let str_viewValue:String = "make stableHe"

/*: "She" :*/
fileprivate let str_modelValue:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let str_fromData:[Character] = ["%","@"," ","i","s"," ","u","s","i"]
fileprivate let str_bottomContent:[Character] = ["n","g"," ","t","o","o"," ","l","o","w"," ","a"," ","v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let str_showData:String = "Shootmodel behind self make model"
fileprivate let str_frontValue:String = "o atmake self cover"
fileprivate let str_noTitle:[Character] = ["5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let str_skinToName:[Character] = ["U","p","l","o","a","d"," ","f"]
fileprivate let str_modelTitle:String = "aileair"

/*: "播放状态: :*/
fileprivate let str_timeValue:String = "播放状from:"

/*: "c2c_" :*/
fileprivate let str_makeData:String = "c2c_area add bring print"

/*: "msg" :*/
fileprivate let str_textValue:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let str_faceName:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let str_layerValue:[UInt8] = [0x3c,0x52,0x58,0x3,0x46,0x44,0x51,0x3,0x58,0x56,0x48,0x3,0x57,0x4b,0x48,0x3,0x59,0x4c,0x47,0x48,0x52,0x3,0x46,0x4b,0x44,0x57,0x3,0x49,0x48,0x44,0x57,0x58,0x55,0x48,0x3,0x57,0x52,0x3,0x4a,0x4c,0x59,0x48,0x3,0x8,0x23,0x3,0x44,0x3,0x46,0x44,0x4f,0x4f]

fileprivate func quantityRequest(info num: UInt8) -> UInt8 {
    let value = Int(num) - 227
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cellData" :*/
fileprivate let str_languageData:String = "clabelll"

/*: "System notification" :*/
fileprivate let str_levelValue:[Character] = ["S","y","s","t","e","m"," ","n","o","t"]
fileprivate let str_currentData:String = "premiumfpremiumca"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ClassTipsProtocol.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import UIKit
import UIKit

//: import AVFAudio
import AVFAudio

//: import Photos
import Photos

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum VoiceFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ClassTipsProtocol: NameViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = CoverThen()
    private var userWrap = CoverThen() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = VoiceFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: CoverThen = CoverThen()) {
    init(chatID: String, isFrom: VoiceFromEnum = .Normal, userWrap: CoverThen = CoverThen()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ClassTipsProtocol.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        HandleThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == IconThen.getXiaoMiID() {
        if targetId == IconThen.finishBy() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == IconThen.getCustomerServiceID() {
        } else if targetId == IconThen.motivation() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_lineTitle.map{colorDevice(model: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if WithAdministratorAppManager.share.appConfigMode.disableShootScreen {
            if WithAdministratorAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CoverShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        yearWith()
        //: createUI()
        prize()
        //: createUIConstraints()
        saveFromRawMaterial()
        //: bindInteraction()
        squareTotaleraction()
        //: addTapGestureRecognizer()
        ascendingColon()
        //: func__requestChatInfo()
        jump()
        //: func__getFillDraftText()
        headTitle()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        ExampleManagerDelegate.shared.daskMatch()
        //: func__addVoiceNotification()
        managerCamera()
        //: addReadPeceiptNotification()
        parameter()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            imageSearched()
            //: func__TipsNotification()
            source()
            //: if WithAdministratorAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.male.rawValue {
                //: func__setReplyTipsView()
                roundParadigmName()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        messageCell(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        messageCell(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            cellBook()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (str_tableValue.replacingOccurrences(of: "value", with: "F5")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(HandleInstanceMessageCell.self, forCellReuseIdentifier: (String(str_successData) + String(str_colorValue.prefix(5)) + "CellID"))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(ExtraMsgCell.self, forCellReuseIdentifier: (String(str_mainTitle.suffix(6)) + "gCha" + String(str_layerContent.prefix(5)) + "eMsgC" + String(str_nameValue.suffix(5))))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(UsanceMessageCell.self, forCellReuseIdentifier: (String(str_constraintTitle) + String(str_blowTitle) + String(str_managerTitle.suffix(4)) + "ellID"))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(ViewCell.self, forCellReuseIdentifier: (String(str_oldValue) + String(str_countryText) + String(str_tempName.prefix(7))))
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(MagnitudeeractionMessageCell.self, forCellReuseIdentifier: (String(str_toValue) + String(str_alwaysName) + String(str_progressValue)))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(OffReactiveCompatible.self, forCellReuseIdentifier: (String(str_textContent.suffix(6)) + "gChatT" + String(str_sizeName.prefix(7)) + String(str_attentionValue)))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(SubmitThen.self, forCellReuseIdentifier: (String(str_modelLabValue) + String(str_cellData)))
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(ContainerMsgCell.self, forCellReuseIdentifier: (String(str_upEqualData.suffix(4)) + "ingC" + String(str_failLoadData.prefix(5)) + "dioMs" + String(str_buttonValue.prefix(7))))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(InfoViewCell.self, forCellReuseIdentifier: String(bytes: str_modelText.map{keyLeading(format: $0)}, encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_makeContent) + String(str_viewData.suffix(3)) + String(str_momentData)))

        //: msgVC.tableView.register(TalkingChatMapMsgCell.self, forCellReuseIdentifier: "TalkingChatMapMsgCellID")
        msgVC.tableView.register(TouchViewDelegate.self, forCellReuseIdentifier: (String(str_disappearValue) + String(str_agentName.suffix(6)) + "sgCellID"))

        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: EstateMenuManager = {
        //: let m = TalkingCallMenuManager()
        let m = EstateMenuManager()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: ButtonCellView = {
        //: var type: InputViewType = .PrivateChat
        var type: RoomViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = ButtonCellView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.liveAttention(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(str_countProfileData.suffix(7))))!, .font: UIFont.liveAttention(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (str_pressValue.replacingOccurrences(of: "content", with: "F") + str_nearTitle.replacingOccurrences(of: "color", with: "e")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(naturalActionClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: PopView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = PopView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: AssemblageReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = AssemblageReactiveCompatible(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_replyTitle.prefix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(str_viewName) + String(str_fromValue.suffix(8))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.alright().cgColor
        btn.layer.borderColor = UIColor.alright().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(viewFor), for: .touchUpInside)
        //: btn.isHidden = !WithAdministratorAppManager.share.appUserConfigMode.showDirectGreet || WithAdministratorAppManager.share.loginUserMode.sex == Gender.male.rawValue || WithAdministratorAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !WithAdministratorAppManager.share.appUserConfigMode.showDirectGreet || WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.male.rawValue || WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: InstanceThen? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = InstanceThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: ModelTranslationView = {
        //: let v = TalkingSelectTranslationView()
        let v = ModelTranslationView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.logTo(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: LeadingThen = {
        //: let v = TalkingTranslationResultView()
        let v = LeadingThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.colorModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.liveAttention(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_replyTitle.prefix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(str_succeedText.suffix(9)) + String(str_representationValue)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.alright().cgColor
        btn.layer.borderColor = UIColor.alright().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(doTimeScreen), for: .touchUpInside)
        //: btn.isHidden = WithAdministratorAppManager.share.loginUserMode.sex == Gender.male.rawValue || WithAdministratorAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.male.rawValue || WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: FrankThen = {
        //: let v = TalkingLiveRoomGamesView()
        let v = FrankThen()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: LeadingBannerView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = LeadingBannerView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: ViewThen = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = ViewThen()
        //: v.isHidden = (WithAdministratorAppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: StandardReactiveCompatible = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = StandardReactiveCompatible()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.limited()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ClassTipsProtocol: PrivateManagerBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func runView(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if WithAdministratorAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if WithAdministratorAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.dropSkirt(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ClassTipsProtocol: TMessageControllerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func viewFor() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        HandleThen.digitiserShared(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.atShow(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.dict(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func doTimeScreen() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        HandleThen.stroke(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = SubmitPhotoVc()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().commentGesture(urlStr: dict?[(str_managerData.replacingOccurrences(of: "center", with: "l"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(str_cosTitle.replacingOccurrences(of: "match", with: "o") + String(str_errorValue))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = TalkingModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: str_itemName.map{refreshAllow(start: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(str_logValue.prefix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.contextLab(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: ClickThen.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: ClickThen.self), self.userWrap.uid.count > 0 {
            //: cellData.msgModel.userInfoModel = self.userWrap
            cellData.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(str_videoModelName))].stringValue == (String(str_pushName) + String(str_contentName)) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(str_presentName.replacingOccurrences(of: "time", with: "h") + "eDir" + str_roomData.lowercased())].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData.msgModel.user.uid = jsonDic[(String(str_sizeValueTitle))][(String(str_cameraValue) + String(str_draftName.prefix(5)))][(String(str_lineInstanceName.suffix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData.msgModel
                let msgModel = cellData.msgModel
                //: cellData = CollectionThen(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = CollectionThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData.msgModel = msgModel
                cellData.msgModel = msgModel
            }
        }
        //: if cellData.isKind(of: VoxCellData.self) == true || cellData.isKind(of: IndexClearThen.self) == true || cellData.isKind(of: CollectionThen.self) == true || cellData.isKind(of: DemonstrateCellData.self) == true || cellData.isKind(of: UserCellData.self) == true {
        if cellData.isKind(of: VoxCellData.self) == true || cellData.isKind(of: IndexClearThen.self) == true || cellData.isKind(of: CollectionThen.self) == true || cellData.isKind(of: DemonstrateCellData.self) == true || cellData.isKind(of: UserCellData.self) == true {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
        //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
        HandleThen.viewLegalStatus(cellData: cellData, msgView: self.msgView)

        // 女性记录最新回复时间, 刷新表格
        //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
        HandleThen.reply(cellData.msgIncome, data.timestamp, msgView)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = self.textData(data: data)

        //: if data.isKind(of: CollectionThen.self) {
        if data.isKind(of: CollectionThen.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = HandleInstanceMessageCell(style: .default, reuseIdentifier: (String(str_successData) + String(str_colorValue.prefix(5)) + "CellID"))
            //: let cellData = data as! CollectionThen
            let cellData = data as! CollectionThen

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.of() {
                //: if cellData.msgModel.quoteMsgInfo.uid == WithAdministratorAppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == WithAdministratorAppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = WithAdministratorAppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = WithAdministratorAppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: IndexClearThen.self) {
        if data.isKind(of: IndexClearThen.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = ExtraMsgCell(style: .default, reuseIdentifier: (String(str_mainTitle.suffix(6)) + "gCha" + String(str_layerContent.prefix(5)) + "eMsgC" + String(str_nameValue.suffix(5))))
            //: let cellData = data as! IndexClearThen
            let cellData = data as! IndexClearThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: DemonstrateCellData.self) {
        if data.isKind(of: DemonstrateCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = UsanceMessageCell(style: .default, reuseIdentifier: (String(str_constraintTitle) + String(str_blowTitle) + String(str_managerTitle.suffix(4)) + "ellID"))
            //: let cellData = data as! DemonstrateCellData
            let cellData = data as! DemonstrateCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ButtonCellData.self) {
        if data.isKind(of: ButtonCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = ViewCell(style: .default, reuseIdentifier: (String(str_oldValue) + String(str_countryText) + String(str_tempName.prefix(7))))
            //: let cellData = data as! ButtonCellData
            let cellData = data as! ButtonCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: QuantityCellData.self) {
        if data.isKind(of: QuantityCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = MagnitudeeractionMessageCell(style: .default, reuseIdentifier: (String(str_toValue) + String(str_alwaysName) + String(str_progressValue)))
            //: let cellData = data as! QuantityCellData
            let cellData = data as! QuantityCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = GiftSectionModelType()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.notStatus(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(str_pointData) + str_bottomSucceedValue.replacingOccurrences(of: "user", with: "f")))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: VoxCellData.self) {
        if data.isKind(of: VoxCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = ContainerMsgCell(style: .default, reuseIdentifier: (String(str_upEqualData.suffix(4)) + "ingC" + String(str_failLoadData.prefix(5)) + "dioMs" + String(str_buttonValue.prefix(7))))
            //: let cellData = data as! VoxCellData
            let cellData = data as! VoxCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ClickThen.self) {
        if data.isKind(of: ClickThen.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = SubmitThen(style: .default, reuseIdentifier: (String(str_modelLabValue) + String(str_cellData)))
            //: let cellData = data as! ClickThen
            let cellData = data as! ClickThen
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ShowCellData.self) {
        if data.isKind(of: ShowCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = OffReactiveCompatible(style: .default, reuseIdentifier: (String(str_textContent.suffix(6)) + "gChatT" + String(str_sizeName.prefix(7)) + String(str_attentionValue)))
            //: let cellData = data as! ShowCellData
            let cellData = data as! ShowCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: AtonicityThen.self) {
        if data.isKind(of: AtonicityThen.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = InfoViewCell(style: .default, reuseIdentifier: String(bytes: str_modelText.map{keyLeading(format: $0)}, encoding: .utf8)!)
            //: let cellData = data as! AtonicityThen
            let cellData = data as! AtonicityThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: UserCellData.self) {
        if data.isKind(of: UserCellData.self) {
            //: let cell = TalkingChatMapMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMapMsgCellID")
            let cell = TouchViewDelegate(style: .default, reuseIdentifier: (String(str_disappearValue) + String(str_agentName.suffix(6)) + "sgCellID"))
            //: let cellData = data as! UserCellData
            let cellData = data as! UserCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != IconThen.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != IconThen.motivation() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: LeadingEqualReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
            LeadingEqualReactiveCompatible.share.bindDownFrom(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ExtraMsgCell.self) { // 图片
            //: let imageCellData = cell.messageData as! IndexClearThen
            let imageCellData = cell.messageData as! IndexClearThen
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                //: let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                //: let num = TalkingPrivateChatManager.checkCanPreview(isImg: true, needAlert: needAlert)
                let num = HandleThen.cardAlert(isImg: true, needAlert: needAlert)
                /// 需要解锁的照片
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                let vc = StartContainerRecognizerDelegate(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 1)
                        self.bequestType(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_screenContent, type: 1)
                        //: return
                        return
                    }
                    //: WithAdministratorAppManager.share.loginUserMode.updateResidualNum(type: 1, num: max(0, num - 1))
                    WithAdministratorAppManager.share.loginUserMode.nearDay(type: 1, num: max(0, num - 1))
//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                navigation()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(kLet_appText)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = ClimaxModelType()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = ModelHandyJSON()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [ClimaxModelType] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = CountryViewController(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                navigation()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: UsanceMessageCell.self) { // 视频
            //: let videoCellData = cell.messageData as! DemonstrateCellData
            let videoCellData = cell.messageData as! DemonstrateCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = ViewManagerReactiveCompatible(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                navigation()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                //: let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                //: let num = TalkingPrivateChatManager.checkCanPreview(isImg: false, needAlert: needAlert)
                let num = HandleThen.cardAlert(isImg: false, needAlert: needAlert)
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                let vc = ManagerTotalimatePreviewVideoViewDelegate(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 2)
                        self.bequestType(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_screenContent, type: 2)
                        //: return
                        return
                    }
                    //: WithAdministratorAppManager.share.loginUserMode.updateResidualNum(type: 2, num: max(0, num - 1))
                    WithAdministratorAppManager.share.loginUserMode.nearDay(type: 2, num: max(0, num - 1))

//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                navigation()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ContainerMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ContainerMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! VoxCellData, cellView: audioCell)
            modelConstraintView(cellData: audioCell.cellData as! VoxCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: HandleInstanceMessageCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! HandleInstanceMessageCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(str_sizeValueTitle))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_applicationTitle) + String(str_createAddValue))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(str_applicationTitle) + String(str_createAddValue))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(str_backText.prefix(8)) + String(str_indexName.prefix(6))) {
                    //: if WithAdministratorAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if WithAdministratorAppManager.share.loginUserMode.isTPAuth == ConditionSendable.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if WithAdministratorAppManager.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if WithAdministratorAppManager.share.loginUserMode.isTPAuth == ConditionSendable.unknown.rawValue {
                        //: LeadingEqualReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                        LeadingEqualReactiveCompatible.share.shove(toast: nil)
                        //: } else if WithAdministratorAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if WithAdministratorAppManager.share.loginUserMode.isTPAuth == ConditionSendable.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = CommensurateViewController()
                        //: LeadingEqualReactiveCompatible.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        LeadingEqualReactiveCompatible.share.getEqualVc()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: LeadingEqualReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                        LeadingEqualReactiveCompatible.share.shove(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<SendHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LeadingEqualReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LeadingEqualReactiveCompatible.share.terminal(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(str_videoModelName))] as? String == (String(str_pushName) + String(str_contentName)) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(str_cameraValue) + String(str_draftName.prefix(5)))][(String(str_backgroundContent.suffix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    tick()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    refer()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: OffReactiveCompatible.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! OffReactiveCompatible
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(str_sizeValueTitle))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_applicationTitle) + String(str_createAddValue))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<SendHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LeadingEqualReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LeadingEqualReactiveCompatible.share.terminal(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: SubmitThen.self) {
            //: LeadingEqualReactiveCompatible.share.func__pushToUserDetailVC(uid: targetId)
            LeadingEqualReactiveCompatible.share.bindDownFrom(uid: targetId)
            //: }else if cell.isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: TouchViewDelegate.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! TouchViewDelegate
            //: let mapCellData = mapCell.cellData as! UserCellData
            let mapCellData = mapCell.cellData as! UserCellData
            //: self.func__showLocationView(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: (mapCellData.direction == .MsgDirectionOutgoing))
            self.timeSelf(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: mapCellData.direction == .MsgDirectionOutgoing)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: CollectionReactiveCompatible?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ExtraMsgCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! ExtraMsgCell
            //: cellData = imageCell.cellData as! IndexClearThen
            cellData = imageCell.cellData as! IndexClearThen
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: UsanceMessageCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! UsanceMessageCell
            //: cellData = videoCell.cellData as! DemonstrateCellData
            cellData = videoCell.cellData as! DemonstrateCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ContainerMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ContainerMsgCell
            //: cellData = audioCell.cellData as! VoxCellData
            cellData = audioCell.cellData as! VoxCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: HandleInstanceMessageCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! HandleInstanceMessageCell
            //: cellData = textCell.cellData as! CollectionThen
            cellData = textCell.cellData as! CollectionThen
            //: } else if cell .isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: TouchViewDelegate.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! TouchViewDelegate
            //: cellData = mapCell.cellData as! UserCellData
            cellData = mapCell.cellData as! UserCellData
        }

        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: DemonstrateCellData.self) {
                if reCellData!.isKind(of: DemonstrateCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    awakeSend(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    weltanschauung(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = HandleThen.magnitudeer(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        HandleThen.tab(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TUIMessageCellData) -> TUIMessageCellData {
    private func textData(data: TUIMessageCellData) -> TUIMessageCellData {
        //: let isSelf = data.msgModel.user.uid == Int(WithAdministratorAppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(WithAdministratorAppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if WithAdministratorAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if WithAdministratorAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = WithAdministratorAppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = WithAdministratorAppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = WithAdministratorAppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = WithAdministratorAppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func responseEnable() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (String(str_aspectContent))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(str_atData)): (String(str_managerText.suffix(3)) + String(str_tableName.suffix(5)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo, String(bytes: str_shadowData.map{$0^114}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
        self.weltanschauung(msgData: cellData, reSend: false)
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func dict(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: str_titleName.map{$0^71}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: str_titleName.map{$0^71}, encoding: .utf8)!: content, (String(str_broadcastTitle)): greetId, String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (String(str_aspectContent))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: str_playerValue.map{$0^2}, encoding: .utf8)!: content, (String(str_broadcastTitle)): greetId, (String(str_atData)): (str_videoValue.uppercased() + String(str_transformLabData.suffix(5)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo, String(bytes: str_shadowData.map{$0^114}, encoding: .utf8)!: paramsDic]
            //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.weltanschauung(msgData: cellData, reSend: false)
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            HandleSessionDelegate.shared.locationCell(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.atShow(showMsg: String(bytes: str_enableControlTitle.map{elseObserver(section: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        HandleThen.technique(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(str_bottomValue))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (str_objectData.lowercased()),
                               //: "audioId": greetId,
                               (String(str_equalTitle)): greetId,
                               //: "audioLength": length,
                               String(bytes: str_offName.reversed(), encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (str_textImageValue.replacingOccurrences(of: "result", with: "u") + String(str_clickValue)): model.unZipFilePath,
                               //: "senderId": WithAdministratorAppManager.share.loginUserMode.userID]
                               String(bytes: str_userValue.reversed(), encoding: .utf8)!: WithAdministratorAppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(str_equalTitle)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: str_offName.reversed(), encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (str_textImageValue.replacingOccurrences(of: "result", with: "u") + String(str_clickValue)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(str_atData)): (str_topTitle.replacingOccurrences(of: "country", with: "A") + String(str_popData.prefix(7)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo, String(bytes: str_shadowData.map{$0^114}, encoding: .utf8)!: paramsDic]
                //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.weltanschauung(msgData: cellData, reSend: false)
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func colorModel(msgStr: String, quoteModel: PathModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            atShow(showMsg: (str_fillName + " send b" + str_errorBarValue.replacingOccurrences(of: "dismiss", with: "l") + " message").localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: str_titleName.map{$0^71}, encoding: .utf8)!: temStr, String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (String(str_aspectContent))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: str_playerValue.map{$0^2}, encoding: .utf8)!: temStr, (String(str_atData)): (str_videoValue.uppercased() + String(str_transformLabData.suffix(5)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(str_extraData.replacingOccurrences(of: "cloud", with: "qu") + "MsgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(str_stackInputViewContent))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: str_shadowData.map{$0^114}, encoding: .utf8)!] = paramsDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
        cellData.msgModel.quoteMsgInfo = quoteModel ?? PathModel()

        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        weltanschauung(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func contextLab(img: UIImage, sendModel: TalkingModel) {
        //: let imagePath = IndexClearThen.getChatImagePath(image: img)
        let imagePath = IndexClearThen.largess(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: str_requestIntervaleractionValue.map{toDropImage(attention: $0)}, encoding: .utf8)!: (String(str_sexValue)), String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (str_nameTitle.replacingOccurrences(of: "color", with: "m")), String(bytes: str_fullData.reversed(), encoding: .utf8)!: imagePath, String(bytes: str_makeValue.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(str_hiddenData.replacingOccurrences(of: "page", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(str_atData)): (String(str_sexValue)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_itemName.map{refreshAllow(start: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_logValue.prefix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo, String(bytes: str_shadowData.map{$0^114}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        weltanschauung(msgData: cellData, reSend: false)
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TUIMessageCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func awakeSend(msgData: TUIMessageCellData?, sendModel: TalkingModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: str_requestIntervaleractionValue.map{toDropImage(attention: $0)}, encoding: .utf8)!: (String(str_cornerDoinglineText.suffix(4)) + "oMsg"), String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (String(str_normalTitle)), String(bytes: str_makeValue.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(str_atData)): (String(str_cornerDoinglineText.suffix(4)) + "oMsg"),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: str_delayData.map{returnData(to: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: str_labelSexText.map{frameTalk(make: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: str_textScaleData.reversed(), encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_itemName.map{refreshAllow(start: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_logValue.prefix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo, String(bytes: str_shadowData.map{$0^114}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            //: self.msgView.sendCustomMessage(uploadCellData)
            self.msgView.sendCustomMessage(uploadCellData)
            //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
            self.fileModel(msgData: uploadCellData, sendModel: sendModel)
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.fileModel(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        weltanschauung(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func success(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (str_objectData.lowercased()),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: str_offName.reversed(), encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (str_textImageValue.replacingOccurrences(of: "result", with: "u") + String(str_clickValue)): voicePath,
                       //: "senderId": WithAdministratorAppManager.share.loginUserMode.userID]
                       String(bytes: str_userValue.reversed(), encoding: .utf8)!: WithAdministratorAppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(str_textImageValue.replacingOccurrences(of: "result", with: "u") + String(str_clickValue)): voicePath,
                         //: "type": "AudioMsg",
                         (String(str_atData)): (str_topTitle.replacingOccurrences(of: "country", with: "A") + String(str_popData.prefix(7))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: str_backCenterValue.map{labelShared(cover: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo, String(bytes: str_shadowData.map{$0^114}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        weltanschauung(msgData: cellData, reSend: false)
    }

    //: func func__sendLocationMsg(latlon: String) {
    func nextFrom(latlon: String) {
        //: let msgInfo = ["msgType": "map",
        let msgInfo = [String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (String(str_makeName)),
                       //: "latlon": latlon,
                       String(bytes: str_targetThanTitle.reversed(), encoding: .utf8)!: latlon,
                       //: "messageType": 28,
                       (str_playValueName.replacingOccurrences(of: "number", with: "e") + String(str_inputName.suffix(8))): 28,
                       //: "senderId": WithAdministratorAppManager.share.loginUserMode.userID] as [String : Any]
                       String(bytes: str_userValue.reversed(), encoding: .utf8)!: WithAdministratorAppManager.share.loginUserMode.userID] as [String: Any]

        //: let paramsDic = ["latlon": latlon,
        let paramsDic = [String(bytes: str_targetThanTitle.reversed(), encoding: .utf8)!: latlon,
                         //: "type": "MapMsg"] as [String : Any]
                         (String(str_atData)): (String(str_itemValue))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo, String(bytes: str_shadowData.map{$0^114}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        weltanschauung(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func weltanschauung(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            atShow(showMsg: kLet_detailMessageValue)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(str_lineInstanceName.suffix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: DemonstrateCellData.self) == false && msgData.isKind(of: QuantityCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: DemonstrateCellData.self) == false, msgData.isKind(of: QuantityCellData.self) == false, params[(String(str_atData))] as? String != (String(str_managerText.suffix(3)) + String(str_tableName.suffix(5))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = TUIMsgChatManager.getMessageInsertTime()
        let recordTime = TUIMsgChatManager.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((str_hiddenData.replacingOccurrences(of: "page", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(str_hiddenData.replacingOccurrences(of: "page", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(str_hiddenData.replacingOccurrences(of: "page", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(String(str_atData))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (str_topTitle.replacingOccurrences(of: "country", with: "A") + String(str_popData.prefix(7))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((str_textImageValue.replacingOccurrences(of: "result", with: "u") + String(str_clickValue))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(str_textImageValue.replacingOccurrences(of: "result", with: "u") + String(str_clickValue))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (str_textImageValue.replacingOccurrences(of: "result", with: "u") + String(str_clickValue)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(str_managerName.suffix(5)) + "ents/U" + String(str_labelCellName))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = kLet_turnName + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(str_sexBuildContent) + String(str_viewText))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kLet_keyMarginClickText + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(str_equalTitle))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(str_objectData.lowercased())] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: BridgeReactiveCompatible.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        BridgeReactiveCompatible.enableView(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(str_managerText.suffix(3)) + String(str_tableName.suffix(5))) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.bequestType(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_infoValue)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(str_broadcastTitle))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    HandleThen.technique(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(str_cameraValue) + String(str_draftName.prefix(5)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome = NSNumber(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((str_gestureTitle.replacingOccurrences(of: "make", with: "m") + String(str_voiceData.prefix(8)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(str_gestureTitle.replacingOccurrences(of: "make", with: "m") + String(str_voiceData.prefix(8)))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if SectionPushListener.cover().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: kLet_conversationName, object: nil, userInfo: [String(bytes: str_statuteValue.map{keyTitle(mention: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.removeSumimate(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(str_playValueName.replacingOccurrences(of: "number", with: "e") + String(str_inputName.suffix(8)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(str_managerText.suffix(3)) + String(str_tableName.suffix(5))) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!] = (String(str_popTitle.prefix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kLet_voiceText.quantityingTime(eventID: kLet_showValue)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(str_managerText.suffix(3)) + String(str_tableName.suffix(5))) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!] = (String(str_aspectContent))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo]
                //: updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.msgView.sendCustomMessage(updateMsgData)
                self.msgView.sendCustomMessage(updateMsgData)
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(WithAdministratorAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(WithAdministratorAppManager.share.loginUserMode.userID)" + (String(str_viewTitle) + String(str_fileName) + String(str_firstText)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if WithAdministratorAppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.male.rawValue, type != (String(str_managerText.suffix(3)) + String(str_tableName.suffix(5))) {
                //: self.removeReplyTipsView()
                self.viewSize()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(str_addTitle.prefix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            HandleThen.reply(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(str_cellTitle))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(str_cellTitle))] as! NSNumber
                //: WithAdministratorAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                WithAdministratorAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((str_yearName.lowercased() + "SysMsg")) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(str_yearName.lowercased() + "SysMsg")] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.renderConfidential(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((str_saveText.replacingOccurrences(of: "user", with: "n") + String(str_toolTalkText))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(str_saveText.replacingOccurrences(of: "user", with: "n") + String(str_toolTalkText))] as! [String: Any]
                //: let model = HypothesisModel(dic: intimatePhotoDict)
                let model = HypothesisModel(dic: intimatePhotoDict)
                //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                updateMsgData.msgModel.msgInfo.intimatePhoto = model
                //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(str_normalTitle))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(str_normalTitle))] as! [String: Any]
                //: let model = ValueJsonModel(dic: intimateVideoDict)
                let model = ValueJsonModel(dic: intimateVideoDict)
                //: updateMsgData.msgModel.msgInfo.video = model
                updateMsgData.msgModel.msgInfo.video = model
                //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        visualCommunication()

        //: updateSession()
        standInTitle()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func standInTitle() {
        //: ChiefThen.cache_updateSessionAndPostNotif(with: targetId)
        ChiefThen.dataCountTop(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String, type: Int = 0) {
    private func bequestType(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent _: String, type: Int = 0) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard WithAdministratorAppManager.share.loginUserMode.status != 1 else {
            guard WithAdministratorAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    atShow(showMsg: errorStr)
                }
                //: return
                return
            }
            //: LeadingEqualReactiveCompatible.share.func__pushToRechargeAlert(appendParams: "&type=\(type)")
            LeadingEqualReactiveCompatible.share.squash(appendParams: (String(str_pageData.prefix(6))) + "\(type)")
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.actionValue()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: LeadingEqualReactiveCompatible.share.func__pushToSubscribeAlert()
            LeadingEqualReactiveCompatible.share.nowKeyLog()

            //: } else if errorCode == 60003, !WithAdministratorAppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !WithAdministratorAppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            ShowWindowManager.shared.mObjectDetail()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                atShow(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func unitedStatesOfAmerica(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(str_cameraValue) + String(str_draftName.prefix(5)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.removeSumimate(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func fit() {
        //: if self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.worldViewMatch(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(WithAdministratorAppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(WithAdministratorAppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (kLet_dismissData.array(forKey: kLet_packageContent)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.first()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                kLet_dismissData.set(intimateCacheData, forKey: kLet_packageContent)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func sumLog(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(str_sizeValueTitle))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(str_cameraValue) + String(str_draftName.prefix(5)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(str_equalViewData.prefix(6)) + "ntima" + String(str_hiddenCellTitle))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.colorStatus(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func removeSumimate(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(str_equalViewData.prefix(6)) + "ntima" + String(str_hiddenCellTitle))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(str_equalViewData.prefix(6)) + "ntima" + String(str_hiddenCellTitle))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.colorStatus(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func colorStatus(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = ChiefThen.cache_getCachedUserInfo(targetId: self.targetId)
        let model = ChiefThen.cached(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.worldViewMatch(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.putDoing(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.successHalf(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.successHalf(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: ChiefThen.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        ChiefThen.accountTime(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: str_titleNumberData.map{$0^215}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: str_insertData.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_turnValue,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ClassTipsProtocol: ContentViewDelegate {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func sizeTo() {
        //: guard WithAdministratorAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.filter(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func mention(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func saveer(actionType: ShouldMirrorPath) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    passJudgmentShould()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    distortion()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                passJudgmentShould()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if WithAdministratorAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if WithAdministratorAppManager.share.loginUserMode.loungePlus {
                if WithAdministratorAppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.outsideStart(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                distortion()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    gildTheLilyVideo()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    distortion()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                gildTheLilyVideo()
            }

        //: case .Location:
        case .Location:
            //: TalkingPermissionTool.func__openLocationServiceWithBlock(true) { [weak self] isOpen in
            LaissezPasserThen.blockStream(true) { [weak self] isOpen in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if isOpen {
                if isOpen {
                    //: self.func__showLocationView(latlon: nil, lookSelf: false)
                    self.timeSelf(latlon: nil, lookSelf: false)
                }
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.selectedShow(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func distortion() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.atShow(showMsg: String(bytes: str_containerText.map{sourceStar(leading: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func quantityeraction(giftModel _: GiftSectionModelType, giftNum _: String, model _: PopScaleReactiveCompatible) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func nameNum(giftModel: GiftSectionModelType, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        notStatus(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: QuantityCellData?, fromStr: String?) {
    func notStatus(giftModel: GiftSectionModelType, giftNum: String, cellData: QuantityCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            atShow(showMsg: kLet_detailMessageValue)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(str_lineInstanceName.suffix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(str_logValue.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(str_commentTitle))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_makeMicData.suffix(4)) + "tems" + String(str_mName.suffix(4)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(str_labName.replacingOccurrences(of: "style", with: "m"))] = fromStr
        }
        // 送礼接口
        //: BridgeReactiveCompatible.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        BridgeReactiveCompatible.sendToCompletionSearched(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.bequestType(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_timeLicenseName)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.constraintContainer(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.behindLoadView(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.standInTitle()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(str_cameraValue) + String(str_draftName.prefix(5)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(str_equalViewData.prefix(6)) + "ntima" + String(str_hiddenCellTitle))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(str_equalViewData.prefix(6)) + "ntima" + String(str_hiddenCellTitle))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.colorStatus(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kLet_voiceText.quantityingTime(eventID: kLet_pointContent)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func beforeChecked(giftModel: GiftSectionModelType, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kLet_voiceText.quantityingTime(eventID: kLet_barUserName)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            atShow(showMsg: kLet_detailMessageValue)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= WithAdministratorAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            distortion()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(str_appText.suffix(8)) + String(str_tagValue))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(str_atData))] = (String(str_viewHiddenValue) + str_dataTitle.replacingOccurrences(of: "shared", with: "g"))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (String(str_popTitle.prefix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(str_cameraValue) + String(str_draftName.prefix(5))): msgInfo, String(bytes: str_shadowData.map{$0^114}, encoding: .utf8)!: params]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        weltanschauung(msgData: cellData, reSend: false)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func beforeConstraint(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(kLet_noText - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func inputPricePlay(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func withRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.withRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                halfLive(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func noBackground() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if ExampleManagerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            ExampleManagerDelegate.shared.participant()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func behindLoadView(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(str_cameraValue) + String(str_draftName.prefix(5)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!] = (String(str_layerName))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_cameraValue) + String(str_draftName.prefix(5)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((str_gestureTitle.replacingOccurrences(of: "make", with: "m") + String(str_voiceData.prefix(8)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(str_gestureTitle.replacingOccurrences(of: "make", with: "m") + String(str_voiceData.prefix(8)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_addTitle.prefix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        equalDic(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(str_cellTitle))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(str_cellTitle))] as! NSNumber
            //: WithAdministratorAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            WithAdministratorAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: WithAdministratorAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.aperture(needReload: false, mf_coin: WithAdministratorAppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func imageSearched() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.taget(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func equalDic(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.blockMessage(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func welt(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.giftExecuteEnable(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func imageReport() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func tick() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.sharedComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.standInTitle()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func refer() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.sendCall { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.standInTitle()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    //: @objc private func feedbackBtnClick() {
    @objc private func naturalActionClick() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = EmptyReactiveCompatible()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        navigation()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func tillToAGreaterExtent() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = SuspensionBridgeDataSource(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.titleEvent(cellTitleList: [(String(str_nameData)).localized, (String(str_labQuoteDataText.prefix(5))).localized, (String(str_communicationTitle.suffix(6))).localized])
        //: vc.cancelColor = .occurrence()
        vc.cancelColor = .occurrence()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: LeadingEqualReactiveCompatible.share.func__pushToUserDetailVC(uid: self.targetId)
                LeadingEqualReactiveCompatible.share.bindDownFrom(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                limited()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = SectionThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.format(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func limited() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = ModelNotesView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.constraintUid(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.lockBarError(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func behindTorso() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            halfLive(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            navigation()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func notificationMedium(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = CountScreenModel.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.indexFamily(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func parameter() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(acquisition(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: kLet_dataName,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(funcTimeDate),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: kLet_greetData,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func acquisition(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard WithAdministratorAppManager.share.loginUserMode.loungePlus == true else { return }
        guard WithAdministratorAppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func funcTimeDate() {
        //: WithAdministratorAppManager.share.loginUserMode.loungePlus = true
        WithAdministratorAppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func userMedium() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if HandleThen.mic(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.overbusy(tipStr: (String(str_sharedData)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ClassTipsProtocol: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func passJudgmentShould() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        LaissezPasserThen.conceptAlbum(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = EqualToolThen.magnification(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.designation(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func canView() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        LaissezPasserThen.effectOf(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.designation(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func designation(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: WithAdministratorAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TalkingModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.contextLab(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = CountryViewController(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TalkingModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.contextLab(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        navigation()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func gildTheLilyVideo() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard HandleThen.actionVersion(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == MiniSubqueryConvertible.male.rawValue ? "He".localized : (String(str_modelValue)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.atShow(showMsg: (String(str_fromData) + String(str_bottomContent)).localizedArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.be()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func be() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = EqualToolThen.magnification(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            EqualToolThen.dataStorageMedium(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.iconTo(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.atShow(showMsg: (String(str_showData.prefix(5)) + " vide" + String(str_frontValue.prefix(4)) + " least " + String(str_noTitle)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func iconTo(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: EffectEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = PriceEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TalkingModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.awakeSend(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        StandardProgressHUD.giftTo()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        navigation()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TUIMessageCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func fileModel(msgData: TUIMessageCellData, sendModel: TalkingModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        AssociatedStateDirectorSignatureProvider.shared.net(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.atShow(showMsg: (String(str_skinToName) + str_modelTitle.replacingOccurrences(of: "air", with: "d")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.awakeSend(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    //: func func__addVoiceNotification() {
    func managerCamera() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(imageOnActive),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(withObject),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(appSession(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(birthNotif(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: kLet_screenData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(sumLog(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kLet_barCornerClickData,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func messageCell(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            ExampleManagerDelegate.shared.daskMatch()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            ExampleManagerDelegate.shared.disappearUser()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func imageOnActive() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        messageCell(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func withObject() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        messageCell(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func appSession(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            messageCell(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            messageCell(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: VoxCellData, cellView: TalkingChatAudioMsgCell) {
    func modelConstraintView(cellData: VoxCellData, cellView: ContainerMsgCell) {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard DataSocketDelegate.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.atShow(showMsg: kLet_keyTitle)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TUIMessageCellData)!.isKind(of: VoxCellData.self) == false {
            if (indexModel as? TUIMessageCellData)!.isKind(of: VoxCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! VoxCellData
            let audioModel = indexModel as! VoxCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: cellData.voiceModel.db_voiceCacheWrap = ChiefThen.cache_getAudioDB(message: message)
        cellData.voiceModel.db_voiceCacheWrap = ChiefThen.thirdMessage(message: message)
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        ExampleManagerDelegate.shared.participant()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if ExampleManagerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (str_timeValue.replacingOccurrences(of: "from", with: "态")) + "\(ExampleManagerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if ExampleManagerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                ExampleManagerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        ExampleManagerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        ExampleManagerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: VoxCellData.self) {
                if subModel.isKind(of: VoxCellData.self) {
                    //: let audioModel = subModel as! VoxCellData
                    let audioModel = subModel as! VoxCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: voiceModel.db_voiceCacheWrap = ChiefThen.cache_getAudioDB(message: subMessage)
                    voiceModel.db_voiceCacheWrap = ChiefThen.thirdMessage(message: subMessage)
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: MarkThen.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    MarkThen.flushWithVoiceOver(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        ExampleManagerDelegate.shared.subedit(msgArr: waitArray)
    }
}

// MARK: - 地图相关

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 发送地图
    //: private func func__showLocationView(latlon: String?, lookSelf: Bool) {
    private func timeSelf(latlon: String?, lookSelf: Bool) {
        //: let location = TalkingMapView(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        let location = GiveAndTakeMapView(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        //: location.show()
        location.bag()
        //: location.sendBlcok = { [weak self] latlon in
        location.sendBlcok = { [weak self] latlon in
            //: self?.func__sendLocationMsg(latlon: latlon)
            self?.nextFrom(latlon: latlon)
        }
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func jump() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.dealIndex()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.dict(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TUIMessageCellData
                let indexModel = model as? TUIMessageCellData
                //: if indexModel?.isKind(of: VoxCellData.self) == true || indexModel?.isKind(of: IndexClearThen.self) == true || indexModel?.isKind(of: CollectionThen.self) == true || indexModel?.isKind(of: DemonstrateCellData.self) == true {
                if indexModel?.isKind(of: VoxCellData.self) == true || indexModel?.isKind(of: IndexClearThen.self) == true || indexModel?.isKind(of: CollectionThen.self) == true || indexModel?.isKind(of: DemonstrateCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.visualCommunication()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.serviceWithPlace()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.userMedium()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func dealIndex() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.nearSquare(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.someoneIn(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.fit()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == WithAdministratorAppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == WithAdministratorAppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == WithAdministratorAppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == WithAdministratorAppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.lockBarError(userWrap)
    }

    //: func func__getFillDraftText() {
    func headTitle() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_makeData.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.sizeTo()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func cellBook() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_makeData.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    //: func func__TipsNotification() {
    func source() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(modelGift(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_managerPlayName,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func modelGift(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: ShowCellData  = userinfo["msg"] as! ShowCellData
        let celldata: ShowCellData = userinfo[(String(str_textValue))] as! ShowCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func overbusy(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: str_titleName.map{$0^71}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: str_styleName.map{numericalCount(actual: $0)}, encoding: .utf8)!: (String(str_faceName))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func serviceWithPlace() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        HandleThen.voiceGesture { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.overbusy(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func renderConfidential(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.overbusy(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func visualCommunication() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        HandleThen.flagHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.overbusy(tipStr: String(bytes: str_layerValue.map{quantityRequest(info: $0)}, encoding: .utf8)!.localizedArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func birthNotif(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TUIMessageCellData = dict?["cellData"] as! TUIMessageCellData
        let cellData: TUIMessageCellData = dict?[(str_languageData.replacingOccurrences(of: "label", with: "e") + "Data")] as! TUIMessageCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ClassTipsProtocol: IndexTipsProtocol {
    //: func func__setReplyTipsView() {
    func roundParadigmName() {
        //: let key =  "\(WithAdministratorAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(WithAdministratorAppManager.share.loginUserMode.userID)" + (String(str_viewTitle) + String(str_fileName) + String(str_firstText)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && WithAdministratorAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.todayInfo()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            runView(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            viewSize()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func rowStr(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        colorModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        viewSize()
    }

    //: func removeReplyTipsView() {
    func viewSize() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            runView(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func yearWith() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kLet_contentBarName, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: ScreenHeight - kLet_noText, width: ScreenWidth, height: ScreenHeight)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: ScreenWidth,
                                     //: height: ScreenHeight)
                                     height: ScreenHeight)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func halfLive(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.actionController() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if StackMThen.estimated().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_countKeyValue, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: CleanManagerDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: ScreenHeight - kLet_noText,
                                     //: width: ScreenWidth,
                                     width: ScreenWidth,
                                     //: height: ScreenHeight)
                                     height: ScreenHeight)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if StackMThen.estimated().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_countKeyValue, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ClassTipsProtocol {
    /// 创建UI
    //: private func createUI() {
    private func prize() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(str_levelValue) + str_currentData.replacingOccurrences(of: "premium", with: "i") + "tion").localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func saveFromRawMaterial() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? kLet_noText : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.corners(width: contentView.width,
                                //: height: contentView.height,
                                height: contentView.height,
                                //: corners: [.topLeft, .topRight],
                                corners: [.topLeft, .topRight],
                                //: cornerRadii: .init(width: 8, height: 8))
                                cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:kLet_likeTitle)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_indexText : kLet_likeTitle)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kLet_appMarginText)
                make.height.equalTo(206 + kLet_appMarginText)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:kLet_likeTitle)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_indexText : kLet_likeTitle)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kLet_appMarginText)
                make.height.equalTo(206 + kLet_appMarginText)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kLet_appMarginText)
                make.height.equalTo(56 + kLet_appMarginText)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func squareTotaleraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.behindTorso()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.tillToAGreaterExtent()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.saveer(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kLet_voiceText.quantityingTime(eventID: kLet_partyName, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
