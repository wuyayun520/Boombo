
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_viewReadValue:[UInt8] = [0xf8,0xff,0xf8,0xe5,0xb9,0xf2,0xfe,0xf5,0xf4,0xe3,0xab,0xb8,0xb1,0xf9,0xf0,0xe2,0xb1,0xff,0xfe,0xe5,0xb1,0xf3,0xf4,0xf4,0xff,0xb1,0xf8,0xfc,0xe1,0xfd,0xf4,0xfc,0xf4,0xff,0xe5,0xf4,0xf5]

private func mainTrue(show num: UInt8) -> UInt8 {
    return num ^ 145
}

/*: "uid" :*/
fileprivate let str_kitText:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let str_coverValue:String = "interval list bottom let touchheadPic"

/*: "nickname" :*/
fileprivate let str_infoValue:[Character] = ["n","i","c","k","n","a","m"]
fileprivate let str_iconValue:[Character] = ["e"]

/*: "birthday" :*/
fileprivate let str_intervalSectionTitle:String = "birviewh"
fileprivate let str_colorValue:String = "daframe"

/*: "videoUrl" :*/
fileprivate let str_centerValue:String = "videoUrlequal to"

/*: "cover" :*/
fileprivate let str_multipleData:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let str_tableValue:[Character] = ["l","o","c"]
fileprivate let str_toData:String = "amodeon"

/*: "cmd" :*/
fileprivate let str_messageCenterText:[UInt8] = [0x4a,0x44,0x4d]

private func constantColor(scale num: UInt8) -> UInt8 {
    return num ^ 41
}

/*: "refuseCall" :*/
fileprivate let str_ofData:String = "phone selfrefuseC"
fileprivate let str_makeMyData:[Character] = ["a","l","l"]

/*: "data" :*/
fileprivate let str_frameName:[UInt8] = [0x5,0x0,0x15,0x0]

private func nearSucceed(section num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "Call interruption" :*/
fileprivate let str_partyTitle:[Character] = ["C","a","l","l"," ","i"]
fileprivate let str_cellValue:[Character] = ["n","t","e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let str_sizeValue:String = "officialop"

/*: "logId" :*/
fileprivate let str_makeValue:[UInt8] = [0x4e,0x4d,0x45,0x6b,0x46]

/*: "cancelCall" :*/
fileprivate let str_toTitle:String = "cacellce"

/*: "No answer" :*/
fileprivate let str_actualName:[Character] = ["N"]
fileprivate let str_titleData:[Character] = ["o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let str_dataName:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let str_meValue:[Character] = ["S","h","e"," ","h","a","s"," "]
fileprivate let str_theValue:[Character] = ["h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let str_changeData:String = "cclosence"
fileprivate let str_sharedTitle:String = "lSexmodel background"

/*: "He has hung up" :*/
fileprivate let str_customValue:[Character] = ["H","e"," ","h","a","s"," ","h","u"]
fileprivate let str_buttonTargetValue:[Character] = ["n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let str_userData:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let str_nameSizeBackData:[Character] = ["s","t","o","p","S","e"]
fileprivate let str_roomData:String = "border"

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_layerPhotoData:[UInt8] = [0x2,0x27,0x2c,0x2e,0x1f,0x1f,0x22,0x1c,0x22,0x1e,0x27,0x2d,0xd9,0x1c,0x2b,0x1e,0x1d,0x22,0x2d,0xe5,0xd9,0x1c,0x1a,0x25,0x25,0xd9,0x21,0x1a,0x2c,0xd9,0x1e,0x27,0x1d,0x1e,0x1d]

fileprivate func nooksAndCranniesManager(model num: UInt8) -> UInt8 {
    let value = Int(num) + 71
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let str_moveMakeName:String = "return name info type imageThe o"
fileprivate let str_loadFileTitle:String = "progress app let hun"
fileprivate let str_alwaysModelName:String = "path success text sizeg "
fileprivate let str_giftTitle:[Character] = ["u","p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let str_requestValue:String = "typshow"

/*: "jumpUrl" :*/
fileprivate let str_sendEqualData:[Character] = ["j"]
fileprivate let str_scaleValue:String = "umpUrlself super"

/*: "Cancel" :*/
fileprivate let str_modeRoomName:String = "Cancelhidden class interaction"

/*: "Purchase" :*/
fileprivate let str_equalCustomToName:String = "table leading atPurch"
fileprivate let str_tableData:[Character] = ["a","s","e"]

/*: "&type=3" :*/
fileprivate let str_keyData:String = "&type=3burn open actual to as"

/*: "addBean" :*/
fileprivate let str_resignText:[Character] = ["a","d","d","B","e"]
fileprivate let str_modelStyleData:String = "atime"

/*: "refuseSex" :*/
fileprivate let str_picData:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let str_viewData:String = "be color frameonAcceptC"
fileprivate let str_locationData:String = "ALL"

/*: "acceptCall" :*/
fileprivate let str_clickBetweenData:String = "acceptCmake size top path self"
fileprivate let str_layerName:[Character] = ["a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let str_labelData:String = "Calls aequal length extension"
fileprivate let str_modelContent:String = "re calens let self var"
fileprivate let str_labText:[Character] = ["n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let str_phoneTitle:String = "intimate make to outThe ca"
fileprivate let str_giftModelBackgroundData:[Character] = ["n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SubmitExtraObjectProtocol.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let kLet_domainContent = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class SubmitExtraObjectProtocol: NameViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: StandardChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: InfoEqualCallPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: NameEffectThen? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: ConditionView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        DataSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DataSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DataSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_viewReadValue.map{mainTrue(show: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! DataViewController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.theDrop()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            gestureAuth()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            nameRoomLogStart(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DataSocketDelegate.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.bindRoom()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        viewSubviews()
        //: setupSubViewsConstraint()
        imageDownConstraint()
        //: initNotificationObserver()
        center()
        //: req_callGetUserInfo()
        fruitView()
        //: timeoutRules()
        active()
        //: setUpBGMPlayer()
        cellName()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.checkedInPreview()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if WithAdministratorAppManager.share.appConfigMode.disableShootScreen {
            if WithAdministratorAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CoverShieldView(frame: CGRect(x: 0, y: 0, width: kLet_senseContent, height: kLet_environmentClickValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        DataSocketDelegate.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.fundamental()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: VideoReactiveCompatible = {
        //: let v = TalkingBGVideoView()
        let v = VideoReactiveCompatible()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.ginMill(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: MReactiveCompatible = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = MReactiveCompatible(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: AlterWindowView = {
        //: let v = TalkingVideoWindowView()
        let v = AlterWindowView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kLet_senseContent - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if MeanSunLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: kLet_messageContent + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.install()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: SmartCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = SmartCloudDelegate()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension SubmitExtraObjectProtocol {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func fruitView() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        SmartCloudDelegate.styleCoverCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_kitText.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_coverValue.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(str_infoValue) + String(str_iconValue))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(str_intervalSectionTitle.replacingOccurrences(of: "view", with: "t") + str_colorValue.replacingOccurrences(of: "frame", with: "y"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(str_centerValue.prefix(8)))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(str_multipleData))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(str_tableValue) + str_toData.replacingOccurrences(of: "mode", with: "ti"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.collectionInfo()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.infoGiftStatus()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func infoGiftStatus() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = InfoCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = InfoCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.unseeable(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.exaggerateShared(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension SubmitExtraObjectProtocol {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func gestureAuth() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        LaissezPasserThen.polaroidCameraMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.atShow(showMsg: kLet_viewData)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (String(str_ofData.suffix(7)) + String(str_makeMyData)), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_kitText.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.popThroughSpeakeasy()
                //: return
                return
            }

            //: self.startPreview()
            self.checkedInPreview()
            //: self.stopBGM()
            self.stream()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = InfoEqualCallPlayer.baseballPlayer(type: .Response)
            //: self.startBGM()
            self.theDrop()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func checkedInPreview() {
        //: self.videoManager.startPreview()
        self.videoManager.answerFile()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func fundamental() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.stream()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        SubmitMediaThen.clout()
        //: TalkingSocketManager.shared.callerDelegate = nil
        DataSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        DataSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        DataSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func popThroughSpeakeasy(animated: Bool = true) {
        //: self.stopBGM()
        self.stream()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func install() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.labEqual()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.infoGiftStatus()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func enablePath() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.firstYear()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.infoGiftStatus()
        // 展示投诉说明飘屏（男性）
//        self.show_videoFloatScreenAnimation()
    }

    /// 展示投诉说明飘屏（男性）
//    private func show_videoFloatScreenAnimation() {
//        guard reportView == nil else { return }
//
//        reportView = ConditionView()
//        view.addSubview(reportView!)
//        reportView?.show_animation()
//    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension SubmitExtraObjectProtocol {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func center() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(doContext(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(dataSize(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: kLet_errorTitle,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(visualAspect(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_successTitle,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_domainContent.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func doContext(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            popThroughSpeakeasy()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func visualAspect(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard DataSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        DataSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DataSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DataSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.fossilization()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func dataSize(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.infoGiftStatus()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension SubmitExtraObjectProtocol: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.atShow(showMsg: (String(str_partyTitle) + String(str_cellValue)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (str_sizeValue.replacingOccurrences(of: "official", with: "st") + "Call"), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_makeValue.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (str_toTitle.replacingOccurrences(of: "cell", with: "n") + "lCall"), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_makeValue.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (String(str_ofData.suffix(7)) + String(str_makeMyData)), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_kitText.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.network()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                DataSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            popThroughSpeakeasy()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension SubmitExtraObjectProtocol {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func cellName() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? VoiceFromInsetTarget.Call : VoiceFromInsetTarget.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = InfoEqualCallPlayer.baseballPlayer(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func theDrop() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.ascertain()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func stream() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.handleStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension SubmitExtraObjectProtocol {
    /// 超时定时器
    //: private func timeoutRules() {
    private func active() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_guideSumData : self.chatModel?.exceptToTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            consultation()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.consultation()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func consultation() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !DataSocketDelegate.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.atShow(showMsg: (String(str_actualName) + String(str_titleData)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (str_toTitle.replacingOccurrences(of: "cell", with: "n") + "lCall"), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_makeValue.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (String(str_ofData.suffix(7)) + String(str_makeMyData)), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_kitText.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        DataSocketDelegate.shared.demonstrateInfo(info: socketDict)
        //: popCurrentViewController()
        popThroughSpeakeasy()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension SubmitExtraObjectProtocol {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func disappear(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_makeValue.map{$0^34}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(str_dataName))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.nextWith(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.roomStage()
            //: createTalkRoom()
            enablePath()
            //: self.stopBGM()
            self.stream()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func nameRoomLogStart(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.labEqual()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.ginMill(show: false)
        //: self.stopBGM()
        self.stream()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.roomStage()
        //: self.createTalkRoom()
        self.enablePath()
    }
}

// MARK: - MediaCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension SubmitExtraObjectProtocol: MediaCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func fileNameMode(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_meValue) + String(str_theValue)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(str_changeData.replacingOccurrences(of: "close", with: "a") + String(str_sharedTitle.prefix(4)))].stringValue == MiniSubqueryConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_customValue) + String(str_buttonTargetValue)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.atShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.network()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DataSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        popThroughSpeakeasy()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func callArray(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        nameRoomLogStart(logId: json[String(bytes: str_makeValue.map{$0^34}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func backgroundTo(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_userData))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_meValue) + String(str_theValue)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_nameSizeBackData) + str_roomData.replacingOccurrences(of: "border", with: "x"))].stringValue == MiniSubqueryConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_customValue) + String(str_buttonTargetValue)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if WithAdministratorAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_layerPhotoData.map{nooksAndCranniesManager(model: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_moveMakeName.suffix(5)) + "ther" + String(str_loadFileTitle.suffix(4)) + String(str_alwaysModelName.suffix(2)) + String(str_giftTitle)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.atShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.network()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DataSocketDelegate.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.portionDismiss()
        //: popCurrentViewController()
        popThroughSpeakeasy()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func star(msg: String, data: [String: Any]) {
        //: guard WithAdministratorAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(str_requestValue.replacingOccurrences(of: "show", with: "e"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_sendEqualData) + String(str_scaleValue.prefix(6)))] as? String else { return }
            //: let httpsUrl = WithAdministratorAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = WithAdministratorAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = HandleBladeConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: LeadingEqualReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            LeadingEqualReactiveCompatible.share.loadWith(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            VoiceAlertShow.enablelineMedium(title: nil,
                                              //: message: msg,
                                              message: msg,
                                              //: leftBtnTitle: "Cancel".localized,
                                              leftBtnTitle: (String(str_modeRoomName.prefix(6))).localized,
                                              //: rightBtnTitle: "Purchase".localized) {
                                              rightBtnTitle: (String(str_equalCustomToName.suffix(5)) + String(str_tableData)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                VoiceAlertShow.bias()
                //: } rightBlock: {
            } rightBlock: {
                //: LeadingEqualReactiveCompatible.share.func__pushToRechargeAlert(appendParams: "&type=3")
                LeadingEqualReactiveCompatible.share.squash(appendParams: (String(str_keyData.prefix(7))))
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func doingReply(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.personalIdentity(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func socketToData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_resignText) + str_modelStyleData.replacingOccurrences(of: "time", with: "n"))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.publicTransportPlay(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.weltBegin(point: addBean)
        }
    }
}

// MARK: - InstanceObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension SubmitExtraObjectProtocol: InstanceObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func noneFileShow(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_meValue) + String(str_theValue)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_picData))].stringValue == MiniSubqueryConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_customValue) + String(str_buttonTargetValue)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.atShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.network()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DataSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        popThroughSpeakeasy()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func designModel(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        nameRoomLogStart(logId: json[String(bytes: str_makeValue.map{$0^34}, encoding: .utf8)!].intValue)
    }
}

// MARK: - StartErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension SubmitExtraObjectProtocol: StartErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func number1(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(str_ofData.suffix(7)) + String(str_makeMyData)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.atShow(showMsg: (String(str_actualName) + String(str_titleData)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.network()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                DataSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            popThroughSpeakeasy()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_viewData.suffix(9)) + str_locationData.lowercased()) && errorNo == 101 { // 接听通话时，余额不足
            //: LeadingEqualReactiveCompatible.share.func__pushToRechargeAlert(appendParams: "&type=3")
            LeadingEqualReactiveCompatible.share.squash(appendParams: (String(str_keyData.prefix(7))))

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_viewData.suffix(9)) + str_locationData.lowercased()) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.atShow(showMsg: msg)
            //: popCurrentViewController()
            popThroughSpeakeasy()
        }
    }
}

// MARK: - ModelQuantityObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension SubmitExtraObjectProtocol: ModelQuantityObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func appText() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (String(str_clickBetweenData.prefix(7)) + String(str_layerName)), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_kitText.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func comeOutAtRequest() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.atShow(showMsg: (String(str_labelData.prefix(7)) + String(str_modelContent.prefix(5)) + String(str_labText)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (str_toTitle.replacingOccurrences(of: "cell", with: "n") + "lCall"), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_makeValue.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.atShow(showMsg: (String(str_labelData.prefix(7)) + String(str_modelContent.prefix(5)) + String(str_labText)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (String(str_ofData.suffix(7)) + String(str_makeMyData)), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_kitText.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.atShow(showMsg: (String(str_phoneTitle.suffix(6)) + "ll has e" + String(str_giftModelBackgroundData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_messageCenterText.map{constantColor(scale: $0)}, encoding: .utf8)!: (str_sizeValue.replacingOccurrences(of: "official", with: "st") + "Call"), String(bytes: str_frameName.map{nearSucceed(section: $0)}, encoding: .utf8)!: [String(bytes: str_makeValue.map{$0^34}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        popThroughSpeakeasy()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func videoVoice(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.profile(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func inviteTo(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.ginMill(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.conversationCamera(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.infoGiftStatus()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func squareSearched() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        DataSocketDelegate.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = NameEffectThen.reconstructAddView()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.toText()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.cancelAcross()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.actionController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        popThroughSpeakeasy()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func textSucceed() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension SubmitExtraObjectProtocol {
    /// 添加视图
    //: private func setupSubviews() {
    private func viewSubviews() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.feedbackEnable(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func imageDownConstraint() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
