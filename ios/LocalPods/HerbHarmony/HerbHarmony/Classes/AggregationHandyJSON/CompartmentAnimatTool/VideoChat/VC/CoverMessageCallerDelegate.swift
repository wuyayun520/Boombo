
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_styleData:[UInt8] = [0x82,0x85,0x82,0x9f,0xc3,0x88,0x84,0x8f,0x8e,0x99,0xd1,0xc2,0xcb,0x83,0x8a,0x98,0xcb,0x85,0x84,0x9f,0xcb,0x89,0x8e,0x8e,0x85,0xcb,0x82,0x86,0x9b,0x87,0x8e,0x86,0x8e,0x85,0x9f,0x8e,0x8f]

/*: "uid" :*/
fileprivate let str_messageTitle:[UInt8] = [0xa9,0xb5,0xb8]

private func intervalervalGift(now num: UInt8) -> UInt8 {
    return num ^ 220
}

/*: "headPic" :*/
fileprivate let str_rangeName:String = "headPiback let count quantity"
fileprivate let str_tingData:[Character] = ["c"]

/*: "nickname" :*/
fileprivate let str_resultData:String = "NICKNAME"

/*: "birthday" :*/
fileprivate let str_buttonTitle:String = "nameirthday"

/*: "videoUrl" :*/
fileprivate let str_atName:String = "viduser"

/*: "location" :*/
fileprivate let str_loadVideoData:[Character] = ["l"]
fileprivate let str_logLiveText:String = "postcatipostn"

/*: "cmd" :*/
fileprivate let str_toTitle:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let str_firstHideContent:String = "refm"

/*: "data" :*/
fileprivate let str_makeData:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let str_sizeTitle:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let str_modelPostData:String = "stocontainer"

/*: "logId" :*/
fileprivate let str_bottomData:[UInt8] = [0x5c,0x5f,0x57,0x79,0x54]

/*: "cancelCall" :*/
fileprivate let str_hiddenTitle:String = "of"
fileprivate let str_colorTitle:String = "logce"

/*: "No answer" :*/
fileprivate let str_norValue:String = "live return to birthNo a"
fileprivate let str_deviceName:[Character] = ["r"]

/*: "duration" :*/
fileprivate let str_iconMakeValue:String = "durgifti"
fileprivate let str_contentValue:String = "omanager"

/*: "She has hung up" :*/
fileprivate let str_appName:[Character] = ["S","h"]
fileprivate let str_beData:String = "none to index ende ha"

/*: "cancelSex" :*/
fileprivate let str_tempValue:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let str_onContent:String = "He has hon position cover add"
fileprivate let str_callMediumTitle:[Character] = ["u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let str_emptyEnablelinePiData:String = "orientation arrayendType"

/*: "stopSex" :*/
fileprivate let str_normalData:[Character] = ["s","t","o","p","S"]
fileprivate let str_insideValue:[Character] = ["e","x"]

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_pathTextContent:[UInt8] = [0x5a,0x7d,0x60,0x66,0x75,0x75,0x7a,0x70,0x7a,0x76,0x7d,0x67,0x33,0x70,0x61,0x76,0x77,0x7a,0x67,0x3f,0x33,0x70,0x72,0x7f,0x7f,0x33,0x7b,0x72,0x60,0x33,0x76,0x7d,0x77,0x76,0x77]

private func computerSimulationSize(index num: UInt8) -> UInt8 {
    return num ^ 19
}

/*: "The other hung up the phone" :*/
fileprivate let str_centerTitle:[Character] = ["T","h","e"," ","o"]
fileprivate let str_requestText:String = "message accelerate creditther "
fileprivate let str_shouldValue:String = "no namep th"

/*: "type" :*/
fileprivate let str_arrayValue:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let str_pageValue:String = "player make value value stringjumpU"
fileprivate let str_modelData:[Character] = ["r","l"]

/*: "Cancel" :*/
fileprivate let str_videoReasonText:String = "Cancelimage constraint error"

/*: "Purchase" :*/
fileprivate let str_showData:String = "empty normalPurchase"

/*: "&type=4" :*/
fileprivate let str_intervalTitle:String = "&type=4model true self print self"

/*: "addBean" :*/
fileprivate let str_sendData:[Character] = ["a","d","d","B","e"]
fileprivate let str_modeModelLimitName:String = "willn"

/*: "refuseSex" :*/
fileprivate let str_explainName:String = "colore"
fileprivate let str_proData:String = "phone heightfuseSex"

/*: "onAcceptCall" :*/
fileprivate let str_styleContent:[Character] = ["o","n","A","c","c","e","p","t","C"]
fileprivate let str_genderValue:String = "makel"

/*: "acceptCall" :*/
fileprivate let str_contentTitle:String = "accepextra"
fileprivate let str_sizeName:[Character] = ["C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let str_commentAtTitle:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n","c","e"]
fileprivate let str_actualModelValue:[Character] = ["l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let str_priceData:[Character] = ["T","h","e"," ","c"]
fileprivate let str_errorTitle:String = "invite talk as letall ha"
fileprivate let str_arrayAvailableValue:String = "s endedevent title"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CoverMessageCallerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let kLet_liveText = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class CoverMessageCallerDelegate: NameViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: StandardChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: InfoEqualCallPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: InfoSkirtReactiveCompatible? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        DataSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DataSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DataSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_styleData.map{$0^235}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! DataViewController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.stem()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            atTo()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DataSocketDelegate.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.astatine()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        soilIcon()
        //: setupSubViewsConstraint()
        currentSearch()
        //: initNotificationObserver()
        foursquare()
        //: req_callGetUserInfo()
        withModel()
        //: timeoutRules()
        picView()
        //: resumeTalk()
        resumeThroughProceedTalk()
        //: setUpBGMPlayer()
        videoPlayer()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if WithAdministratorAppManager.share.appConfigMode.disableShootScreen {
            if WithAdministratorAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CoverShieldView(frame: CGRect(x: 0, y: 0, width: kLet_senseContent, height: kLet_environmentClickValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        DataSocketDelegate.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.currentClick()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: PopTargetSumeractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = PopTargetSumeractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: ManagerReactiveCompatible = {
        //: let m = TalkingVoiceChatManager()
        let m = ManagerReactiveCompatible()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension CoverMessageCallerDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func withModel() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        SmartCloudDelegate.styleCoverCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_messageTitle.map{intervalervalGift(now: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_rangeName.prefix(6)) + String(str_tingData))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_resultData.lowercased())].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(str_buttonTitle.replacingOccurrences(of: "name", with: "b"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(str_atName.replacingOccurrences(of: "user", with: "e") + "oUrl")].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(str_loadVideoData) + str_logLiveText.replacingOccurrences(of: "post", with: "o"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.dataFormatting()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension CoverMessageCallerDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func atTo() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        LaissezPasserThen.withLoad(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.atShow(showMsg: kLet_viewData)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_firstHideContent.replacingOccurrences(of: "m", with: "us") + "eCall"), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_messageTitle.map{intervalervalGift(now: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.pushExamineed()
                //: return
                return
            }

            //: self.stopBGM()
            self.postImage()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = InfoEqualCallPlayer.baseballPlayer(type: .Response)
            //: self.startBGM()
            self.stem()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func currentClick() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.postImage()
        //: TalkingSocketManager.shared.callerDelegate = nil
        DataSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        DataSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        DataSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func pushExamineed(animated: Bool = true) {
        //: self.stopBGM()
        self.postImage()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func readingRoom() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.finishClick(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func resumeThroughProceedTalk() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            readingRoom()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension CoverMessageCallerDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func foursquare() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(nameObserver(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(playThan(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_successTitle,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_liveText.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func nameObserver(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            pushExamineed()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func playThan(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard DataSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        DataSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DataSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DataSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.chemicalAgentSize(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension CoverMessageCallerDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.atShow(showMsg: (String(str_sizeTitle)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_modelPostData.replacingOccurrences(of: "container", with: "p") + "Call"), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_bottomData.map{$0^48}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_hiddenTitle.replacingOccurrences(of: "of", with: "c") + str_colorTitle.replacingOccurrences(of: "log", with: "an") + "lCall"), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_bottomData.map{$0^48}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_firstHideContent.replacingOccurrences(of: "m", with: "us") + "eCall"), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_messageTitle.map{intervalervalGift(now: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.network()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                DataSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            pushExamineed()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.pathReplySelected(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension CoverMessageCallerDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func videoPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? VoiceFromInsetTarget.Call : VoiceFromInsetTarget.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = InfoEqualCallPlayer.baseballPlayer(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func stem() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.ascertain()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func postImage() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.handleStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension CoverMessageCallerDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func picView() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_guideSumData : self.chatModel?.exceptToTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            online()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.online()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func online() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !DataSocketDelegate.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.atShow(showMsg: (String(str_norValue.suffix(4)) + "nswe" + String(str_deviceName)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_hiddenTitle.replacingOccurrences(of: "of", with: "c") + str_colorTitle.replacingOccurrences(of: "log", with: "an") + "lCall"), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_bottomData.map{$0^48}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_firstHideContent.replacingOccurrences(of: "m", with: "us") + "eCall"), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_messageTitle.map{intervalervalGift(now: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        DataSocketDelegate.shared.demonstrateInfo(info: socketDict)
        //: popCurrentViewController()
        pushExamineed()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension CoverMessageCallerDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func quantityercalate(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_bottomData.map{$0^48}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(str_iconMakeValue.replacingOccurrences(of: "gift", with: "at") + str_contentValue.replacingOccurrences(of: "manager", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.timeUser(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.notExecute()
            //: createTalkRoom()
            readingRoom()
            //: self.stopBGM()
            self.postImage()
        }
    }
}

// MARK: - MediaCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension CoverMessageCallerDelegate: MediaCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func fileNameMode(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_appName) + String(str_beData.suffix(4)) + "s hung up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_tempValue))].stringValue == MiniSubqueryConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_onContent.prefix(8)) + String(str_callMediumTitle)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.atShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.network()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DataSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        pushExamineed()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func callArray(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_bottomData.map{$0^48}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.postImage()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.notExecute()
        //: self.createTalkRoom()
        self.readingRoom()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func backgroundTo(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_emptyEnablelinePiData.suffix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_appName) + String(str_beData.suffix(4)) + "s hung up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_normalData) + String(str_insideValue))].stringValue == MiniSubqueryConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_onContent.prefix(8)) + String(str_callMediumTitle)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if WithAdministratorAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_pathTextContent.map{computerSimulationSize(index: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_centerTitle) + String(str_requestText.suffix(5)) + "hung u" + String(str_shouldValue.suffix(4)) + "e phone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.atShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.network()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DataSocketDelegate.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.funcLegacyDismissStraddle()
        //: self.popCurrentViewController()
        self.pushExamineed()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kLet_errorName, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func star(msg: String, data: [String: Any]) {
        //: guard WithAdministratorAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard WithAdministratorAppManager.share.appStatus == SectionFreckleTextConvertible.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(str_arrayValue))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_pageValue.suffix(5)) + String(str_modelData))] as? String else { return }
            //: let httpsUrl = WithAdministratorAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = WithAdministratorAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = HandleBladeConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: LeadingEqualReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            LeadingEqualReactiveCompatible.share.loadWith(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            VoiceAlertShow.enablelineMedium(title: nil,
                                              //: message: msg,
                                              message: msg,
                                              //: leftBtnTitle: "Cancel".localized,
                                              leftBtnTitle: (String(str_videoReasonText.prefix(6))).localized,
                                              //: rightBtnTitle: "Purchase".localized) {
                                              rightBtnTitle: (String(str_showData.suffix(8))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                VoiceAlertShow.bias()
                //: } rightBlock: {
            } rightBlock: {
                //: LeadingEqualReactiveCompatible.share.func__pushToRechargeAlert(appendParams: "&type=4")
                LeadingEqualReactiveCompatible.share.squash(appendParams: (String(str_intervalTitle.prefix(7))))
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func socketToData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_sendData) + str_modeModelLimitName.replacingOccurrences(of: "will", with: "a"))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.demonstrateToReleasingHormoneRelease(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.available(point: addBean)
        }
    }
}

// MARK: - InstanceObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension CoverMessageCallerDelegate: InstanceObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func noneFileShow(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_appName) + String(str_beData.suffix(4)) + "s hung up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(str_explainName.replacingOccurrences(of: "color", with: "r") + String(str_proData.suffix(7)))].stringValue == MiniSubqueryConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_onContent.prefix(8)) + String(str_callMediumTitle)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.atShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.network()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            DataSocketDelegate.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        pushExamineed()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func designModel(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_bottomData.map{$0^48}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.postImage()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.notExecute()
        //: self.createTalkRoom()
        self.readingRoom()
    }
}

// MARK: - StartErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension CoverMessageCallerDelegate: StartErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func number1(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (str_firstHideContent.replacingOccurrences(of: "m", with: "us") + "eCall") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.atShow(showMsg: (String(str_norValue.suffix(4)) + "nswe" + String(str_deviceName)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.network()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                DataSocketDelegate.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.pushExamineed()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_styleContent) + str_genderValue.replacingOccurrences(of: "make", with: "al")) && errorNo == 101 { // 接听通话时，余额不足
            //: LeadingEqualReactiveCompatible.share.func__pushToRechargeAlert(appendParams: "&type=4")
            LeadingEqualReactiveCompatible.share.squash(appendParams: (String(str_intervalTitle.prefix(7))))

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_styleContent) + str_genderValue.replacingOccurrences(of: "make", with: "al")) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.atShow(showMsg: msg)
            //: self.popCurrentViewController()
            self.pushExamineed()
        }
    }
}

// MARK: - ModelObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension CoverMessageCallerDelegate: ModelObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func inform() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_contentTitle.replacingOccurrences(of: "extra", with: "t") + String(str_sizeName)), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_messageTitle.map{intervalervalGift(now: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func equalAdd() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.atShow(showMsg: (String(str_commentAtTitle) + String(str_actualModelValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_hiddenTitle.replacingOccurrences(of: "of", with: "c") + str_colorTitle.replacingOccurrences(of: "log", with: "an") + "lCall"), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_bottomData.map{$0^48}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.atShow(showMsg: (String(str_commentAtTitle) + String(str_actualModelValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_firstHideContent.replacingOccurrences(of: "m", with: "us") + "eCall"), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_messageTitle.map{intervalervalGift(now: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.atShow(showMsg: (String(str_priceData) + String(str_errorTitle.suffix(6)) + String(str_arrayAvailableValue.prefix(7))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            DataSocketDelegate.shared.demonstrateInfo(info: [String(bytes: str_toTitle.reversed(), encoding: .utf8)!: (str_modelPostData.replacingOccurrences(of: "container", with: "p") + "Call"), String(bytes: str_makeData.reversed(), encoding: .utf8)!: [String(bytes: str_bottomData.map{$0^48}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.funcLegacyDismissStraddle()
        //: self.popCurrentViewController()
        self.pushExamineed()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func viewPlay(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.another(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func userBlock(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.alongSet(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func dubMethod() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        DataSocketDelegate.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = InfoSkirtReactiveCompatible.pushView()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.actionController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.toTime()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.politicalArena(talkingTime)
        //: popCurrentViewController()
        pushExamineed()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func atError(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.politicalArena(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func meet(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.year(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension CoverMessageCallerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func soilIcon() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func currentSearch() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
