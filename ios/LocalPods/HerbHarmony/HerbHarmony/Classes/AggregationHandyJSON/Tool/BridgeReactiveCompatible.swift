
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let str_userName:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/"]
fileprivate let str_errorSourceData:String = "extension infogiftList"

/*: "type" :*/
fileprivate let str_actualTitle:[UInt8] = [0x60,0x6d,0x64,0x71]

/*: "mf/chatRoom/giftList" :*/
fileprivate let str_popTitle:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","g","i","f","t","L","i","s","t"]

/*: "live/giftList" :*/
fileprivate let str_afterPhotoValue:[Character] = ["l","i","v","e","/","g","i"]
fileprivate let str_labData:[Character] = ["f","t","L","i","s","t"]

/*: "party/giftList" :*/
fileprivate let str_imageName:String = "party/gitime will sex view"
fileprivate let str_roomTitle:[Character] = ["f","t","L","i","s","t"]

/*: "IM/getMessageList" :*/
fileprivate let str_colorContent:String = "hidden top component let messageIM/ge"
fileprivate let str_indicatorValue:String = "saglabel"
fileprivate let str_equalValue:[Character] = ["L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let str_sexValue:String = "mf/busself self view return"
fileprivate let str_viewName:String = "index var data self inendMsg"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let str_blockValue:String = "mf/cstyle model type"
fileprivate let str_pushNameText:[Character] = ["o","o","m","/","s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let str_managerWhiteTitle:String = "mf/busiout pair"
fileprivate let str_controlName:String = "ness/video hidden"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let str_textValue:String = "lab makemf/c"
fileprivate let str_roomText:String = "var simultaneouslyhatRoo"
fileprivate let str_atTitle:String = "ndGiftmake view top page mode"

/*: "live/sendGift" :*/
fileprivate let str_buttonData:[Character] = ["l","i","v","e","/","s","e","n","d","G","i","f","t"]

/*: "party/sendGift" :*/
fileprivate let str_styleName:String = "party/top with frame extra none"
fileprivate let str_positionRawAsValue:[Character] = ["s"]
fileprivate let str_equalModelData:String = "edge atendGift"

/*: "mf/user/getChatInfo" :*/
fileprivate let str_shareBackData:String = "do tool room topmf/u"
fileprivate let str_equalTitle:String = "self manager hung as bargetCha"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let str_giftTitle:[Character] = ["I","M","/","g","e","t","M","e","s","s","a","g","e","U","s","e","r","I"]
fileprivate let str_scaleTitle:String = "N"
fileprivate let str_appButtonAfterName:String = "foListsystem path in"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let str_extraData:String = "mf/crmoment self"
fileprivate let str_liveValue:String = "hoLikeMeLbottom then target"
fileprivate let str_bottomData:[Character] = ["i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let str_backgroundValue:[Character] = ["m","f","/","c","r","u","s","h","/","i","g"]
fileprivate let str_answerTopTitle:String = "noattributee"

/*: "mf/crush/like" :*/
fileprivate let str_pathTitle:String = "mf/make path"
fileprivate let str_clearTitle:[Character] = ["c","r","u","s","h","/","l","i","k","e"]

/*: "mf/user/getRelation" :*/
fileprivate let str_likeData:String = "mf/usesource block pause"
fileprivate let str_indexLineModelText:String = "lainsideon"

/*: "user/attention" :*/
fileprivate let str_tempName:[UInt8] = [0xcd,0xcb,0xdd,0xca,0x97,0xd9,0xcc,0xcc,0xdd,0xd6,0xcc,0xd1,0xd7,0xd6]

private func labLive(model num: UInt8) -> UInt8 {
    return num ^ 184
}

/*: "user/removeAttention" :*/
fileprivate let str_areaData:[Character] = ["u","s","e","r","/","r","e","m","o","v","e","A","t","t","e","n","t","i"]
fileprivate let str_randomText:String = "odata"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let str_giftValue:[UInt8] = [0xfd,0xdf,0xd0,0x99,0xca,0x9e,0xdb,0xdf,0xcc,0xd0,0x9e,0xce,0xd1,0xd7,0xd0,0xca,0xcd,0x9e,0xd7,0xd8,0x9e,0xc7,0xd1,0xcb,0x9e,0xd8,0xd1,0xd2,0xd2,0xd1,0xc9,0x9e,0xdb,0xdf,0xdd,0xd6,0x9e,0xd1,0xca,0xd6,0xdb,0xcc,0xc0]

private func targetAdd(official num: UInt8) -> UInt8 {
    return num ^ 190
}

/*: "follow Success" :*/
fileprivate let str_selectedText:[Character] = ["f","o","l","l","o","w"," ","S"]
fileprivate let str_mValue:[Character] = ["u","c","c","e","s","s"]

/*: "unfollow Success" :*/
fileprivate let str_modelData:String = "uaddfo"
fileprivate let str_buttonValue:String = "cceuser"

/*: "translate/msg" :*/
fileprivate let str_resultName:String = "atan"
fileprivate let str_shareValueTitle:String = "e/msgreturn color name label"

/*: "msgId" :*/
fileprivate let str_bottomName:[UInt8] = [0x64,0x49,0x67,0x73,0x6d]

/*: "targetLng" :*/
fileprivate let str_roomValue:[UInt8] = [0x67,0x6e,0x4c,0x74,0x65,0x67,0x72,0x61,0x74]

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let str_partyValue:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","A","u","t","o","G","r","e","e","t"]
fileprivate let str_clearData:String = "Contentview extension view color"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let str_moveCountHiddenText:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","I","n","t","i","m","a","t","e","P"]
fileprivate let str_makeData:[Character] = ["h","o","t","o"]

/*: "toUid" :*/
fileprivate let str_cellData:[UInt8] = [0x64,0x69,0x55,0x6f,0x74]

/*: "mf/chatRoom/info" :*/
fileprivate let str_numberName:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/"]
fileprivate let str_sharedName:[Character] = ["i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let str_pathData:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","l","i"]
fileprivate let str_objectColorValue:[Character] = ["s","t"]

/*: "roomId" :*/
fileprivate let str_removeValue:[UInt8] = [0x64,0x49,0x6d,0x6f,0x6f,0x72]

/*: "needExtraInfo" :*/
fileprivate let str_languageWithBlockValue:[UInt8] = [0x51,0x5a,0x5a,0x5b,0x7a,0x47,0x4b,0x4d,0x5e,0x76,0x51,0x59,0x50]

private func cellLab(status num: UInt8) -> UInt8 {
    return num ^ 63
}

/*: "mf/chatRoom/check" :*/
fileprivate let str_giftData:String = "app list color tomf/c"
fileprivate let str_voiceListText:[Character] = ["o","o","m","/","c","h","e","c","k"]

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let str_callName:String = "lab create session cell ifmf/chat"
fileprivate let str_contentData:String = "Room/jequal don"

/*: "mf/chatRoom/members" :*/
fileprivate let str_modelSucceedData:String = "request element over view layermf/cha"
fileprivate let str_toolData:String = "equal live/members"

/*: "translate/text" :*/
fileprivate let str_numberSendName:[Character] = ["t","r","a","n","s","l","a"]
fileprivate let str_liveName:String = "te/textwhere file if self view"

/*: "text" :*/
fileprivate let str_mainText:[UInt8] = [0x2c,0x1d,0x30,0x2c]

fileprivate func exitPath(app num: UInt8) -> UInt8 {
    let value = Int(num) + 72
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/user/queryStatus" :*/
fileprivate let str_errorValue:[Character] = ["m","f","/","u","s","e","r","/","q","u","e","r","y"]
fileprivate let str_controlValue:[Character] = ["S","t","a","t","u","s"]

/*: "toUids" :*/
fileprivate let str_imageOriginData:[UInt8] = [0x49,0x44,0x2a,0x3e,0x39,0x48]

fileprivate func atManager(on num: UInt8) -> UInt8 {
    let value = Int(num) - 213
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "call/getCallTab" :*/
fileprivate let str_pathValue:[Character] = ["c","a","l","l","/","g","e"]
fileprivate let str_contentValue:String = "as size apptCallTab"

/*: "mf/user/remark" :*/
fileprivate let str_iconName:String = "mtransform"
fileprivate let str_showKitIconName:String = "/user/score drag else layer add"

/*: "content" :*/
fileprivate let str_layerValue:[UInt8] = [0xee,0xe2,0xe3,0xf9,0xe8,0xe3,0xf9]

/*: "top" :*/
fileprivate let str_pathNameValue:[UInt8] = [0x29,0x32,0x2d]

private func simpleFraction(succeed num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "mf/newFeature/likeList" :*/
fileprivate let str_listValue:String = "data voice indexmf/n"
fileprivate let str_tableTitle:String = "ture/let guard as without"
fileprivate let str_addText:String = "isname"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BridgeReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class BridgeReactiveCompatible: NSObject {
public class BridgeReactiveCompatible: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [CoverThen]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [CoverThen]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func videoEqual(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(str_userName) + String(str_errorSourceData.suffix(8)))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: str_actualTitle.map{$0^20}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func writeCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(str_popTitle))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func circumferenceMethod(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(str_afterPhotoValue) + String(str_labData))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func observerSend(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (String(str_imageName.prefix(8)) + String(str_roomTitle))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func bottomContextCompletion(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(str_colorContent.suffix(5)) + "tMes" + str_indicatorValue.replacingOccurrences(of: "label", with: "e") + String(str_equalValue))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func enableView(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(str_sexValue.prefix(6)) + "iness/s" + String(str_viewName.suffix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func paramCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(str_blockValue.prefix(4)) + "hatR" + String(str_pushNameText))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sendToCompletionSearched(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(str_managerWhiteTitle.prefix(7)) + String(str_controlName.prefix(5)) + "sendGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sunnah(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(str_textValue.suffix(4)) + String(str_roomText.suffix(6)) + "m/se" + String(str_atTitle.prefix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func moveWithoutCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(str_buttonData))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func windowAdd(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(str_styleName.prefix(6)) + String(str_positionRawAsValue) + String(str_equalModelData.suffix(7)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func nationality(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(str_shareBackData.suffix(4)) + "ser/" + String(str_equalTitle.suffix(6)) + "tInfo")
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func postToMessage(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(str_giftTitle) + str_scaleTitle.lowercased() + String(str_appButtonAfterName.prefix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<CoverThen>.deserialize(from: result as? Array) {
            if let datas = Array<CoverThen>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [CoverThen])
                completion(succeed, datas as! [CoverThen])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func effectCompletion(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        kLet_deviceName.course(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func backDown(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(str_extraData.prefix(5)) + "ush/w" + String(str_liveValue.prefix(9)) + String(str_bottomData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func notAdd(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(str_backgroundValue) + str_answerTopTitle.replacingOccurrences(of: "attribute", with: "r"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func acceptAll(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(str_pathTitle.prefix(3)) + String(str_clearTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func isometric(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(str_likeData.prefix(6)) + "r/getRe" + str_indexLineModelText.replacingOccurrences(of: "inside", with: "ti"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func namePlayer(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: str_tempName.map{labLive(model: $0)}, encoding: .utf8)! : (String(str_areaData) + str_randomText.replacingOccurrences(of: "data", with: "n"))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if WithAdministratorAppManager.share.loginUserMode.sex == Gender.female.rawValue {
                    if WithAdministratorAppManager.share.loginUserMode.sex == MiniSubqueryConvertible.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        kLet_meSuccessValue.successMsg(showMsg: String(bytes: str_giftValue.map{targetAdd(official: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        kLet_meSuccessValue.successMsg(showMsg: (String(str_selectedText) + String(str_mValue)).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    kLet_meSuccessValue.successMsg(showMsg: (str_modelData.replacingOccurrences(of: "add", with: "n") + "llow Su" + str_buttonValue.replacingOccurrences(of: "user", with: "ss")).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                kLet_meSuccessValue.atShow(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func toSection(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (str_resultName.replacingOccurrences(of: "at", with: "tr") + "slat" + String(str_shareValueTitle.prefix(5)))
        //: reqModel.params = ["msgId": msgId, "targetLng": IconThen.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: str_bottomName.reversed(), encoding: .utf8)!: msgId, String(bytes: str_roomValue.reversed(), encoding: .utf8)!: IconThen.beforeNameGift(), String(bytes: str_actualTitle.map{$0^20}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func makeBag(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(str_partyValue) + String(str_clearData.prefix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func buildLoad(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(str_moveCountHiddenText) + String(str_makeData))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: str_cellData.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func adopt(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(str_numberName) + String(str_sharedName))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func petNameCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(str_pathData) + String(str_objectColorValue))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func nearBroadcastCompletion(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: str_removeValue.reversed(), encoding: .utf8)!: roomId, String(bytes: str_languageWithBlockValue.map{cellLab(status: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(str_giftData.suffix(4)) + "hatR" + String(str_voiceListText))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func pullThePlug(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(str_callName.suffix(7)) + String(str_contentData.prefix(6)) + "oinOrQuit")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func userCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(str_modelSucceedData.suffix(6)) + "tRoom" + String(str_toolData.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func translateUponCompletion(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (String(str_numberSendName) + String(str_liveName.prefix(7)))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: str_mainText.map{exitPath(app: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func bottomSection(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(str_errorValue) + String(str_controlValue))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: str_imageOriginData.map{atManager(on: $0)}, encoding: .utf8)!: toUids, String(bytes: str_actualTitle.map{$0^20}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func belowTop(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(str_pathValue) + String(str_contentValue.suffix(8)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func origin(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (str_iconName.replacingOccurrences(of: "transform", with: "f") + String(str_showKitIconName.prefix(6)) + "remark")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: str_layerValue.map{$0^141}, encoding: .utf8)!: content, String(bytes: str_pathNameValue.map{simpleFraction(succeed: $0)}, encoding: .utf8)!: isTop, String(bytes: str_cellData.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// I Like列表数据
    /// - Parameter completion: 回调
    //: class func req_getILikeList(completion: @escaping FinishBlock) {
    class func toALesserExtentName(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = NumberryMReactiveCompatible()
        //: reqModel.requestPath = "mf/newFeature/likeList"
        reqModel.requestPath = (String(str_listValue.suffix(4)) + "ewFea" + String(str_tableTitle.prefix(5)) + "likeL" + str_addText.replacingOccurrences(of: "name", with: "t"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_deviceName.clickLocation(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
